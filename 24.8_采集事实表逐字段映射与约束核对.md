# 24.8 采集事实表逐字段映射与约束核对

日期：2026-09-20。状态：设计核对完成；随后首个事实表 migration 已在隔离库验证（见第 12 节），未执行旧数据迁移。

本文件落实“采集事实以旧系统为基础，执行与恢复外围重新设计”。与 24.7 配套；本文件逐列映射比概要字段组更具体，发现差异时同步修正概要。Business 数据库保持不变。

本轮只读核对五张直接承载采集事实/派生结果的表，不能当成所有旧数据库字段已经审计完毕。
自动枚举仅来自旧 bootstrap 快照；运行期 SQL 和实际读写代码另作语义证据，后续 migration 前还需核对其增量变化。

## 1. 范围、证据和使用方式

旧代码提交：`bee141e41b609a583a2b86b34967fce1af404a80`。字段来源：`database/bootstrap/crawler.sql`，SHA-256：`ada6a09f1d6468f2fd7f75ad033bd2acbc23ce9d12e7194fb38b09a1665a7197`。

| 旧表 | 实际列数 | 目标方向 |
|---|---:|---|
| `crawler.channels` | 70 | crawler.channels |
| `crawler.contents` | 56 | crawler.contents |
| `crawler.agent_profiles` | 21 | crawler.agent_results |
| `crawler.finalized_profiles` | 11 | 分解到 Plan / Domain / Publication / Read Model |
| `crawler.channel_about_metric_snapshots` | 15 | crawler.channel_about_metric_snapshots |

合计 **173 个旧字段**，每个字段均有一条处置记录；包含带引号的 `contents."position"`，不能因扫描器只识别普通列名而漏掉。

机器可读副本：`docs/migration/collection-field-map.json`。它是设计记录，不是可直接执行的数据导入配置。
“保留”不等于复制旧约束；“迁移归属/拆分”不等于删除数据；“重建边界”必须用业务样例证明信息仍可表达。
新来源时间、Schema/Hash profile、Plan/Request 关联等新增字段在 24.7 中定义，没有伪装成旧列出现在本表。

## 2. 本轮需要修正的设计判断

1. **视频旧表为 56 列，不是此前初步扫描报出的 55 列。** 差异是带引号的 position；本轮枚举已覆盖。
2. **频道休眠与字段质量信息不能压缩成几个状态。** dormant 周期/复查日、原始国家、is_family_safe、关键词/链接状态等须明确保留。
3. **raw_json 并非全部是 Raw。** videoContentStore 读取 detail.hashtags_observed / keywords_observed，并保存 publication_evidence_conflict、playlist_position 等业务证据；在线证据留 PG，原始巨型响应才外置。
4. **旧内容主键编码包含可变类型。** 旧插入使用 channel:type:source，但 ON CONFLICT(channel_id, source_content_id) 并返回实际 stored key；不能把每次按新类型拼出的字符串当永久身份。新设计选择稳定不透明 key，视频/帖子身份命名空间另辨认。
5. **Hash 格式与算法不能混用。** 旧 publication_item_hash、Agent 输出/输入/版本 Hash 常带 sha256: 前缀，prompt_hash 不带；新 Submission wire Hash 是裸 64 位。这些都不保证使用相同 canonicalization。
6. **Agent Current 与失败请求分开。** 旧失败写入会更新 agent_profiles.status；新架构保留有效 Current、单独表达本轮失败，需要显式映射发布资格/控制台语义，不能误称这是完全不变的存储重命名。
7. **日期不是时间戳。** joined_at 保留 date + precision，不因为统一 UTC 就编造精确加入时刻。

## 3. `crawler.channels`

| 旧字段 | 旧类型 / 可空 | 新位置 | 处理 | 原因与约束 |
|---|---|---|---|---|
| `channel_id` | text / 必填 | crawler.channels.channel_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `channel_url` | text / 必填 | crawler.channels.channel_url | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `handle` | text / 可空 | crawler.channels.handle | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `title` | text / 可空 | crawler.channels.title | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count` | bigint / 可空 | crawler.channels.subscriber_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count_text` | text / 可空 | crawler.channels.subscriber_count_text | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `status` | text / 必填 | crawler.channels.lifecycle_status | 重命名 | 保留 active/dormant/paused/archived/rejected/removed 含义；不是本轮 Plan 是否完成。 |
| `reject_reason` | text / 可空 | crawler.channels.reject_reason | 保留 | 保留拒绝证据；与规范 reason_code 建显式映射，不盲目替换旧字符串。 |
| `priority` | integer / 必填 | control.channel_preferences.manual_priority | 迁移归属 | 保留人工/准入优先级输入；不能当成 Feature 最终计算分数。 |
| `ready_for_agent` | boolean / 必填 | control.agent_requests.input_readiness + read_model.channel_control_state | 重建语义 | 按请求/输入版本判断最低输入；不再以频道永久布尔值授权每轮 Agent。旧标题/订阅条件需进入对应 input_policy 测试。 |
| `agent_status` | text / 必填 | control.agent_requests.state | 迁移归属 | 恢复责任属于具体 Agent 请求；控制台按当前请求投影，旧一行多轮状态不直接导入。 |
| `latest_run_id` | text / 可空 | read_model.channel_control_state.latest_execution_ref | 派生 | 最近执行从 Plan/执行关联产生；不充当写入授权或唯一当前事实来源。 |
| `source_json` | jsonb / 必填 | crawler.channel_candidate_sources + crawler.channels.evidence_json | 拆分映射 | 查询/来源血缘进入来源表；其他有界证据保留，未知 key 在迁移时隔离核查而非静默丢弃。 |
| `created_at` | timestamp with time zone / 必填 | crawler.channels.created_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `updated_at` | timestamp with time zone / 必填 | crawler.channels.updated_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `country` | text / 可空 | crawler.channels.country | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `country_source` | text / 可空 | crawler.channels.country_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `agent_attempts` | integer / 必填 | control.agent_requests.attempts | 迁移归属 | 恢复责任属于具体 Agent 请求；控制台按当前请求投影，旧一行多轮状态不直接导入。 |
| `agent_next_retry_at` | timestamp with time zone / 可空 | control.agent_requests.next_retry_at | 迁移归属 | 恢复责任属于具体 Agent 请求；控制台按当前请求投影，旧一行多轮状态不直接导入。 |
| `agent_error_message` | text / 可空 | control.agent_requests.last_error | 迁移归属 | 恢复责任属于具体 Agent 请求；控制台按当前请求投影，旧一行多轮状态不直接导入。 |
| `country_code` | text / 可空 | crawler.channels.country_code | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `country_canonical_name` | text / 可空 | crawler.channels.country_canonical_name | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `avatar_url` | text / 可空 | crawler.channels.avatar_url | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `summary` | text / 可空 | crawler.channels.summary | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `keywords` | text[] / 必填 | crawler.channels.keywords | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `available_tabs` | text[] / 必填 | crawler.channels.available_tabs | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `about_description` | text / 可空 | crawler.channels.about_description | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `joined_date_text` | text / 可空 | crawler.channels.joined_date_text | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `joined_at` | date / 可空 | crawler.channels.joined_at | 保留 | date + joined_at_precision 保留日期精度，不伪造 UTC 零点的精确加入时刻。 |
| `joined_at_precision` | text / 必填 | crawler.channels.joined_at_precision | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `external_links` | jsonb / 必填 | crawler.channels.external_links | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count_status` | text / 必填 | crawler.channels.subscriber_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count_source` | text / 可空 | crawler.channels.subscriber_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count_observed_at` | timestamp with time zone / 可空 | crawler.channels.subscriber_count_observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count` | bigint / 可空 | crawler.channels.total_view_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count_text` | text / 可空 | crawler.channels.total_view_count_text | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count_status` | text / 必填 | crawler.channels.total_view_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count_source` | text / 可空 | crawler.channels.total_view_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count_observed_at` | timestamp with time zone / 可空 | crawler.channels.total_view_count_observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count` | bigint / 可空 | crawler.channels.total_video_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count_text` | text / 可空 | crawler.channels.total_video_count_text | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count_status` | text / 必填 | crawler.channels.total_video_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count_source` | text / 可空 | crawler.channels.total_video_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count_observed_at` | timestamp with time zone / 可空 | crawler.channels.total_video_count_observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `about_last_observed_at` | timestamp with time zone / 可空 | crawler.channels.about_last_observed_at | 保留 | 沿用明确的原字段名与证据边界；24.7 概要简写不另建第二份同义列。 |
| `about_current_hash` | text / 可空 | crawler.channels.about_current_hash | 保留 | 沿用明确的原字段名与证据边界；24.7 概要简写不另建第二份同义列。 |
| `removed_reason` | text / 可空 | crawler.channels.removed_reason | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `removed_at` | timestamp with time zone / 可空 | crawler.channels.removed_at | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `removed_source` | text / 可空 | crawler.channels.removed_source | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `removed_evidence` | text / 可空 | crawler.channels.removed_evidence | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `dormant_reason` | text / 可空 | crawler.channels.dormant_reason | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `dormant_since` | timestamp with time zone / 可空 | crawler.channels.dormant_since | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `dormant_recheck_day` | date / 可空 | crawler.channels.dormant_recheck_day | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `dormant_last_probe_at` | timestamp with time zone / 可空 | crawler.channels.dormant_last_probe_at | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `dormant_cycle` | integer / 必填 | crawler.channels.dormant_cycle | 保留 | 保留生命周期原始证据和复查进度；不能仅用 lifecycle_changed_at 代替这些不同含义。 |
| `keywords_status` | text / 必填 | crawler.channels.keywords_status | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `available_tabs_status` | text / 必填 | crawler.channels.available_tabs_status | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `description_status` | text / 必填 | crawler.channels.description_status | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `external_links_status` | text / 必填 | crawler.channels.external_links_status | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `rss_url` | text / 可空 | crawler.channels.rss_url | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `vanity_channel_url` | text / 可空 | crawler.channels.vanity_channel_url | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `is_family_safe` | boolean / 可空 | crawler.channels.is_family_safe | 保留 | 首版概要遗漏，补为明确列；保留原始/规范值或字段质量，不凭空合并删除。 |
| `is_verified` | boolean / 可空 | crawler.channels.is_verified | 保留 | true/false/NULL 与 verified/not_verified/unknown 成组约束，不把未知默认 false。 |
| `is_verified_status` | text / 必填 | crawler.channels.is_verified_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `about_identity_last_observed_at` | timestamp with time zone / 可空 | crawler.channels.about_identity_last_observed_at | 保留 | 沿用明确的原字段名与证据边界；24.7 概要简写不另建第二份同义列。 |
| `about_identity_current_hash` | text / 可空 | crawler.channels.about_identity_current_hash | 保留 | 沿用明确的原字段名与证据边界；24.7 概要简写不另建第二份同义列。 |
| `registry_promotion_candidate_id` | bigint / 可空 | crawler.channel_candidates.admitted_plan_id + control.plans.admission_candidate_id | 迁移归属 | 保留候选晋升身份绑定；candidate/plan 成对并按频道复核，不直接复用旧 run 执行状态。 |
| `registry_promotion_run_id` | text / 可空 | crawler.channel_candidates.admitted_plan_id + control.plans.admission_candidate_id | 迁移归属 | 保留候选晋升身份绑定；candidate/plan 成对并按频道复核，不直接复用旧 run 执行状态。 |
| `youtube_business_email_available` | boolean / 可空 | crawler.channels.youtube_business_email_available | 保留 | 值与 observed_at 成对，未知不能解释为不存在。 |
| `youtube_business_email_observed_at` | timestamp with time zone / 可空 | crawler.channels.youtube_business_email_observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |

## 4. `crawler.contents`

| 旧字段 | 旧类型 / 可空 | 新位置 | 处理 | 原因与约束 |
|---|---|---|---|---|
| `content_key` | text / 必填 | crawler.contents.content_key | 保留并审计身份 | 保留不透明稳定主键语义；旧生成式含 channel/type/source，类型修正不能重建实体。新键生成和旧引用适配需身份样例冻结。 |
| `channel_id` | text / 必填 | crawler.contents.channel_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `run_id` | text / 可空 | crawler.contents.last_source_plan_id + control.domain_items | 拆分映射 | 当前值来源与各次计划目标关系分开。旧 run_id 不是新 plan_id，未来导入建立映射，未知来源保留历史引用。 |
| `content_type` | text / 必填 | crawler.contents.content_type | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `source_content_id` | text / 必填 | crawler.contents.source_content_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `title` | text / 可空 | crawler.contents.title | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `url` | text / 可空 | crawler.contents.url | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `thumbnail_url` | text / 可空 | crawler.contents.thumbnail_url | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `published_text_raw` | text / 可空 | crawler.contents.published_text_raw | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `published_at` | timestamp with time zone / 可空 | crawler.contents.published_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `published_at_status` | text / 必填 | crawler.contents.published_at_status | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `published_at_source` | text / 可空 | crawler.contents.published_at_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `length_text` | text / 可空 | crawler.contents.length_text | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `view_count_text` | text / 可空 | crawler.contents.view_count_text | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `like_count` | bigint / 可空 | crawler.contents.like_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `comment_count` | bigint / 可空 | crawler.contents.comment_count | 保留 | 选中存量视频依据有效新观察更新数量；缺失保留，明确 disabled 按旧规则为 0，不替换已有正文。 |
| `raw_json` | jsonb / 必填 | crawler.contents.evidence_json + crawler.raw_objects | 拆分映射 | 字段 observed 标记、发布冲突、采样/来源证据留有界在线结构；大型原始响应才外置。必须审计 JSON 路径，不整体删除。 |
| `first_seen_at` | timestamp with time zone / 必填 | crawler.contents.first_seen_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `last_seen_at` | timestamp with time zone / 必填 | crawler.contents.last_seen_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `last_enriched_at` | timestamp with time zone / 可空 | crawler.contents.last_enriched_at | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `is_recent` | boolean / 必填 | crawler.contents.is_recent | 保留并限定 | 旧调用者查询依赖；保留计算规则与参考时刻。它是可重算选择辅助，不是独立权威时间事实。 |
| `view_count_status` | text / 必填 | crawler.contents.view_count_status | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `like_count_status` | text / 必填 | crawler.contents.like_count_status | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `is_members_only` | boolean / 必填 | crawler.contents.is_members_only | 保留并校验一致性 | 现有读取可能依赖，按受控事实更新维持与 access_status 的兼容；unknown/login_required 不能任意覆盖已知状态。 |
| `access_status` | text / 必填 | crawler.contents.access_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `access_status_source` | text / 可空 | crawler.contents.access_status_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `content_type_source` | text / 可空 | crawler.contents.content_type_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `position` | integer / 可空 | control.domain_items.ordinal + crawler.contents.position | 保留并限定 | 原位置快照用于兼容读取；每次冻结目标的权威顺序在 domain_items，不把 Current position 当所有轮次的顺序。 |
| `duration_seconds` | integer / 可空 | crawler.contents.duration_seconds | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `duration_status` | text / 必填 | crawler.contents.duration_status | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `comment_count_status` | text / 必填 | crawler.contents.comment_count_status | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `comments_disabled` | boolean / 可空 | crawler.contents.comments_disabled | 保留 | NULL/false/true 保留；与 count/status 成组校验，但不强行清空原评论正文。 |
| `published_at_precision` | text / 必填 | crawler.contents.published_at_precision | 保留 | 逐字段保留原有枚举及证据规则，不统一成只有 valid/invalid 的布尔状态。 |
| `duration_source` | text / 可空 | crawler.contents.duration_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `view_count_source` | text / 可空 | crawler.contents.view_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `like_count_source` | text / 可空 | crawler.contents.like_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `comment_count_source` | text / 可空 | crawler.contents.comment_count_source | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `live_scheduled_at` | timestamp with time zone / 可空 | crawler.contents.live_scheduled_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `live_started_at` | timestamp with time zone / 可空 | crawler.contents.live_started_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `live_ended_at` | timestamp with time zone / 可空 | crawler.contents.live_ended_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `extractor_version` | text / 可空 | crawler.contents.extractor_version | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `description` | text / 可空 | crawler.contents.description | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `description_status` | text / 必填 | crawler.contents.description_status | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `description_source` | text / 可空 | crawler.contents.description_source | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `hashtags` | text[] / 必填 | crawler.contents.hashtags | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `keywords` | text[] / 必填 | crawler.contents.keywords | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `view_count` | bigint / 可空 | crawler.contents.view_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `playlist_last_seen_at` | timestamp with time zone / 可空 | crawler.contents.playlist_last_seen_at | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `player_last_observed_at` | timestamp with time zone / 可空 | crawler.contents.player_last_observed_at | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `next_last_observed_at` | timestamp with time zone / 可空 | crawler.contents.next_last_observed_at | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `player_current_hash` | text / 可空 | crawler.contents.player_current_hash | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `next_current_hash` | text / 可空 | crawler.contents.next_current_hash | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `video_change_probability` | double precision / 可空 | crawler.contents.video_change_probability | 保留 | 补齐首版概要未明确字段；保留采样、观察与字段证据语义，不机械改成新名字。 |
| `last_observation_id` | uuid / 可空 | crawler.contents.last_observation_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `publication_item_hash` | text / 可空 | crawler.contents.publication_item_hash | 保留并版本化 | 这是发布投影内容 Hash，不是 wire/content_sha256；旧格式含 sha256: 前缀，禁止无条件套用裸 64 位规则。 |
| `comments_first_page` | jsonb / 可空 | crawler.contents.comments_first_page | 保留 | A 方案 JSONB；已有非空页优先保留，缺失/空页按原规则补齐；页内 collected_at/total_count 属于旧观察。 |

## 5. `crawler.agent_profiles`

| 旧字段 | 旧类型 / 可空 | 新位置 | 处理 | 原因与约束 |
|---|---|---|---|---|
| `channel_id` | text / 必填 | crawler.agent_results.channel_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `agent_mode` | text / 必填 | crawler.agent_results.agent_kind | 重命名并映射 | basic 等模式必须明确映射，不能把领域 kind 与 API 模型名称混为一谈。 |
| `input_url` | text / 必填 | crawler.agent_results.input_url | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `status` | text / 必填 | control.agent_requests.outcome + crawler.agent_results.accepted_outcome | 拆分映射 | 请求失败不能覆盖已接受的历史有效 Current；当前结果和本轮请求分开，需回归旧失败状态展示/发布资格差异。 |
| `metrics_json` | jsonb / 必填 | crawler.agent_results.output_json | 重命名 | 完整业务输出按版本 Schema 保存，包含 audience_profile_agent 等下游字段；不能缩成几个评分。 |
| `agent_model` | text / 可空 | crawler.agent_results.agent_model | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `error_message` | text / 可空 | control.agent_requests.last_error | 迁移归属 | 失败属于本次请求，保留原因码/native/有界消息；不覆盖当前有效输出。 |
| `created_at` | timestamp with time zone / 必填 | crawler.agent_results.created_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `updated_at` | timestamp with time zone / 必填 | crawler.agent_results.updated_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `agent_config_id` | bigint / 可空 | control.agent_requests.frozen_config_ref + crawler.agent_results.agent_config_id | 保留并冻结引用 | 旧配置编号作可追踪引用，执行绑定不可变版本/Hash；新库不直接外键指向旧配置库。 |
| `prompt_template_id` | bigint / 可空 | control.agent_requests.frozen_config_ref + crawler.agent_results.prompt_template_id | 保留并冻结引用 | 旧配置编号作可追踪引用，执行绑定不可变版本/Hash；新库不直接外键指向旧配置库。 |
| `prompt_hash` | text / 可空 | crawler.agent_results.prompt_hash | 保留 | 旧字段为裸 64 位 SHA-256；与其他带前缀 Hash 分开处理。 |
| `prompt_variant` | text / 必填 | crawler.agent_results.prompt_variant | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `attempts` | integer / 必填 | control.agent_requests.attempts + control.budget_entries | 迁移归属 | 分请求保留真实尝试和成本，不按频道无界累加作为本轮预算。 |
| `last_observation_id` | uuid / 可空 | crawler.agent_results.last_observation_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `last_observed_at` | timestamp with time zone / 可空 | crawler.agent_results.last_observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `current_output_hash` | text / 可空 | crawler.agent_results.current_output_hash | 保留并版本化 | 旧值使用 sha256: 前缀及旧规范算法；算法/profile 分开记录，不能仅移除前缀就声称符合新 JCS。 |
| `input_content_ids` | text[] / 必填 | crawler.agent_results.input_content_ids | 保留 | 保留有界、有序/规范化的输入内容身份集合；input_hash 不能替代实际可核对输入。 |
| `input_content_hash` | text / 可空 | crawler.agent_results.input_content_hash | 保留并版本化 | 旧值使用 sha256: 前缀及旧规范算法；算法/profile 分开记录，不能仅移除前缀就声称符合新 JCS。 |
| `taxonomy_version` | text / 可空 | crawler.agent_results.taxonomy_version | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `agent_version_hash` | text / 可空 | crawler.agent_results.agent_version_hash | 保留并版本化 | 旧值使用 sha256: 前缀及旧规范算法；算法/profile 分开记录，不能仅移除前缀就声称符合新 JCS。 |

## 6. `crawler.finalized_profiles`

| 旧字段 | 旧类型 / 可空 | 新位置 | 处理 | 原因与约束 |
|---|---|---|---|---|
| `channel_id` | text / 必填 | publication.revisions.channel_id + read_model.channel_control_state.channel_id | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `run_id` | text / 可空 | control.plans.plan_id + publication.revisions.plan_id | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `status` | text / 必填 | control.plans.outcome + publication readiness + read_model 状态 | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `profile_json` | jsonb / 必填 | publication.revisions.payload | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `quality_json` | jsonb / 必填 | control.domain_receipts.proof_summary + publication readiness reasons | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `finalized_at` | timestamp with time zone / 可空 | control.plans.settled_at / publication.revisions.occurred_at | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `created_at` | timestamp with time zone / 必填 | publication.revisions.created_at | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `updated_at` | timestamp with time zone / 必填 | read_model.channel_control_state.updated_at | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `last_observation_id` | uuid / 可空 | publication.revisions.source_refs.observation_id | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `last_observed_at` | timestamp with time zone / 可空 | publication.revisions.source_observed_at | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |
| `current_output_hash` | text / 可空 | publication.revisions.result_hash | 重建边界 | 保留该字段承载的业务事实/证据，但取消独立 Finalize 当前状态表。旧 finalized 不自动等于 Plan Settled、Publication Ready 或 DELIVERED，迁移逐项映射。 |

## 7. `crawler.channel_about_metric_snapshots`

| 旧字段 | 旧类型 / 可空 | 新位置 | 处理 | 原因与约束 |
|---|---|---|---|---|
| `observation_id` | uuid / 必填 | crawler.channel_about_metric_snapshots.observation_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `observed_at` | timestamp with time zone / 必填 | crawler.channel_about_metric_snapshots.observed_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `channel_id` | text / 必填 | crawler.channel_about_metric_snapshots.channel_id | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count` | bigint / 可空 | crawler.channel_about_metric_snapshots.subscriber_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count` | bigint / 可空 | crawler.channel_about_metric_snapshots.total_view_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count` | bigint / 可空 | crawler.channel_about_metric_snapshots.total_video_count | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `subscriber_count_status` | text / 必填 | crawler.channel_about_metric_snapshots.subscriber_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_view_count_status` | text / 必填 | crawler.channel_about_metric_snapshots.total_view_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `total_video_count_status` | text / 必填 | crawler.channel_about_metric_snapshots.total_video_count_status | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `facts_hash` | text / 必填 | crawler.channel_about_metric_snapshots.facts_hash | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `created_at` | timestamp with time zone / 必填 | crawler.channel_about_metric_snapshots.created_at | 保留 | 保留业务值、类型和含义；旧默认值及约束仍需单独核对，不自动照搬。 |
| `snapshot_origin` | text / 必填 | crawler.channel_about_metric_snapshots.snapshot_origin | 保留 | 区分真实新观察与历史恢复，恢复时间不能冒充原观察时间。 |
| `evidence_source` | text / 可空 | crawler.channel_about_metric_snapshots.evidence_source | 保留 | 区分真实新观察与历史恢复，恢复时间不能冒充原观察时间。 |
| `evidence_ref` | text / 可空 | crawler.channel_about_metric_snapshots.evidence_ref | 保留 | 区分真实新观察与历史恢复，恢复时间不能冒充原观察时间。 |
| `recovered_at` | timestamp with time zone / 可空 | crawler.channel_about_metric_snapshots.recovered_at | 保留 | 区分真实新观察与历史恢复，恢复时间不能冒充原观察时间。 |

## 8. 约束、索引和语义验收矩阵

| 对象 | 已核实旧约束/写入行为 | 新设计处置 | migration/Store 必测 |
|---|---|---|---|
| 频道主键 | channel_id 主键 | 保留稳定身份，handle/URL 不是主键 | 改 handle 不新建频道；重复发现归并来源 |
| 视频主键与去重 | content_key 主键；UNIQUE(channel_id, source_content_id)；旧生成 key 包含 content_type | 保留稳定不透明 key，候选增加 platform/resource_kind/source 唯一约束；类型纠正不换身份 | 同视频重复、错频道、video→short 类型修正、帖子身份不混入视频命名空间 |
| 视频位置 | position 可空；旧索引 (channel_id, run_id, position) | 保留当前位置快照，轮次顺序转到 DomainItem | 同视频处于两次不同目标顺序，不覆盖历史冻结顺序 |
| 频道数量/状态 | 非负；exact/estimated/unavailable/unresolved；快照中值与有效状态互相约束 | 分字段保留状态集合，落库加强一致性时明确差异 | NULL、0、估算、本地化原文、较旧观察晚到 |
| Verified | verified=true / not_verified=false / unknown=NULL | 保留三值成组校验 | unknown 不能变 false；错误组合拒绝 |
| 加入日期 | joined_at 为 date；date_only/unknown | 保留日期精度 | 不能序列化成声称精确到秒的时间 |
| 生命周期 | dormant 要有理由、复查日、最后探测和 cycle；removed 有理由/来源/时间 | 保留证据，不只重命名状态 | 暂停、休眠、移除分别处理；离开 dormant 清理或归档周期字段的规则一致 |
| 评论数量与开关 | disabled 与 count=0/status=disabled 关联；非 disabled 不接受 disabled 状态 | 明确三值和必填条件，不能靠 SQL NULL 侥幸通过 CHECK | disabled=true/count=NULL 必须拒绝；disabled=true/count>0 拒绝；未知不强填 0 |
| 评论页 | JSON 对象、version=1、合法 sort、comments 数组、returned_count 与长度一致 | A 方案保持，严格验证必填字段；旧宽松 SQL CHECK 不等于完整 JSON Schema | missing/null/wrong type/非整数/长度不符；页内 count 与视频最新 count 允许不同 |
| 评论保存 | existing.returned_count>0 优先保留，否则允许非空新页补齐 | 保留合并规则并同步页证据 | comment_count 更新时旧正文和旧页 Hash/观察时间不变 |
| 发布时间 | exact/relative/estimated/unavailable/unresolved 与 second/date_only/unknown 分离 | 保留精度和来源选择规则 | 指标更新不能无条件覆盖已接受的静态发布时间 |
| 成员/访问状态 | access_status 多状态；unknown/login_required 写入可保留旧有效状态 | 保留来源策略，布尔与状态不能两套独立写入 | login_required 不直接推断会员/永久不可用 |
| 字段观测标记 | hashtags/keywords 的 observed 标记影响空数组是否可更新 | 从 raw_json 明确映射为有界证据，不丢标记 | 未观察到不能清空；确实观察到空值沿原合并规则处理 |
| Hash | 旧前缀/裸格式混合；用途不同 | 按字段的格式和 profile 校验 | 去前缀不改变算法；确认过的旧 Hash/字节不可重算覆盖 |
| Agent 失败 | 旧 profile 可能同时保留输出又将 status 改 failed | Current 和本轮 Request 分离，发布依赖按计划判定 | 新请求失败不删旧输出，也不能用旧成功替代新轮必需回执 |
| About 历史快照 | observation_id 与 observed_at；historical_recovery 必须有恢复证据 | 保留小型快照，避免 Feature 重建只剩最新值 | 恢复时间不当采集时间；至少有一项有效指标；数值/状态一致 |
| 删除行为 | 旧 contents 外键有 CASCADE / SET NULL | 新权威事实和回执默认 RESTRICT，显式受控归档 | 删频道不会级联抹掉关键回执/未交付义务 |

PostgreSQL CHECK 在表达式结果为 NULL 时不会拒绝行。因此不能原样复制旧 `count = 0` 或 JSON `->>` 组合，就认为“缺失字段一定失败”；新 DDL 应结合 NOT NULL、显式 IS NOT NULL、IS TRUE 或已验证结构保证真正的约束含义。

旧索引包括 channel/type/last_seen、channel/published_at、current_run/position、player_refresh、recent、关键词/hashtags GIN。它们说明存在实际查询入口，不能全部删到只剩主键；新索引要按新的等价查询与 EXPLAIN 重新选择，不机械照抄所有索引。

## 9. 评论样例的预期结果（后续 Store 测试输入）

| 已保存 | 本次有效输入 | 预期 |
|---|---|---|
| count=100、非空评论页 A | count=120、非空页 B | 数量/其来源时间按规则更新；正文仍 A，A 的页 Hash/时间保持 |
| count=100、页 A | 数量未知、页 B | 数量保留 100；正文 A 保留；不假称本轮成功观察到数量 |
| 页缺失 | 合法非空页 B | 在授权/输入版本成立时补齐 B，数量独立按证据更新 |
| 合法非空页 A | 明确 disabled，count=0 | 数量/状态按关闭规则更新，历史正文按保留规则维持；不把两者混成一条“页必须为空”约束 |
| 合法确认空页 | 暂时网络错误/未知 | 不把错误当明确禁用或新的有效空页；恢复责任按旧规则 |
| 合法非空页 A | 比已应用数量观察更旧的结果 | 不因到达更晚而回退当前指标；保留对应诊断与原提交处理语义 |

上述是依据已核对代码和确认规则整理的验收目标，不是声称已跑通真实数据库事务。

## 10. 未被本轮覆盖的内容与下一步

- 173 列是这五张快照表的完整列覆盖，不是旧系统所有字段，也不是每一个 JSON 子字段已完成映射。
- Query、候选、API 请求、Feature 算法、Publication、Rota 和业务库兼容仍按 24.7 分批核对。
- 本轮只读检查关键 Store/调用路径，不以快照结构证明旧实现没有缺陷。
- 本文第 1～11 节记录字段映射与验收目标；后续实施状态见第 12 节，未向旧库写入或迁移旧数据。
- 第一批 migration 以明确映射为依据，在隔离验证数据库运行，先测试身份/字段质量/评论规则，再接 Submission 事务；不直接用生产库做结构试验。
- 内容身份生成、旧 Hash profile/新发布适配和 Agent 失败语义的差异，在落实相应结构前必须有样例结论；不以一份通用 JSON 契约跳过。

## 11. 核对证据

- `database/bootstrap/crawler.sql`：五表结构、CHECK、主外键及视频索引。
- `services/qybullmq/src/videoContentStore.js`：旧 key 生成、ON CONFLICT、存量指标更新与 raw_json 业务证据。
- `services/qybullmq/src/videoContentMutationPolicy.js`：已有非空评论页优先保留。
- `services/qybullmq/src/incrementalYoutubeJsVideo.js`、`youtubeJs.js`：full/metrics、底层评论请求。
- `services/qybullmq/src/commentFirstPageBackfill.js`：空/缺失评论页与确认空页的补采条件。
- `services/qybullmq/src/incrementalAgentResultStore.js`、`agentPublicationCurrent.js`：Agent 状态写入、输出/输入 Hash 与发布依赖。
- `services/qybullmq/src/aboutObservationStore.js`：小型指标快照写入与来源读取。

以上路径相对只读参考仓库 `oldjiagousys`；新代码与文档唯一工作目录仍是 `newcrawlSystem`。


## 12. 首个 migration 实施更新

`0001_collection_facts.sql` 在独立验证库完成 channels/contents 建表，详细范围与验收见 `database/README.md`、`ops/checks/2026-09-20-database-facts.md`。
逐列比对实际 information_schema：频道 61 列、内容 59 列；旧直接映射事实字段中 contents.last_observation_id 暂缓到 Observation 表一起建立外键。
旧 run_id 对应的新 last_source_plan_id、来源明细、Agent/Finalized/About snapshot 等跨表目标同样尚未落库。

内容稳定身份、错频道去重拒绝、video/short/live 类型修正、帖子独立命名空间已通过数据库样例。
指标/认证/日期/生命周期和评论 JSON 必填约束已测试；is_members_only 允许 NULL 是明确的新库差异，原因见 24.7 DB-12。
评论 total_count 与当前数量可不同，历史正文可以在当前禁用状态保留；这是结构能力。
第 9 节“已有非空页保留、空页补齐、旧观察拒绝”仍是后续 Store 合并测试目标，不以 UPDATE 未涉及正文的测试代替。

机器映射副本仍是设计记录，不是已实现字段清单或可运行迁移工具；以每个 migration 与本节实施说明判断进度。


## 13. 评论与指标 Store 验收更新

第 9 节的数量更新保留正文、未知数量保留旧证据、缺失/空页补齐、disabled 保留历史页、较旧数量不回退，现已通过真实 Store/数据库测试。
0002 新增正文 Hash/profile/observed_at 和页面 checked_at，保留正文时 Hash 与原采集时间同步保持；具体等时冲突/迟到页边界见 `docs/migration/content-store.md`。
本轮不声称第 8 节整个矩阵都已实现：频道生命周期 Store、完整描述/发布时间、类型纠正、Observation/Plan 关联、Agent 与 Business 适配仍未完成。
测试记录见 `ops/checks/2026-09-20-content-store.md`。
