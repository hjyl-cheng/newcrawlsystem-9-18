# 24.9 Kafka 采集结果入口与并发入库实施方案

日期：2026-09-20。决策：用户已确认采用现有 Kafka 承接采集结果，取代 Worker 直连 gRPC Fast Apply 作为普通结果主入口。
状态：首个增量视频指标切片已在真实三 Broker Kafka + 隔离 PostgreSQL 完成验证；没有上线真实 Worker 或常驻消费者服务。

## 1. 为什么改，以及哪些仍保留

目标是让大量 Worker 可以并行提交，消息接收/保存/排队/消费组分配交给成熟 Kafka，项目只实现采集业务的授权、字段合并、幂等和回执。
现有 PostgreSQL Store、最小执行授权和 Submission 原子事务继续使用；Data Ingestor 现在作为 Kafka 消费者运行。
不新增 RabbitMQ/NATS，不把 Temporal 调度、Rota 请求、普通日志或大 Raw 全部塞进 Kafka。
Business DB 保持不变，旧仓库继续只读参考，新代码唯一来源为当前 newcrawlSystem。

```text
Worker 1…N（成熟客户端、复用连接、有界发送）
             │ 签名的结构化采集结果，key=channel_id
             ▼
现有 S1/S2/S3 Kafka：Results Topic
        分区0          分区1          分区2
          │              │              │
          └────── Data Ingestor 消费者组 ──┘
                    │ 多实例、跨分区并行
                    ▼
            PostgreSQL 单逻辑库
       ┌────────────同一事务────────────┐
       │ 授权核对、事实、检查点、APPLIED │
       │ Kafka record 的 APPLIED 记录  │
       └───────────────────────────────┘
                    │ COMMIT 成功
                    ▼
          提交该分区 Kafka offset+1

另一路，继续独立：
PG Publication Outbox → Debezium → Publication Topic → Business Consumer
```

不同分区并行，同一分区按消息顺序处理。按频道分区使同一频道通常落到同一条车道；同频道 PG 短事务仍有协调锁。
分区数量限制同组消费者有效并行数；增加消费者不代表无限增加 PG 连接或数据库写入能力。
两条链共享 Broker 的 CPU/内存/磁盘，是逻辑隔离，尚未完成强资源隔离或容量验收。

## 2. 两种确认必须分清

| 确认/状态 | 含义 | 不能解释成 |
|---|---|---|
| BROKER_ACKED | Kafka Producer 收到 acks=all 的成功确认，返回 topic/partition/offset | PG RECEIVED、APPLIED 或整轮成功 |
| APPLIED | PG 已按规则处理该 Submission，事实/检查点/回执同事务提交 | 每个字段都取得新值、Domain/Plan 完成或 Business 已交付 |
| NOT_OBSERVED | 权威查询当前没看到该提交的 PG 回执 | 永久“未提交”或 Kafka 没收到 |
| QUARANTINED | 该 Kafka 记录无法应用，原始消息与原因已在 PG 耐久保留 | 成功入库或已有 APPLIED |

首期不发 PG RECEIVED。Worker 不能仅凭 BROKER_ACKED 删除最后一份待确认结果；未来真实 Worker 的 Local Journal/终态查询尚需实现。
Kafka 发送失败/超时保守报告 KAFKA.PUBLISH_UNKNOWN，按原 Submission 身份重发或查询，不能随意换身份。
Kafka 保存和 PG 成功之间没有跨系统分布式事务；采用至少一次消费 + PG 幂等，不能宣称跨 Kafka/PG 的通用 exactly-once。

## 3. 首批 Topic 与配置

实际配置文件：`infra/kafka/validation-results.json`。

| 对象 | 验证期配置 |
|---|---|
| Results Topic | crawler.results.validation.v1 |
| Apply Consumer Group | crawler-results-apply-validation-v1 |
| stream_id | validation-results-20260920-v1 |
| 分区 / 副本 / 最小同步副本 | 3 / 3 / 2 |
| 生产确认 | acks=all、幂等 Producer、max in-flight=1 |
| 消息 key | channel_id，显式 murmur2_random 分区器 |
| 保留策略 | delete；7 天或每分区 128 MiB，任一容量/时间阈值都可能先触发删除 |
| segment | 16 MiB / 1 小时 |
| 应用单条记录上限 | 1 MiB，包含签名封装；Broker topic max.message.bytes=1100000 |
| 验证期消费并发 | 每实例默认 2 个分区；不等同于已经压测出的生产容量 |

保留 7 天不是“保证至少保存 7 天”：字节上限、segment 清理时机等共同决定实际窗口。
不得让积压越过保留窗口再静默跳过。当前启动和每批处理前检查消费进度相对 log start/end；发现缺口或疑似 Topic 重建时停止推进并报 KAFKA.RETENTION_OR_STREAM_GAP。
仍需生产 lag/最老未处理年龄/磁盘/保留水位告警，以及跨 Worker/PG 的容量准入；不能用配置值代替这些监控。

分区数首期固定。增加分区会改变 key 映射，可能破坏同频道跨旧/新分区顺序；后续必须有停发/排空或新 Topic 迁移方案，不能直接扩分区就承诺顺序不变。
重建 Topic 时必须更换 stream_id，并对账消费进度；防止相同 topic/partition/offset 被当成旧消息。

## 4. 生产者身份与消息完整性

采用 `@confluentinc/kafka-javascript@1.10.1`，基于 librdkafka 2.15.1，通过标准客户端完成连接、重试、分区、Broker 复制确认及消费者组协调。
Node 24 原生模块已实测加载，npm ci 可复现，安装脚本仅对该固定版本放行。
客户端选型时发现 KafkaJS 2.2.4 在 Node 24 下负定时器导致空闲反复轮询，因此没有将其保留为运行依赖；不自行维护 Kafka 客户端协议。

记录版本：crawler-result-kafka/1-draft.1。主体为 `{signed:{transport_version,key_id,submission},signature}`。
signature 使用 Node 标准 Ed25519，对带固定域前缀的规范 signed 字节签名；body 不用 Base64 搬运，只有短签名用 Base64。
消费者从可信公钥登记获取 holder 与允许频道，不信任消息自报 Principal；再检查 key 与正文频道一致，复核原业务 Hash 及 PG 当前执行授权。
私钥只在生产者侧；本轮测试密钥仅在内存生成，没有落到仓库。实际密钥分发、撤销和轮换尚未上线。

签名不提供传输加密，也不代替 Kafka Broker 的 ACL。
当前服务器 Kafka 仍是内网验证用 PLAINTEXT；生产前必须落实 SASL/TLS、结果生产/消费最小 ACL、凭据管理和服务网络边界。
当前测试不能证明未知外部节点已经被 Broker 阻止连接。

## 5. 消费、失败与原子性

新增 `ingestion.kafka_record_outcomes`，主键为 stream/topic/partition/offset，记录消息指纹、APPLIED 或 QUARANTINED、回执/原因、必要原始字节。
已 APPLIED 的处理记录与事实/Submission/批次检查点/回执同事务写入，成功后才提交 Kafka offset+1。
同一位置重放时复用已保存结果；相同位置字节不同报 KAFKA.STREAM_IDENTITY_CONFLICT，不静默覆盖。

| 故障/结果 | 处理 |
|---|---|
| PG 不可用、SQL 错误、COMMIT_UNKNOWN、未知程序错误 | 不推进 offset，暂停该分区，指数退避 + Full Jitter 重试 |
| PG 已提交，offset 尚未提交就退出 | 重启后重放；复用 PG 已有结果，不重复入库 |
| 伪造签名、非法载荷、错频道/目标、过期 epoch、业务冲突 | 事务回滚；另一个短事务保存原始消息与原因，再提交 offset |
| 隔离记录保存也失败 | 不推进 offset，继续重试/报警 |
| 单记录/Key 超过隔离保存预算 | 停止推进并报错，不为释放队列丢弃字节 |
| 接管发生在消息排队期间 | 消费时仍核对当前 epoch/holder/租约，旧结果隔离，不把生产时有权限当成永久入库权 |

隔离不是另一个 APPLIED 或否定 Submission 回执，也不释放 Plan 的恢复义务。
隔离回放需要显式复核当前授权/目标，再生成新 Kafka 记录；原隔离记录不可修改。没有做无限 DLT↔主 Topic 循环重试。
隔离管理 UI、回放命令及 Worker 的终态查询服务本轮尚未实现；当前保留完整记录供 SQL 核查。

## 6. 背压、客户端边界与停机

Producer 本地队列限制 4 MiB/1000 条，消息投递超时 30 秒；消费者 native 队列目标 4 MiB，单批最多 8 条，fetch 配置约 2.2 MB，跨分区并发有显式上限。
这些是 SDK 队列/抓取预算，不是对整个进程 RSS 的严格上界；单批最大消息、JSON 解码、签名、PG 参数和多个 Broker 都需要额外内存。
生产者/消费者使用长连接；不为每条结果创建连接，不为每条结果创建独立 Pod。
消费者关闭自动提交与 batch 自动 resolve，调用方不能再开启另一套自动 offset 提交。
librdkafka 管理心跳/组协调；服务停机通过 disconnect 等待当前回调退出，未确认进度由下一消费者重放。

本 SDK 的 offset 适配内部涉及 JS Number；消费代码在推进前拒绝达到安全整数边界的 offset，不承诺任意 64 位 offset 无损。
业务计数/epoch 的 20 位字符串和 PG numeric 域不受此 Kafka SDK 边界影响。
全系统消费者 pool 总连接数仍要纳入 PG/PgBouncer 预算；本轮未做大量 Worker 压测、Broker 故障演练或磁盘满恢复。

## 7. 代码、执行与验收

- 生产/消费及签名适配：`services/kafka-results/src/transport.ts`。
- 入库逻辑：复用 `services/ingestion/src/metrics-submission.ts`，通过 prepareMetricsApply 在事务外校验，事务内执行。
- 新迁移：`0004_kafka_result_outcomes.sql`；0001～0003 保持校验和不变。
- Topic 准备：设置 KAFKA_BROKERS 后运行 `npm run kafka:provision`；只管理验证 Results Topic，已有 Topic 不自动改参数。
- SDK Admin 尚不支持 DescribeConfigs，已有配置必须用 Broker 官方 kafka-configs.sh 复核；本轮已实际核查。
- 无 Kafka 的签名/契约测试：`npm test`。
- 真正 Kafka+PG 集成测试：准备独立 crawler_schema_test_* 数据库及 PG/KAFKA 环境变量，运行 `npm run test:kafka`。
  测试只创建带唯一后缀的 crawler.results.test.* Topic/消费者组和测试数据，结束后清理，不消费 Publication。

当前业务范围仍是 incremental/delta 视频指标、每个冻结批次 1～100 项、单计划局部 VIDEO_METRICS 义务；不是五种 Worker 的全部正文协议。
真实 Kafka 已通过 9 项测试，含双消费者并行、重复消费、PG/offset 提交窗口、坏消息隔离、排队后失权及保留缺口。
验证记录见 `ops/checks/2026-09-20-kafka-results.md`。

## 8. 被本次决策替代的旧约束与后续

24.4 中 Kafka 仅 Distribution、Worker 必须直连 gRPC、Kafka 故障只影响发布的约束不再适用。
新采集结果现在依赖 Kafka；Broker 故障会阻止新结果投递/消费，已开始的 PG 事务仍可完成。
Debezium 继续只捕获 Publication Outbox；Analytics/日志不因本次改变而自动加入同一 Topic。
24.6 的 gRPC Manifest/Frame 是旧传输草案；本轮采用独立 Kafka 记录版本，不声称已经实现旧 Frame/OBJECT/RECEIVED 全部语义。

下一步是把消费者打包为常驻验证服务、落实凭据/ACL和运行时最小 PG 权限、暴露健康/积压指标及原身份回执/隔离查询，再做受控滚动和故障演练。
本轮只执行独立库与临时消费者测试；正式 crawler/Business 未写入，真实 Worker 和消费者 Pod 尚未上线。

## 9. 常驻验证服务后续实施

`services/data-ingestor` 已接入本方案的库，新增受控查询、就绪/存活与 lag 指标。部署使用独立 crawler_validation_ingestor 库和受限 runtime 账号，自动测试库仍保留独立。
两个消费者副本经 Argo CD 部署，具体发布和实测证据见 `ops/checks/2026-09-20-ingestor-service.md`，步骤见 `ops/ingestor/README.md`。
本节是后续进展，第 1～8 节的测试记录保留为当时状态。Broker TLS/SASL/ACL、真实 Worker Journal、自动告警和生产 HA 仍待实施。
