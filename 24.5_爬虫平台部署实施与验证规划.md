# 爬虫平台 24.4 部署实施与验证规划

版本：24.5（实施规划）  
依据：`24.4_爬虫平台完整重构方案_业务控制台与全链路追踪定案版.md`  
目标：先用 6 台低并发服务器跑通完整链路，再通过升配、调整副本和增加 Worker 节点进入生产。

本文记录实施步骤和验收方法。单逻辑 PostgreSQL、五执行角色、三 Clock、Data Plane、Read Model 和 Publication 边界继续作为基线；实际修订另行记录。

自动业务运行、控制台管理、GitOps 发布和 Worker 扩缩容的职责及实施顺序，补充见 `24.13_业务操作与GitOps发布职责及扩缩容实施建议.md`。任务由系统按策略自动产生，不依赖人工建任务或页面在线；业务配置不要求逐次提交 Git。首期固定副本验证自动闭环，后续由 KEDA/HPA 接管实际副本数，避免业务 API、Argo 和 HPA 竞争写入。

**2026-09-20 实施顺序调整（用户明确要求）：先完成外围基础设施，再继续业务代码。暂停代理管理 API/UI、Planner、真实采集 Worker、Journal 等业务功能开发；保留已有验证服务。当前按 24.12 推进备份恢复、稳定入口/故障切换、存储与队列可靠性、监控告警和访问控制。下文历史“下一步写业务代码”的顺序暂不执行。部署配置和运维脚本属于外围建设，仍在本仓库维护。**

外围最新进展：PG 加密物理备份、WAL 归档、定时全备/差异备份和独立指定点恢复通过；etcd/配置加密备份跨机保存、独立启动恢复通过。备份证据见 `ops/checks/2026-09-20-backup-recovery.md`。PG 已进一步接入 Patroni 严格同步主备、固定写入口和三节点连接池，计划切主/协调网络隔离/数据与备份恢复通过，见 `ops/checks/2026-09-20-postgresql-patroni.md`。Kubernetes API 本地多后端入口、DNS 分散与 Argo server/repo/cache 多副本也已部署，A1 API 故障中的 Git 发布通过；发布控制器仍为单活，整机失联无人值守接管保留待办，见 `ops/checks/2026-09-20-kubernetes-argocd-ha.md`。其他存储、CDC、外部灾备与集中监控继续按 24.12 推进。

**2026-09-20 代理决策更新：按 24.10 实现 Worker 内的 Crawlee 公共模块、代理生命周期扩展和 YouTube.js fetch 适配；不部署 Rota/Resin。下文历史 Rota 入口/许可待办按 24.10 的节点分组、健康池和使用预算替代。当前先验收本地模块，再接真实代理、业务映射、Journal/Kafka 与 Temporal，不将公共模块完成等同于全链路完成。**

外围后续进展：Kafka 单服务停服接管、恢复三副本、121 条消息完整回放及 Ingestor 回归已通过；修复 systemd 正常停服被误报失败的配置，补充只读巡检和保留/积压监控要求。见 `ops/checks/2026-09-20-kafka-broker-failover.md`。继续 SeaweedFS 等外围收尾，尚未完成 Kafka 的断电/灾备/认证或集中告警验收。

SeaweedFS 最新进展（2026-09-21）：TCP 18888 已放通；目录已迁移到现有 PG 独立 schema，S1/S2/S3 均运行 Filer 和带认证的 S3 网关，A 集群有跨节点双份固定入口。单后端停服、入口替换及 PG 计划切主后的真实 Service 读写验证通过。配套维护备份独立还原 52 条目录元数据和 19 个非内部日志文件通过；持续在线备份、内部接口加固与告警仍待，见 `ops/checks/2026-09-21-seaweed-ha.md`。业务功能开发继续暂停，后续先处理 ClickHouse、CDC、监控与安全。

ClickHouse 最新进展（2026-09-21）：原生在线备份、A1/S2 加密副本、每日定时任务及隔离实例恢复已通过，见 `ops/checks/2026-09-21-clickhouse-reliability.md`。仍为 S3 单机且使用系统盘，与 24.4 §23.10 的独立数据盘要求存在已记录差异；不能标记生产 HA。接下来继续 CDC/逻辑槽位、监控和安全，业务代码仍暂停。

CDC 最新进展（2026-09-21）：双副本 Connect、Debezium 与 PG17 原生 failover 槽已部署；首轮 Pod 替换和 PG 计划切主验证 16 条事件全收。针对随后发现的两个问题，已部署动态候选 guard 和 Patroni 晋升 hook，普通/同步备库停服及主库 DCS 隔离时自动恢复投递，后续 32 条事件全收；独立探针复现的旧主非同步槽冲突也由安装程序的同一修复路径处理并完成原生重建。最终 S1 主库、严格同步 1 备库，详见 `ops/checks/2026-09-21-cdc-guard-recovery.md`、`ops/cdc/HA-GUARD.md`。仍不能据此声称任意多节点/磁盘/槽失效可无人值守恢复。正式 Publication/外部业务库投递尚未实现；下一阶段先接集中监控告警，其余遗留继续按 24.12 跟踪，业务开发仍暂停。

监控最新进展（2026-09-21）：六机 mTLS 指标采集、双 Prometheus、三 Alertmanager、双 Grafana（复用现有 PG 独立逻辑库，不使用 SQLite）和只读 K8s/Kafka/应用探针已通过新仓库 Argo 部署。统一覆盖资源、复制/槽位、CDC guard、Kafka ISR/积压和备份时间；受控告警触发/恢复、平台访问与历史指标保留的证据见 `ops/checks/2026-09-21-monitoring.md`，操作见 `ops/monitoring/README.md`。Seaweed 持续备份仍作为真实待办告警显示。集中日志、外部通知与安全收尾未完成，仍按 24.12 推进外围，不恢复业务开发。

集中日志最新进展（2026-09-21）：Vector → ClickHouse HTTPS → Grafana 已覆盖六机系统日志与现有 Kubernetes 命名空间日志，权限、脱敏、查询与短时阻断/重启补传验证通过，见 `ops/checks/2026-09-21-logging.md`。本地缓冲和中央保留均有限，可能丢弃诊断日志，不改变业务事实通道；下一个外围切片是内部通信认证/权限/TLS，尚不恢复业务代码。

公共模块及职责修订已完成：相关 13 项测试，连同既有测试共 40 项通过；记录见 `ops/checks/2026-09-20-collector-runtime.md`。Crawlee 原版不改，IP 扩展只维护代理状态，会话用原生能力，重试和 HTTP 业务判定由采集层控制。第一版仅标准 HTTP/HTTPS 代理、匿名只读采集、进程内代理组；真实代理尚未提供，不对真实 YouTube 成功率作结论。

最新进度：存量视频指标映射及 Submission 组装已完成，详见 24.11；保留原始大整数，校验视频/频道/冻结目标，未知不补零，评论数量和正文分离。全量测试现为 50 项通过；下一项为 Worker Journal 与实际 Kafka 提交衔接，真实采集仍待代理与任务准备。

后续进展（2026-09-20）：Temporal 1.32.0 已通过新仓库和 Argo CD 部署为两个跨节点副本，运行账号和网络边界已验证。合成任务的排队、Activity 重试、验证 Worker 停止/重建后的定时器恢复、完成结果重查通过，见 `ops/checks/2026-09-20-temporal-service.md`。这是调度底座验收；常驻采集 Worker、业务 Planner、Journal 和真实任务到入库终态的衔接仍未完成。SQLite 未引入，Journal 存储方式未定。

**当前入口决策（2026-09-20）：用户已确认 Worker → Kafka Results → Data Ingestor → PG，详见 24.9。下文早期进度中 gRPC/Manifest 接入待办由此替代；各次测试数字是当时的验收记录，最新进度见文末。**

> 登录用户、密码和服务器凭据只记录在 `SERVER_INVENTORY.local.md`，不复制到本规划文档，也不提交到代码仓库。

---

安全最新进展（2026-09-21）：三台 PG 专用 SQL 证书、Patroni 复制/rewind 和 Grafana verify-full 已上线，相关角色拒绝明文；本机证书匹配、实际切主/切回及 12 条 CDC 事件验证通过，见 `ops/checks/2026-09-21-postgresql-sql-tls.md`。其余 PG 调用方、Kafka/Seaweed 内部认证加密仍按 24.12 分批处理，外围未全部完成。

安全后续进展（2026-09-21）：剩余 Temporal、Debezium、Ingestor/PgBouncer、Seaweed Filer SQL 连接已全部加固，PG/连接池 TCP 明文入口关闭。真实应用探针与 S1→S3→S1 切主恢复通过，见 `ops/checks/2026-09-21-application-sql-tls.md`。下一批继续 Kafka/其他内部协议和外围可靠性收尾，业务功能仍暂停。



2026-09-21 Kafka 安全第一阶段：三S节点逐台增加9094双向TLS入口，本机TLS1.2/1.3及四类错误证书拒绝验证通过，三副本/quorum恢复，原入库与4条确认CDC事件回归通过。六机到三S的跨机9094均超时，等待轻量云TCP9094（来源10.4.4.0/22）应用到S1/S2/S3后继续迁移客户端。9092/9093仍保留明文，ACL与节点通信加密未完成，不能标记Kafka安全收尾完成。详见 `ops/checks/2026-09-21-kafka-mtls-listener.md` 和 `ops/kafka/SECURITY-MIGRATION.md`；业务代码仍暂停。



2026-09-21 Kafka应用TLS第二阶段：用户放通9094后，六机到三S的18条路径通过；两Ingestor、两Connect及两exporter已使用独立客户端证书通过9094双向TLS连接，应用9092出口已关闭。证书错误/缺失、旧端口隔离、加密入库/CDC、单协调节点停服恢复均按独立证据验收，见 `ops/checks/2026-09-21-kafka-client-tls.md`。迁移暴露的Ingestor原生Admin查询崩溃已改用Consumer.committed绕开并发布，原失败记录保留。**下一阶段仍需处理Kafka内部副本/控制器加密、ACL和关闭旧监听，不能将应用TLS等同Kafka安全全部完成。** 外围优先，业务功能开发继续暂停。

## 0. 代码仓库与唯一来源

### 0.1 唯一工作目录

本次重构的唯一代码来源是：

```text
/home/ubuntu/workspace/newcrawlSystem
```

业务代码、共享库、数据库迁移、部署清单、测试、验收脚本、运行手册和方案文档都在这个目录维护。服务器不直接编辑代码，只运行由 Git commit 构建并发布的制品。

### 0.2 GitHub 的使用方式

新架构 GitHub 仓库：

```text
git@github.com:hjyl-cheng/newcrawlsystem-9-18.git
```

新仓库的主分支：

```text
main
```

第一次准备当前目录时：

1. 在 `newcrawlSystem` 初始化 Git 工作树。
2. 将 `git@github.com:hjyl-cheng/newcrawlsystem-9-18.git` 配置为 `origin`。
3. 在新仓库创建 `main`，提交 24.3、24.4、24.5 方案和当前目录的实施基线。
4. 保留当前目录已有的 24.3、24.4、24.5 文档和本地清单。
5. 把 `SERVER_INVENTORY.local.md`、本地 Secret、临时检查结果加入忽略规则，禁止提交密码和私钥。
6. 以后所有 commit、测试、镜像构建和部署变更都从这个目录产生。

新仓库保存重构后的源代码和部署声明；镜像仓库保存不可变镜像；Argo CD 根据 Git revision 和镜像 digest 发布到 Kubernetes：

```text
newcrawlSystem → GitHub → CI 测试/构建 → Registry → Argo CD → Kubernetes
```

旧的 `crawlSystem` GitHub 仓库不作为新系统的 remote，不执行 push、merge、tag、release 或任何写操作。

### 0.3 oldjiagousys 的使用边界

`/home/ubuntu/workspace/oldjiagousys` 只保留为历史参考目录，允许用于：

- 理解已经存在的采集业务行为和数据库语义；
- 查阅历史修复记录、原因码、恢复规则和测试样本；
- 对旧行为做回放、Shadow/Diff 和兼容性对照。

禁止：

- 在 `oldjiagousys` 继续编写新代码或作为发布目录；
- 将旧目录 1:1 复制到新系统；
- 直接复制旧模块、旧队列、旧状态机或旧部署拓扑后只改名称；
- 让 CI、镜像构建或 Argo CD 读取 `oldjiagousys`。

新代码必须在 `newcrawlSystem` 中按照 24.4 已冻结的 Control/Data/Storage/Distribution 边界重新设计、实现、测试和提交。旧代码只能提供行为证据，不能自动成为新实现。旧的 GitHub 仓库只读，不作为新仓库的上游或发布源。

### 0.4 代码提交门槛

每个可合并提交至少完成：

```text
[ ] 单元测试/契约测试通过
[ ] 数据库迁移可正向执行，并有恢复说明
[ ] 业务身份、幂等键、原因码和状态迁移经过对照测试
[ ] 不包含密码、Token、私钥和真实业务数据
[ ] 镜像可以用 Git revision 唯一定位
[ ] validation overlay 可以渲染
[ ] 变更说明写入 docs/ 或对应模块文档
```

---

## 1. 当前服务器与角色分配

| 逻辑节点 | 公网 IP | 内网 IP | 初始角色 | 规格 |
|---|---|---|---|---|
| A1 | 43.173.68.88 | 10.4.4.12 | Kubernetes 控制面、Argo CD、核心应用 | 4C/8G |
| A2 | 43.173.68.187 | 10.4.4.3 | Kubernetes 控制面、应用副本 | 2C/8G |
| A3 | 43.172.94.2 | 10.4.4.17 | Kubernetes 控制面、应用副本 | 2C/8G |
| S1 | 43.172.65.165 | 10.4.4.2 | PostgreSQL Primary、Kafka、SeaweedFS | 2C/8G |
| S2 | 43.159.169.76 | 10.4.4.8 | PostgreSQL Standby、Kafka、SeaweedFS | 2C/8G |
| S3 | 43.172.80.48 | 10.4.4.5 | PostgreSQL Standby/仲裁、Kafka、SeaweedFS、ClickHouse | 2C/8G |

### 1.1 角色边界

```text
A1/A2/A3
  Kubernetes 控制面
  Argo CD
  Temporal / Planner / Starter
  Query Engine
  Discover / Full / Incremental Worker（验证期少量运行）
  Data API / Agent Worker
  Data Ingestor
  Feature / Read Model Projector
  Business Console API

S1/S2/S3
  一个逻辑 Crawler PostgreSQL：Primary + Standby
  PgBouncer 或稳定数据库写入口
  Kafka 3 Broker（验证期低保留）
  SeaweedFS 3 节点
  ClickHouse（验证期单节点，放在 S3）
```

S 节点的有状态组件首期可采用 Pigsty、系统服务或受控容器部署；不强行全部放进 Kubernetes。逻辑拓扑保持不变。

---

## 2. 实施原则

1. 先建立基础设施和稳定地址，再部署应用。
2. 先固定副本和低并发跑通链路，再开启 KEDA/HPA。
3. Worker 不直接写 PostgreSQL，所有执行结果进入 Data Ingestor。
4. 普通 Delta 直接走 Data Ingestor → PG；只有大型 Raw、恢复对象和超大 Submission 使用 SeaweedFS。
5. `publication.outbox` 只走 Debezium → Kafka；分析链使用独立的 minimal outbox → ClickHouse。
6. Read Model 和 Execution Trace Projection 可重建，不能参与授权、Plan 结算、Clock 推进或 Publication 幂等。
7. 验证环境不做吞吐承诺，不用低配环境替代生产容量压测。
8. 所有部署参数进入 GitOps 的 `base` 和环境 `overlay`，避免以后升配时改业务代码。

### 2.1 基础设施与代码的交替节奏

本项目采用“基础底座 → 最小代码切片 → 部署验证 → 扩展下一层”的节奏：

| 阶段 | 先搭建的内容 | 同阶段编写的内容 | 进入下一阶段的条件 |
|---:|---|---|---|
| 0 | GitHub 新仓库、当前工作树、文档和凭据边界 | 只做旧系统行为盘点，不写业务代码 | 新仓库可提交，来源边界明确 |
| 1 | 六台机器网络、主机名、swap、系统依赖 | 建立 migration/module/runtime 映射和测试约定 | SSH、内网、系统基线通过 |
| 2 | A1/A2/A3 Kubernetes、稳定 API Endpoint、Argo CD | 创建空的应用骨架、健康检查和 validation overlay | 集群可发布、可回滚 |
| 3 | S1/S2/S3 PostgreSQL/Kafka/SeaweedFS/ClickHouse 基础服务 | 编写第一批领域契约和最小 PostgreSQL migration | PG 可连接、migration 可重复执行 |
| 4 | Temporal、PgBouncer、监控和 Secret 注入 | 实现 Store、幂等和最小 Data Ingestor | 一条测试 Submission 能 RECEIVED/APPLIED |
| 5 | 固定副本的 Worker Runtime | 实现一个最小 Discover/Full 测试 Worker 和 Planner/Starter | Query → Plan → Temporal → Worker → PG 跑通 |
| 6 | Worker 部署隔离、资源限制和日志/Trace | 实现 Incremental、Data API、Agent 五角色共用内核 | 五角色结果都走同一 Data Plane |
| 7 | Read Model/Projector 运行环境 | 实现 Current Read Model、Execution Trace、Console API | Console 可读且投影可重建 |
| 8 | Debezium、Kafka Topic、ClickHouse 投递环境 | 实现 Publication Consumer、Inbox/Apply 和分析适配 | Publication、分析和交付回执分别通过 |
| 9 | 故障演练、备份、切主和 Worker 扩展节点 | 补齐 BC/Q/P/T/C/D 验收与 Runbook | 验收通过后再升配和扩容 |

代码可以在第 2 阶段开始写契约、单元测试和空服务，但只有在对应基础服务完成后才做集成测试和部署。每个阶段都必须在当前目录完成：

```text
代码切片 → 本地测试 → 构建镜像 → Argo validation 部署 → 链路验收 → Git commit/push
```

这样可以明确每一段是“正在搭基础设施”还是“正在实现代码”，避免基础设施和业务实现互相等待。

---

## 3. 阶段 0：建立本地交付目录和变量

### 3.1 创建实施目录

在当前目录维护以下文件，敏感信息不进入 Git：

```text
newcrawlSystem/
├── 24.3_...md
├── 24.4_...md
├── 24.5_爬虫平台部署实施与验证规划.md
├── SERVER_INVENTORY.local.md
├── deploy/
│   ├── base/
│   └── overlays/
│       ├── validation/
│       └── production/
├── ops/
│   ├── inventory/
│   ├── scripts/
│   ├── runbooks/
│   └── checks/
└── secrets/                 # 只放本地或 Secret Manager 引用，不提交明文
```

### 3.2 固定环境变量

先确定并记录以下变量：

```text
CLUSTER_NAME=crawlsystem
PRIVATE_NETWORK=10.4.4.0/22
CONTROL_PLANE_ENDPOINT=<内网 VIP 或稳定 DNS，待供应商提供/分配>
PG_PRIMARY_ENDPOINT=<稳定写入口>
PG_READ_ENDPOINT=<只读入口，可选>
KAFKA_BOOTSTRAP=<Kafka 内网地址列表>
SEAWEEDFS_S3_ENDPOINT=<S3 内网地址>
CLICKHOUSE_ENDPOINT=<ClickHouse 内网地址>
```

三台控制面必须使用同一个稳定的 Kubernetes API Endpoint。若没有可用内网 VIP，先配置 kube-vip/keepalived 或供应商内网负载均衡；不能把某一台 A 节点的临时 IP 当成永久控制面地址。

### 3.3 代码基线盘点

代码正式编写前，在当前目录完成一次基线导入和盘点：

1. 在新的 `newcrawlsystem-9-18` 仓库建立 `main`，当前目录作为唯一工作树。
2. 只把本地 `oldjiagousys` 当作行为和历史资料参考，不复制其目录结构、旧队列或旧运行时。
3. 对现有 API、Worker、数据库表、兼容层和部署文件建立索引，记录“复用、适配、重新实现、废弃”四种结论。
4. 在 `docs/migration/` 保存旧模块到 24.4 目标模块的映射、行为样本和差异说明。
5. 为每个目标边界建立最小编译和测试入口，先保证当前代码可以构建，再开始拆分服务。

基线盘点的输出至少包括：

```text
docs/migration/module-map.md
docs/migration/database-map.md
docs/migration/runtime-map.md
docs/migration/compatibility-boundary.md
```

### 3.4 代码实施顺序

代码按业务事实和恢复边界从底向上实施，不能先写控制台页面再倒推权威状态：

| 顺序 | 代码交付 | 关键验收 |
|---:|---|---|
| 1 | 领域契约、稳定 ID、原因码、状态和版本对象 | 编译、序列化、旧结果对照 |
| 2 | PostgreSQL migration、CrawlerStore、PlanStore、Feature Store、幂等约束 | T1～T4、并发和重复提交 |
| 3 | `publication.outbox`、analytics/minimal outbox 和回执模型 | 两条 Outbox 责任不混用 |
| 4 | Temporal workflow、Planner、Starter、Query Engine、三 Clock | Query/Plan/Clock 验收 |
| 5 | Data Ingestor、Submission/Frame/Connection 边界和本地 Journal | RECEIVED/APPLIED、背压、恢复 |
| 6 | Discover、Full、Incremental、Data API、Agent 五执行角色适配 | P-01～P-40 及原采集规则 |
| 7 | Feature、Plan Settlement、Read Model、Execution Trace Projection | 可重建、水位、BC 读链路 |
| 8 | Business Console API/UI 和控制命令复核 | BC-01～BC-30，控制写回权威 Store |
| 9 | Debezium/Kafka 配置、Business Consumer Inbox/Apply、ClickHouse 适配 | Publication、分析和交付回执 |
| 10 | Kubernetes/Argo manifests、观测、Runbook 和端到端测试 | validation overlay 可部署可回滚 |

每完成一个顺序，先在当前目录运行测试并提交 Git，再进入下一个边界。旧系统的采集算法迁移必须通过固定样本、回放和 Shadow/Diff 验收，不能用复制旧代码或只改服务名称代替业务等价验证。

### 3.5 GitHub、镜像仓库与 Argo CD 的关系

```text
newcrawlSystem 工作目录
        │ git commit / push
        ▼
GitHub hjyl-cheng/newcrawlsystem-9-18
        │ CI：测试、迁移检查、构建镜像
        ▼
镜像仓库（GHCR 或确定的 Registry）
        │ 镜像 digest + Git revision
        ▼
Argo CD
        │ 同步当前环境 overlay
        ▼
Kubernetes 集群
```

Argo CD 只发布 `newcrawlSystem` 产生的 Git revision。服务器上的手工修改、`oldjiagousys` 中的代码和未提交的本地文件都不能成为发布来源。

---

## 4. 阶段 1：六台服务器基础初始化

在每台服务器执行，并将结果保存到 `ops/checks/`：

### 4.1 主机和网络

1. 设置唯一主机名：`a1`、`a2`、`a3`、`s1`、`s2`、`s3`。
2. `/etc/hosts` 写入六台机器的内网 IP 和主机名。
3. 确认所有节点之间使用内网 IP 通信，不让 Kafka、PG、SeaweedFS 和 Kubernetes 节点通信绕公网。
4. 确认 DNS、NTP/chrony、默认路由和 MTU 一致。
5. 记录磁盘设备、文件系统、可用空间和挂载点；S 节点优先使用 SSD。

### 4.2 系统基线

1. 更新系统补丁并固定内核版本。
2. A1/A2/A3 安装 `containerd`、`runc`、`CNI` 依赖、Kubernetes 工具和基础诊断工具；S1/S2/S3 按 PostgreSQL/Kafka/SeaweedFS/ClickHouse 的受控部署方式安装对应依赖，不强行加入 Kubernetes。
3. Kubernetes 节点关闭 swap，加载所需内核模块并设置转发、bridge-nf 和连接跟踪参数。
4. 设置时区、日志轮转、文件句柄和进程限制。
5. 配置防火墙：公网只开放 SSH；集群和数据端口只允许内网网段与指定节点访问。
6. 禁止密码登录后再关闭密码登录，必须先验证 SSH 公钥登录成功。
7. 配置磁盘和内存告警，避免低配验证时 OOM 后没有记录。

### 4.3 初始化验收

```text
[ ] 六台机器主机名唯一
[ ] 六台机器可以通过内网互 ping
[ ] A 节点可以访问 S 节点指定端口
[ ] chrony 同步正常
[ ] containerd 正常运行
[ ] swap 已关闭
[ ] 磁盘挂载和剩余空间已记录
[ ] SSH 公钥登录成功
[ ] 防火墙规则已导出保存
```

---

## 5. 阶段 2：建立 Kubernetes 三控制面集群

### 5.1 集群方案

采用 kubeadm + containerd，A1/A2/A3 组成一个 Kubernetes 集群：

```text
稳定 API Endpoint
        │
   ┌────┼────┐
   A1   A2   A3
 control plane + etcd
```

安装一套 Kubernetes 集群，不为每台 A 服务器安装独立集群。Argo CD 是该集群中的一套逻辑实例，组件副本可分布在 A1/A2/A3。

### 5.2 安装顺序

1. 在 A1 初始化第一个 control plane 和集群证书。
2. 配置稳定 API Endpoint，确认 A1 故障时 Endpoint 不变。
3. 安装 CNI，验证 Pod 跨节点网络和 Service DNS。
4. 生成 join 命令，让 A2、A3 以 control plane 身份加入。
5. 配置节点标签：

```text
A 节点：node-role=crawler-control
S 节点：node-role=storage（若后续纳入 K8s，仅作为明确标记）
```

6. 配置 taint/affinity，避免验证期普通应用无意中占满控制面节点。
7. 安装 metrics-server；Prometheus/Grafana 在资源允许时部署轻量版本。
8. 导出集群配置、证书备份和节点清单，保存到受保护位置。

### 5.3 Kubernetes 验收

```text
[ ] kubectl get nodes 三台 A 节点均为 Ready
[ ] control plane/etcd 无持续错误
[ ] Pod 可以跨 A 节点通信
[ ] Service DNS 正常
[ ] A1 上停止一个非关键 Pod，能在其他 A 节点恢复
[ ] A1 重启后 A2/A3 仍可管理集群
[ ] API Endpoint 不依赖单台 A 节点临时地址
```

---

## 6. 阶段 3：安装 Argo CD 和 GitOps 骨架

### 6.1 Argo CD 部署

1. 在 Kubernetes 集群中安装一套 Argo CD。
2. 为 Argo CD 配置持久化和访问入口；验证期可以使用内网入口。
3. 将 Git 仓库、目标集群和命名空间注册到 Argo CD。
4. 先关闭自动 prune，首期人工批准同步。
5. 为 `validation` 和 `production` 建立不同 overlay。

### 6.2 GitOps 目录边界

```text
deploy/base/
  namespace、ServiceAccount、ConfigMap 模板
  Temporal、API、Worker、Ingestor、Projector、Console

deploy/overlays/validation/
  replicas=1
  worker_concurrency=1
  低资源 requests/limits
  低 Kafka retention
  ClickHouse 单节点

deploy/overlays/production/
  更高 requests/limits
  应用副本 2/3+
  Worker 并发和扩缩容边界
  生产存储、备份和 HA 参数
```

应用镜像、数据库迁移和配置变更必须有版本号；数据库迁移先执行，再发布依赖新字段的应用。

### 6.3 Argo 验收

```text
[ ] Git commit 可以触发 Argo CD 差异检测
[ ] 手工同步成功
[ ] 应用健康状态可见
[ ] 回滚镜像不会回滚数据库数据
[ ] 误删测试 Deployment 可以被 Git 配置恢复
[ ] validation overlay 不会读取 production Secret
```

---

## 7. 阶段 4：部署数据层（S1/S2/S3）

数据层按依赖顺序部署。所有服务使用内网地址，应用只拿稳定入口，不绑定某一台物理机器。

### 7.1 PostgreSQL：一个逻辑 Crawler PG

1. S1 部署 PostgreSQL Primary。
2. S2、S3 配置 Streaming Replication，形成 Standby/恢复节点。
3. 配置 PgBouncer 或稳定写入口：应用只连接稳定域名/虚拟 IP。
4. 初始化业务 schema、迁移表、索引、分区和约束。
5. 创建只包含 `publication.outbox` 的 PostgreSQL Publication。
6. 配置备份、WAL 归档、恢复目录和保留策略。
7. 首期验证主备复制、备份恢复和连接切换；不做 Citus、Shard Router 或应用级分片。
8. 生产使用 PostgreSQL 17+ 时验证 Debezium failover slot；若使用更早版本，执行书面 slot 重建和 LSN/offset 对账 Runbook。

验收：

```text
[ ] Primary 写入后 Standby 可读到
[ ] PgBouncer/稳定入口可用
[ ] 业务事务和约束正常
[ ] publication 只包含 publication.outbox
[ ] 备份可恢复到独立目录
[ ] 模拟切主后能恢复应用连接
```

### 7.2 Kafka：只承载 Distribution Plane

1. S1/S2/S3 部署 3 个 Kafka Broker，使用 KRaft 模式。
2. 配置低保留、低分区用于验证；生产再按吞吐和顺序要求调整。
3. 创建 Publication Topic，例如 `crawler.publication.v1`。
4. 首期不把 Worker Result、普通遥测或内部 Temporal 任务放入 Kafka。
5. 配置 Broker、Topic、Consumer Group、保留和磁盘告警。

验收：

```text
[ ] 三个 Broker 能互相发现
[ ] Topic 可以生产和消费
[ ] Broker 重启后消息仍可消费
[ ] Consumer 重复消费不会破坏测试 Inbox 幂等
[ ] Kafka 不承载采集主链和普通日志
```

### 7.3 SeaweedFS：只承载大对象路径

1. S1/S2/S3 部署 SeaweedFS Master/Volume/Filer/S3 所需组件。
2. 创建 Raw、Recovery、Submission 等受保护 bucket/collection。
3. 设置对象引用、校验和、生命周期与恢复策略。
4. Worker 使用 S3 流式上传大型 Raw 后提交受保护引用；普通 Delta 不先上传 SeaweedFS。

验收：

```text
[ ] 对象可写入、读取和删除
[ ] 对象引用能在 PG 中保存
[ ] Worker 断开后不会留下不可追踪的无主对象
[ ] S3 访问密钥不写入镜像和 Git
```

### 7.4 ClickHouse：验证期单节点

1. 在 S3 部署单节点 ClickHouse。
2. 初始化历史分析表和去重键。
3. 使用独立 analytics/minimal outbox 投递，不复用 Publication Kafka 链。
4. 设置小批量、低并发和保留期限，防止验证期占满磁盘。

验收：

```text
[ ] 分析事件可批量写入
[ ] 重复事件按版本/事件键去重
[ ] ClickHouse 暂停时 PG 事实不回滚
[ ] 积压、最老事件年龄和磁盘使用可告警
```

---

## 8. 阶段 5：按依赖顺序部署应用服务

应用服务部署在 Kubernetes，由 Argo CD 管理。验证期所有副本先设为 1，并设置资源上限。

### 8.1 基础服务顺序

```text
1. Namespace / Secret 引用 / ConfigMap
2. 数据库迁移和基础 schema
3. Temporal Server 与 Worker Namespace
4. Planner / Starter
5. Query Engine 与 Query Clock
6. Data Ingestor
7. Discover / Full / Incremental Worker
8. Data API / Agent Worker
9. Feature
10. Read Model Projector / Execution Trace Projection
11. Business Console API
12. Publication Outbox → Debezium → Kafka
13. 外部 Business Consumer 模拟器
```

### 8.2 关键边界

- Temporal 负责工作流持久化、重试和执行编排，不替代业务事实表。
- Planner/Starter 负责生成合法 Plan，不直接替 Worker 判定最终事实。
- Worker 负责采集和本地 Journal，不直写 Crawler PG。
- Data Ingestor 负责接收、身份校验、批量 Apply 和回执。
- Feature/Clock/Plan 结算继续以 PG 权威事实为准。
- Read Model 只从权威事实投影，可删除后重建。
- Business Console 的控制命令必须回权威 Store 复核授权、版本和当前状态。
- Debezium 只捕获 `publication.outbox`，Business Consumer 通过 Inbox/Apply 幂等落地。

---

## 9. 阶段 6：跑通第一条最小业务链路

先使用一个测试 Query、一个测试 Channel 和极小任务量，不连接大规模真实采集。

### 9.1 主链路

```text
Query Engine
  → Planner / Starter
  → Temporal
  → Discover 或 Full Worker
  → Rota / YouTube 测试入口
  → Worker Local Journal
  → Data Ingestor
  → PgBouncer
  → PostgreSQL Primary
  → Feature / Clock / Plan Settlement
  → Read Model Projector
  → Business Console
```

### 9.2 验证项

```text
[ ] QueryRun 能创建并绑定 Country + Business Category
[ ] Plan 能被 Temporal 接收
[ ] Worker 执行身份、版本和租约可追踪
[ ] Worker 不持有 PG 直写权限
[ ] Data Ingestor 能返回 RECEIVED/APPLIED
[ ] PG 中事实、checkpoint、observation、outbox 同事务成立
[ ] Query Clock 能按 WEEK/MONTH 推进
[ ] Read Model 有 source_watermark/updated_at
[ ] Console 能从当前读模型查看状态和执行链路
[ ] 删除 Read Model 后可以重建
```

---

## 10. 阶段 7：跑通三条分发和读取链路

### 10.1 Publication 链路

```text
PG publication.outbox
  → PostgreSQL WAL
  → Debezium Connector
  → Outbox Event Router
  → Kafka crawler.publication.v1
  → Business Consumer
  → Business DB Inbox/Apply
  → DELIVERED 回执
```

验收消息重复、Consumer 重启、Kafka 暂时不可用和 Business DB 暂时不可用时，Crawler PG 的事实仍然正确，恢复后可以按 `event_id` 幂等补齐。

### 10.2 分析链路

```text
PG analytics/minimal outbox
  → 有界批量 Publisher
  → ClickHouse
  → 历史趋势/多维统计 API
```

验证 ClickHouse 延迟时页面显示数据水位，不用 Kafka offset 冒充业务交付确认。

### 10.3 大对象链路

```text
Worker
  → SeaweedFS S3
  → PG 保存 object_key、hash、size、保护状态
  → Read Model/Console 展示引用
```

验证对象上传中断、重复上传、引用丢失和恢复清理。

---

## 11. 阶段 8：故障与恢复验证

验证期只做小规模故障演练，记录实际恢复时间和日志，不把结果当生产 RPO/RTO 承诺。

```text
1. 重启一个应用 Pod：检查 Temporal/Worker 有界收尾和任务恢复。
2. 停止一个 A 节点：检查 Kubernetes 调度和 Console 可用性。
3. 停止 S2 Kafka：检查 Topic 副本和恢复。
4. 暂停 ClickHouse：检查 analytics outbox 积压和水位展示。
5. 暂停 SeaweedFS：检查大对象失败原因码和重试边界。
6. PostgreSQL Standby 重启：检查复制追赶。
7. 模拟 PostgreSQL 主切换：检查稳定连接入口、slot、Debezium 和 offset 对账。
8. 重复提交同一批次：检查 Idempotency 和 APPLIED 结果。
9. 删除并重建 Read Model：检查投影水位和结果一致性。
```

每次演练记录：开始时间、故障对象、用户可见现象、恢复动作、恢复完成时间、数据是否缺失、是否需要人工介入。

---

## 12. 阶段 9：验收顺序

严格按依赖从底向上验收：

```text
I. 网络、时间、磁盘和 SSH
II. Kubernetes 三控制面
III. Argo CD 发布和回滚
IV. PostgreSQL 主备、备份和 PgBouncer
V. Kafka / SeaweedFS / ClickHouse 基础读写
VI. Temporal 和 Planner/Starter
VII. Worker → Data Ingestor → PG
VIII. Feature / Clock / Plan Settlement
IX. Read Model / Execution Trace / Console
X. Publication → Debezium → Kafka → Business Consumer
XI. 故障、恢复、重复和水位
XII. BC-01～BC-30、Q-01～Q-30、P-01～P-40 及原有 T/C/D 验收映射
```

验收结果至少保存：

```text
ops/checks/<date>-network.md
ops/checks/<date>-k8s.md
ops/checks/<date>-storage.md
ops/checks/<date>-application.md
ops/checks/<date>-e2e.md
ops/runbooks/postgres-failover.md
ops/runbooks/debezium-slot-recovery.md
ops/runbooks/read-model-rebuild.md
```

---

## 13. 阶段 10：从验证环境升到生产

### 13.1 只升配时的操作

1. 先对 A/S 节点逐台增加 CPU、内存和 SSD 容量。
2. 修改 production overlay 中的 requests/limits、连接池、Kafka retention、ClickHouse 和 PG 参数。
3. 逐步增加应用副本和 Worker 并发。
4. 观察 PG 锁等待、WAL、事务尾延迟、Kafka lag、Apply 积压、磁盘和 OOM。
5. 逐个开启 HPA/KEDA，不一次性开启所有自动扩缩容。
6. 执行生产备份、切主和恢复演练后，再增加任务准入。

业务代码、核心协议、数据模型和服务边界不因升配改变；有状态系统的主备、备份、slot、分区、磁盘和副本参数必须按 Runbook 明确配置。

### 13.2 增加采集 Worker 节点

当 A 节点不再适合承载采集 Worker 时：

1. 新服务器安装相同版本的操作系统、containerd、CNI 和监控代理。
2. 以 Kubernetes worker 身份加入同一个集群。
3. 添加标签和 taint：

```text
node-role=collector
worker-pool=discover|full|incremental|api|agent
```

4. Argo CD 更新 Worker Deployment 的 nodeSelector、affinity 和副本上限。
5. 先增加一个 Worker 节点，验证接单、停止、租约和结果回流。
6. 再按采集角色增加节点和副本，不为每个频道创建永久 Pod。
7. 通过 Data Plane 背压、Rota 许可、PG 连接预算和 Apply 积压限制扩容速度。

新增 Worker 节点不会要求修改 Query、Plan、Data Ingestor、Read Model 或 Publication 业务协议。

---

## 14. 当前第一批实际执行顺序

```text
第 1 步：newcrawlSystem 初始化为唯一 Git 工作树，关联 newcrawlsystem-9-18 并创建 main（已完成）
第 2 步：oldjiagousys 只读盘点，形成模块/数据库/运行时/兼容边界文档（已完成）
第 3 步：六台服务器完成 SSH、内网、主机名、hosts、swap 和系统基线（已完成）
第 4 步：A1/A2/A3 完成 containerd、Kubernetes 工具和内核参数准备（已完成）
第 5 步：确定 Kubernetes API VIP/DNS，建立 A1/A2/A3 集群
第 6 步：安装 Argo CD，同时创建空应用骨架、健康检查和 validation overlay
第 7 步：部署 S1/S2/S3 的 PostgreSQL/Kafka/SeaweedFS/ClickHouse 基础服务
第 8 步：编写并执行第一批领域契约、migration、Store、幂等和 Outbox 测试
第 9 步：部署 Temporal/PgBouncer，实现最小 Data Ingestor 和测试 Submission
第 10 步：实现最小 Planner/Starter/Discover 或 Full Worker，跑通 Query → Plan → Worker → PG
第 11 步：按角色扩展 Incremental、Data API、Agent，并统一接入 Data Plane
第 12 步：实现 Read Model、Execution Trace 和 Business Console，验证可重建水位
第 13 步：部署 Debezium/Kafka/ClickHouse 适配，实现 Publication 和分析链
第 14 步：完成故障、恢复、重复、切主和读模型重建验收
第 15 步：整理 Runbook，再开始升配、增加 Worker 节点和 production overlay
```

2026-09-18 更新：第 1～4 步已完成；三控制面已建立，当前 API DNS 指向 A1，属于验证入口，生产 HA 入口尚待完成。Argo CD 已通过新仓库部署基础设施探针，并开展 Git 发布与回滚验证。PG 主备、PgBouncer、Kafka、SeaweedFS、ClickHouse 已完成基础部署及各自读写检查。

补充 UDP 4789 规则后，A1/A2/A3 的跨节点 Pod、DNS、Service 和存储端口验收均通过。基础设施探针 GitOps 已完成 v1 → v2 → Git revert 恢复 v1，三节点内容检查通过。具体证据与限制见 `ops/checks/2026-09-18-infrastructure-gitops.md`。

应用工程骨架已建立：Node 24 / TypeScript、健康和版本接口、停止收尾、GitHub Actions 编译测试与 GHCR 镜像发布。`runtime-smoke` 通过 Argo CD 部署到验证环境，固定镜像 digest，两个副本跨 A 节点运行。该应用不含业务逻辑；Temporal 和领域代码尚未实施。镜像发布、跨节点访问与回滚记录见 `ops/checks/2026-09-18-application-runtime.md`。

接下来进入第 8 步：先冻结第一批领域契约、稳定 ID、原因码和样例，再编写版本化 migration、Store 与幂等测试。完成数据库验收后再接 Temporal 和最小 Data Ingestor；不先批量创建只有名字的业务服务。

2026-09-20 更新：第 8 步的第一项已形成首批 Worker → Data Ingestor 提交/回执契约草案，见 `24.6_业务通信协议与数据契约_第一阶段.md`。
JSON Schema 与固定样例位于 `contracts/data-plane/`，格式及字节/Hash 向量测试接入 `npm test`。
这是第一批元数据协议定义，尚未正式冻结生产 v1；完整业务正文、Discover/API/Agent 等边界随对应切片补充。
此时仅完成协议；随后事实表 migration 进展见下文。Store、gRPC 服务和运行时故障验收仍未完成，不能把 Schema 测试等同于幂等事务已实现。

数据库设计讨论已记录到 `24.7_数据库表结构设计_单库与事务边界讨论稿.md`：Business DB 不变，爬虫业务单库，评论采用视频表 JSONB 方案；评论数量更新与已有非空正文保留分开。旧库仅参考，未来数据导入单独编写脚本。
该文档列出目标表、核心字段/约束、原子事务、首批实施范围与验收，第 16 节集中记录架构/协议/实际实现差异。24.4 作为设计基线接受实际验证和有依据的修订，不作为不可修改的实现约束。设计阶段的记录不等同于所有表已实施；当前进展见下文。

字段核对补充：`24.8_采集事实表逐字段映射与约束核对.md` 逐列记录 channels、contents、agent_profiles、finalized_profiles、channel_about_metric_snapshots 共 173 列的新位置与处置理由，配套 `docs/migration/collection-field-map.json`。
本轮补齐视频 position/is_recent/观察 Hash、频道字段质量和休眠复查等概要遗漏，明确评论数量/正文、Hash 格式、Raw 中在线证据及 Agent Current/请求失败分离的兼容边界。首个事实表 migration 已以此为输入在隔离库验证，其他目标表仍待后续切片实施。

验证期暂使用系统盘和单节点入口；生产前必须补齐稳定入口、磁盘与资源隔离、备份和故障切换演练。在 GitOps 和网络验收通过、数据库 migration 可追踪前，不开始大批量部署应用。


2026-09-20 首个 migration 更新：新增版本/校验和执行器及 channels/contents 事实表，在 S1 独立测试库完成 10 项集成验证；既有 19 项测试通过。
正式 crawler 库尚未应用本批，测试库保留空表与迁移台账。执行/恢复说明见 `database/README.md`，证据见 `ops/checks/2026-09-20-database-facts.md`。
下一步仍在第 8 步内：实现并验证 Store 的字段合并、评论保留/补齐、观察顺序，再接授权与 Submission/回执原子事务，暂不跳到部署业务 Worker。


2026-09-20 Store 更新：内容指标/评论内部 Store 与 0002 证据迁移完成，在独立 PG 库通过 24 项集成测试，另有 19 项既有测试通过。
已实现正文保留、空页补齐、有效数量观察顺序、同时间冲突回滚、并发身份合并及共享事务接口。
实际范围/待办见 `docs/migration/content-store.md`，证据见 `ops/checks/2026-09-20-content-store.md`。
仍在第 8 步，下一切片接 Plan/授权/Submission/检查点/回执原子事务；不提前部署真实采集服务。


2026-09-20 受控提交更新：0003 与内部 Fast Apply 完成，最小计划/执行授权、冻结输入和目标、事实/批次检查点/APPLIED 同事务验证通过。
38 项数据库测试、21 项契约/应用测试通过；包括并发幂等、旧 epoch、错目标、整体回滚、COMMIT 确认丢失对账。
仍是第 8 步的局部增量指标切片，未完成完整 Domain/Plan 结算。下一步补认证/Manifest 适配和最小 Data Ingestor 服务接口，然后部署验证环境；Temporal/真实 Worker 尚未实施。
说明见 `docs/migration/metrics-submission.md`，验收见 `ops/checks/2026-09-20-metrics-submission.md`。

2026-09-20 Kafka 结果入口更新：复用 S1/S2/S3 Broker，建立独立 Results 验证 Topic（3 分区、3 副本、min ISR=2），不改变 Publication Topic。
签名生产端、消费组并发处理、PG 成功后提交 offset、重放幂等、坏消息原文隔离已实现；0004 仅应用于隔离验证库。
9 项真实 Kafka/PG 测试及 24 项无数据库测试通过；数据库回归 37 项通过、1 项已升级库跳过。证据见 `ops/checks/2026-09-20-kafka-results.md`。

接下来的搭建与代码顺序：

1. 编写常驻 Data Ingestor 消费服务入口及健康、积压指标和回执/隔离查询；复用已验证的库。
2. 配置生产者签名凭据、Broker 认证/ACL、运行时最小 PG 角色和受控 Secret 注入，补齐相应部署配置。
3. 构建新仓库镜像，通过 Argo CD 部署验证消费者；先用受控样例检查滚动重启、重放与查询，再接真实 Worker。
4. 接最小 Planner/Temporal 和 Worker 的本地待确认记录，形成任务到终态闭环；扩充各采集角色、Publication 和控制台按后续切片推进。

当前部署仍为 runtime-smoke；本次没有新增消费者 Pod、正式 crawler 迁移或真实采集。链路跑通不等于生产容量、自动切主和备份恢复已验收。

2026-09-20 常驻服务实施：Data Ingestor 入口、受保护回执/处理记录查询、依赖健康和积压指标已编写，专用验证库 crawler_validation_ingestor 与受限 runtime 账号已准备。
部署目标为 A 集群两个副本、同一 Kafka Results 消费组，经新仓库镜像/digest/Argo CD 发布；部署与验收结果见 `ops/checks/2026-09-20-ingestor-service.md`，操作说明见 `ops/ingestor/README.md`。
此验证阶段沿用内网 PLAINTEXT Kafka；生产 SASL/TLS/ACL 和密钥轮换仍未完成，不能把消息签名或 Pod 网络策略等同于 Broker 身份隔离。

常驻部署验收完成：CI 镜像 6255a883 对应 digest 已固定，Argo 同步两个副本；真实提交、隔离、查询权限、Pod 网络限制和 135 条滚动重放均通过。下一项可接最小控制调度与 Worker 待确认记录，生产 Kafka 安全、自动告警和 HA 单独推进。
