# 业务操作与 GitOps 发布职责及扩缩容实施建议

日期：2026-09-20。状态：讨论后的实施建议；本次仅补充规划，未部署业务后台、KEDA 或自动接管组件。

**用户明确定位：业务系统按策略自动持续运行，不依赖人工逐个创建任务，也不依赖控制台页面保持打开。**首次完成业务范围、种子、代理与资源预算配置后，由 Query 闭环、Planner 和三 Clock 自动产生发现、采集、更新需求；Temporal 执行工作流，Worker 完成采集，入库及结算结果驱动后续轮次。控制台主要负责观察、资源管理、规则调整和异常干预；手动触发仅为可选辅助功能。

## 1. 两条控制链

```text
自动业务运行（不需要页面在线）
已生效策略、种子与持久进度 → Query / Planner / 三 Clock
          ↑                            ↓
结果与结算驱动后续轮次 ← 入库与回执 ← Temporal / Worker

管理与干预
业务控制台 → 已鉴权的业务 API → PG 中的业务配置、操作记录
                                ├→ 调整自动运行规则、预算或启停状态
                                └→ 代理配置快照 → Worker 的 IP 扩展

运行规模（后续分阶段实现）
业务接单预算、合格积压、下游容量 → KEDA / HPA → Kubernetes 调整 Worker 数量

程序发布
当前新仓库 → GitHub CI 构建 → Registry 不可变镜像
          → Git 中更新镜像摘要 → Argo CD → Kubernetes 更新程序
```

Git 保存代码、迁移、部署清单和运维资料；业务运行配置以受控 API 写入的 PG 状态为准，不要求用户为每次采集操作提交 Git。镜像在 Registry；数据库、代理凭据和运行数据不放到 Git。代码的唯一来源仍为当前 newcrawlSystem，旧项目只读参考。

业务控制台的 Read Model 用于展示；用户操作调用权威业务 API，不直接修改展示投影作为执行依据。页面显示操作已接收、目标配置版本、实际生效版本和失败原因，不能把保存成功显示成执行完成。

## 2. 谁管理什么

| 配置或操作 | 权威来源 / 操作入口 | 执行者 |
|---|---|---|
| 业务范围、策略、系统启停、接单预算 | 业务控制台 → 业务 API → PG | 自动业务运行链读取已生效配置 |
| 任务产生、下一次执行时间、持续采集与更新 | 已生效策略、持久进度与业务事实 | Query / Planner / 三 Clock / Temporal / Worker；不依赖人工建任务 |
| 代理资产、分组、启停、节点归属 | 业务控制台 → 业务 API → PG | Worker IP 扩展应用版本化配置；批量报告运行状态 |
| 单 Worker 并发与请求速率 | 业务控制台提交受限运行配置 | Worker 应用配置；仍受全局预算约束 |
| 程序镜像、资源限制、网络、服务账号 | Git 中的部署声明 | Argo CD / Kubernetes |
| 首期固定 Worker 实例数 | Git 中的验证部署配置 | Argo CD / Kubernetes |
| 启用弹性后的实际 Worker 实例数 | KEDA 管理的 HPA | Kubernetes；业务 API 和 Argo 不并行写 replicas |
| 平台允许的伸缩上下限 | Git 中的平台部署配置 | KEDA / HPA 在范围内运行 |
| 新增物理服务器 | 采购、初始化、接入集群 | 运维自动化；接入和验证后显示在控制台 |

副本数、单实例并发、全局接单预算是三个不同量。增加 Pod 不增加物理 CPU、内存或代理出口；Pending Pod 不能记作已增加的可用采集能力。

## 3. 业务控制台如何调整采集规模

首期页面提供采集启停、全局/分组接单预算、单 Worker 并发和代理组配置。固定实例先验证真实链路。用户暂停接单只停止领取新工作；在途工作需正常收尾或通过明确取消流程处理，不把暂停等同于直接杀 Pod。

后续启用自动伸缩后，页面展示实际就绪实例、允许范围、队列等待、代理余量和下游积压。用户调整采集预算，调度和弹性系统在平台允许范围内调整执行能力，不承诺预算变大就立即增加实例或线性提速。

若需要页面直接指定“运行 10 个 Worker”，作为单独的管理员能力实现：必须明确手动/自动模式，使用受控扩缩容接口和唯一写入者，完成 HPA 的暂停与恢复交接。不能让业务 API 直接 patch replicas，同时让 HPA 持续改回。该能力目前属于建议，不作为已确认或已开发功能。

不让浏览器持有 Kubernetes 管理凭据。需要 Kubernetes 操作的后台身份只授权指定命名空间和工作负载；业务策略修改不需要集群管理员权限。

## 4. 与 24.4 的关系及防覆盖规则

24.4 第 13.4 节已规定：Git/Argo 管镜像、资源和伸缩上下限，KEDA/HPA 管启用后的实际 replicas。这里继续沿用，不把日常操作都改为 Git 提交。

本次澄清了此前对话中“页面直接改实例数”的简化说法：自动伸缩模式下，页面调整业务预算，实际数量由唯一扩缩容机制决定。若后续将伸缩上下限也开放到页面，必须另行记录对 24.4 的修改，并明确这些字段的唯一权威来源；不能 Git 和 PG 各保存一套互相竞争的目标值。

实际接入 HPA 时，对指定 Worker 工作负载设置 Argo 的 `/spec/replicas` 差异忽略和 `RespectIgnoreDifferences=true`，同时审查清单字段管理。只设差异忽略不等于同步时不会覆盖。初次创建、从固定副本移交、镜像发布、同步和回退都要实测；不对所有资源整体忽略差异，也不在当前没有 HPA 的验证服务上提前放弃副本管理。

伸缩需求必须包含合格待执行需求，不能只依赖可能被准入压到零的队列。扩容受代理容量、Kafka 积压、Ingestor/PG 处理能力和物理资源约束；缺失或过期指标不能被当作容量充足。缩容先验证停止接单、在途收尾和结果确认，不能凭队列为零认定 Worker 空闲。

## 5. 实施顺序与验收

1. **现在：继续外围收尾。**按 24.12 推进 Kafka、SeaweedFS、ClickHouse、CDC、监控、访问控制和恢复验收。业务功能仍暂停。为后续扩容准备积压、资源、健康和背压指标。
2. **外围完成后：自动业务闭环与配置链。**先实现 Query / Planner / 三 Clock 的最小自动循环，以及 API、权限、配置版本与审计；控制台逐步接入展示和管理，不作为循环运行的前提。验证组件重启后配置与进度保留，旧配置不能覆盖新配置。
3. **固定少量 Worker 验收持续运行。**验证自动发现、准入、采集、入库、结算和到期更新能产生后续轮次，并检查任务恢复、代理分配、Kafka 结果、幂等入库与业务回执。验收期间不通过页面逐个建任务，关闭页面仍持续工作；不把 Temporal 或 smoke 演练当作真实采集完成。
4. **引入 KEDA/HPA。**先受限扩容，再安全缩容；验证自动伸缩、Git 同步、版本发布互不覆盖；下游积压时限制接单/扩容。
5. **验收新增节点。**新机器标准初始化、安装节点本地 API 入口、加入集群、应用网络及权限配置，再接纳 Worker；验证代理归属和在途任务，不复制数据库到每个采集节点。

发布控制器的无人值守整机接管属于外围可靠性待办，与任务调度高可用分开验收。云 API 确认旧机关闭再接管只是候选方案，尚未部署；还需受限云凭据、实例映射、唯一执行者，以及对该节点其他服务的影响验证。当前不因失联超时自动关闭 A 节点。Argo 暂时不可用通常不停止现有采集程序，但会影响新的发布与 Git 配置同步。
