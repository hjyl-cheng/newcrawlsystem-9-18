# 外围基础设施收尾与验收计划

日期：2026-09-20。用户要求：先完成外围，之后再开发业务代码。本文件覆盖 24.5 中旧的业务优先顺序。

## 当前范围

先建设服务器、运行平台、数据库/队列/对象存储、备份、稳定入口、权限、监控和故障恢复。保留现有 Data Ingestor、Temporal 和 smoke 服务作为探针。暂停代理管理 API/UI、业务 Planner、真实 Worker、Journal、Publication 业务逻辑等开发。

所有配置、脚本、验收和文档保存在唯一新仓库；旧仓库不改。没有引入 SQLite。外围建设完成并不代表尚未开发的业务全链路已完成。

业务操作与 GitOps 发布职责补充见 `24.13_业务操作与GitOps发布职责及扩缩容实施建议.md`：业务按策略自动持续运行，任务由 Query / Planner / 三 Clock 自动产生，不要求人工设置任务或保持页面在线；控制台负责观察、资源、规则与干预，程序发布走 Git/Argo。启用弹性后实际副本数由 KEDA/HPA 管理。本次只补充实施建议，不提前开发业务后台，不改变本文件的外围优先顺序。

## 按依赖顺序推进

| 顺序 | 工作 | 验收标准 | 本轮开始时状态 |
|---|---|---|---|
| 1 | PG 物理备份、WAL 归档、集群/配置备份 | 独立目录真实恢复；时间点恢复有证据；定时器与保留策略可检查；明确密钥和异地副本位置 | PG 仅主备，archive_mode=off；尚无备份任务 |
| 2 | PG 稳定入口与故障切换 | 停主后唯一可写主、旧主隔离、入口切换、回归主备；记录 RPO/RTO | PgBouncer 仅 S1，异步主备，无自动切主 |
| 3 | Kubernetes API 与 Argo 可用性 | 不依赖 A1 单机；控制面故障后仍可管理和发布 | 三控制面正常，API 地址仍指向 A1，Argo 组件集中 A1 |
| 4 | Kafka、SeaweedFS、ClickHouse 可靠性 | 分别完成副本/入口、元数据与数据恢复、访问边界和必要部署补齐 | Kafka 三节点；SeaweedFS 共享 PG、三 Filer/S3 与双入口已部署并验收部分故障；ClickHouse 单机 |
| 5 | 监控、告警、集中日志与凭据边界 | 节点/磁盘/复制/备份/积压异常可见；触发并解除测试告警；访问控制验证 | 部分健康接口；集中采集、告警和生产认证未完成 |
| 6 | 外围综合验收与运行手册 | 小数据量逐项故障演练、恢复与扩容流程；区分已验证和外部依赖 | 待前述项目完成 |

不把“装好软件”“端口通”“两个副本”直接记成生产高可用。各项逐步验收，不用压力测试替代功能与恢复验证。

## 现有容量与边界

现场 S1/S2/S3 各为 80 GiB 系统盘，可用约 68～70 GiB；没有独立数据盘。当前 PG 各库只有约 8～10 MB，小规模恢复无需增购机器或升配。

首期备份利用现有机器跨机保存，并使用受保护的专用目录和凭据。跨机备份仍在同一地域、账号和现有磁盘故障域内，不能视为异地灾备；外部备份存储及其保留权限在有实际资源后再接入。恢复演练只使用全新独立目录、独立端口/Unix socket，不覆盖现有数据库或复制槽。

PG 的自动切主与稳定入口需要一起设计，不能只装代理而没有防双主措施。Kubernetes/PG 稳定入口不能假设轻量云允许任意漂移 VIP，需验证供应商网络能力或选择有实际支撑的入口方案。

ClickHouse 是否以单节点加备份满足当前验证目标、或启用副本与 Keeper，要按最终可用性要求明确；不能把单节点标为 HA。SeaweedFS 元数据入口与数据副本需分别验收。

监控告警先在平台内形成可见状态；向外部聊天/邮箱发送消息需用户指定接收渠道并授权。系统升级配置时还需复核数据盘、连接池、并发和故障恢复参数，不承诺单纯升配即可自动成为生产系统。

## 完成记录

每一项完成后追加对应 `ops/checks/` 证据及遗留项。当前首先实施 PostgreSQL 备份与独立恢复；其余项目保持待办，避免把本计划当成已部署事实。

2026-09-20 第一批已完成：pgBackRest 加密全备/差异备份、持续 WAL 归档、S3 独立指定点恢复；A1/S2 加密 etcd/六节点配置备份、独立 etcd 启动验收；对应定时器均启用。证据见 `ops/checks/2026-09-20-backup-recovery.md`。外部灾备、备份监控和完整集群重建仍待完成，不能把第一项全部风险标为已消除。下一项推进 PG 稳定入口与故障切换设计和实施，业务功能开发继续暂停。


2026-09-20 第二批已完成：现有 PG 由 Patroni 接管，独立三成员 etcd、严格一同步副本、软件 watchdog、双副本固定写入口和三节点 PgBouncer 已部署。现有 Ingestor/Temporal 已改接固定地址；计划切主、DCS 网络隔离后的自动切主/旧主只读、回归主备、已确认记录完整性、应用恢复、换主后归档/备份与跨时间线恢复均有实测。见 `ops/checks/2026-09-20-postgresql-patroni.md`。软件看门狗整机冻结测试、生产 SLO、PG 传输加密、监控与异地灾备仍不能标为完成。

下一项按第 3 项推进 Kubernetes API 与 Argo CD 可用性。第 4 项还需明确包含 Kafka Connect/Debezium 的 logical failover slot 同步和 offset/LSN 恢复演练（24.4 的硬要求）；目前 wal_level=replica，本轮物理 PG HA 不能替代 CDC HA。


2026-09-20 第三批进展：A1/A2/A3 节点本地 API 入口、kubelet/kube-proxy/发现配置迁移、CoreDNS 强制跨节点已完成；A1 API 停止期间真实 Git 发布与三节点网络验收通过。Argo server/repo/cache 已按官方稳定 HA 配置分散，Redis 正常替换与发布控制器 A2→A3 受控恢复通过，详见 `ops/checks/2026-09-20-kubernetes-argocd-ha.md`。**第 3 项不能标为完整无人值守 HA**：application-controller 仍单活，整机失联需先确认隔离再接管，官方动态接管在当前版本为 Alpha，未启用；SSO/ApplicationSet/Notifications 未作多副本。此边界保留待办，不阻止继续第 4 项外围工作，但生产综合验收前必须明确所需自动恢复等级。

2026-09-20 第四批第一部分：Kafka 当前 leader 所在 S2 的服务停止/恢复通过，S1 自动接任；121 条探针消息在线消费及恢复后完整回放通过，副本不足写入保护通过专用严格队列验证。三机 systemd 正常停止配置已修复，结果入库服务回归通过、三个分区 lag=0。见 `ops/checks/2026-09-20-kafka-broker-failover.md`。保留边界及监控要求已记录，定时告警、安全认证和灾备仍待完成；下一项为 SeaweedFS 可靠性，第 4 项整体仍未完成。

2026-09-20 第四批第二部分：SeaweedFS 维护基线备份、解密及独立 Master/Volume/Filer 恢复完成，原日志大小/SHA256 匹配；7 个原卷由单副本改为跨机双副本，停止 S1 Volume 时旧文件可读、新文件仍落两台，恢复后校验通过。解除本机 Master 的连带停服依赖。详见 `ops/checks/2026-09-20-seaweed-reliability.md`。**Filer 元数据与 S3 入口仍单点**，尚未迁移 PG；跨节点 TCP 18888 实测不通，需轻量云规则后继续多 Filer/入口与元数据配套恢复。下一步方案已记录在 `ops/seaweedfs/README.md`，不新增数据库服务器。

2026-09-21 第四批第三部分：用户放通 TCP 18888 后，三 S 节点全互通已验证。SeaweedFS 目录由 S3 本地 LevelDB2 迁入现有 `crawler.object_metadata`，原 17 条目和 7 文件校验通过；三 Filer/带认证的 S3 网关及跨 A 节点双入口已上线，由新仓库 Argo CD 管理。真实 Service DNS 客户端的单 Filer/Gateway 停服、入口 Pod 替换、PG 计划切主后上传/读取/列表均通过（切主观察到一次暂态失败，4.6 秒完成成功往返，不承诺无错误切换）。新配套加密维护备份已在隔离 PG/Master/Volume/Filer 中恢复，52 条目录元数据指纹一致、19 个非内部日志文件 SHA256 一致。见 `ops/checks/2026-09-21-seaweed-ha.md`。

**本轮不是外围全部完成**：SeaweedFS 持续对象备份、生产在线恢复、内部接口认证/TLS与告警仍待；此次备份为小规模短停维护备份。下一步先评估并落实 ClickHouse 的备份恢复与单机可用性边界，再继续 CDC、集中监控告警和安全收尾；业务代码仍暂停。Argo controller 整机失联无人值守接管边界继续保留。
