# 24.11 存量视频指标采集映射与提交校验

日期：2026-09-20。状态：采集层字段映射、原始响应取证、Submission 组装和本地验证完成。全量 `npm test` 50 项通过；尚未接真实 YouTube、Worker Journal 或实际 Kafka 发布。

## 1. 本次范围与职责

针对已经被任务选中的存量视频，更新播放量、点赞数和评论数量。内容类型使用任务冻结的 video/short/live，不在指标阶段擅自修正。评论正文不采集、不生成 page 字段；不调用 getComments、评论 continuation 或评论分页。

实现位于 `services/collection/src/youtube-metrics.ts`，属于采集业务层。继续使用 Crawlee 原版会话能力和现有 IP 扩展；本次字段解析不进入代理模块。

```text
可信任务：内部频道 ID、YouTube 频道 ID、视频 ID、冻结内容类型
                   │
                   ▼
YouTube.js getInfo(videoId)
   ├─ /youtubei/v1/player → 视频/频道身份、原始播放量
   └─ /youtubei/v1/next   → 当前视频身份、点赞按钮、评论入口数量
                   │ fetch 中保留原始 JSON 数字证据
                   ▼
身份核对、字段映射、缺失/冲突判定
   ├─ 不完整 → 返回 unresolved 与问题列表，不组装完整提交
   └─ 完整   → 按冻结批次组装 Submission，计算稳定内容 Hash
                   │
                   ▼
后续 Worker Journal → Kafka Results → Data Ingestor → PG
```

`getBasicInfo()` 只覆盖 player，不能据此宣称已取得点赞和评论数量。因此指标切片使用 `getInfo()` 的 player + next 两个请求。每次尝试只允许这两个端点，必须携带相同的 videoId，禁止 continuation；失败重试仍由采集层显式配置。

## 2. 字段来源

| 字段 | 本次认可的来源 | 输出 |
|---|---|---|
| 内部 channelId | 任务提供的内部频道身份 | 绑定 Submission 的 channel_id |
| YouTube 频道身份 | player.videoDetails.channelId | 必须等于任务提供的 sourceChannelId；不假定它等于内部 ID |
| 视频 ID | 请求 videoId、player.videoDetails.videoId、next.currentVideoEndpoint.watchEndpoint.videoId | 三者必须与冻结目标一致 |
| contentType | 任务冻结值 | 不由时长或当前页面猜测 |
| observedAt | 当前尝试开始时的 UTC 毫秒时间 | 两个请求使用共同的保守观察时间，失败尝试的证据不复用 |
| view | player.videoDetails.viewCount 的原始值 | 非负十进制字符串，最大为 PG bigint 上限 |
| like | 当前视频主结果区域的点赞按钮文字/精确无障碍标签 | 支持旧 ToggleButton/SegmentedLikeDislikeButton 和现代 LikeButtonViewModel 的明确路径 |
| comment | 当前视频评论区域的 commentsEntryPointHeaderRenderer.commentCount | 只解析精确计数，不把聊天人数、推荐视频或单条评论获赞当作总数 |
| comments_disabled | 当前视频评论区域的明确英文关闭提示 | status=disabled、value="0"、disabled=true；缺少表面不推断关闭 |

本切片显式请求英文区域设置，支持纯数字及合法英文千位分组。`1.2K`、`1万`、`Like`、空值、非法分组或负数不能伪装成精确数。标题缩写和精确无障碍标签同时存在时可以使用后者；多个精确来源冲突时返回未知。

本地验证样本由测试构造，不是线上页面录制。真实 YouTube 新版布局、地区差异或登录限制仍需要真实样本验收；不支持的表面明确报错/标为未知，不递归全页面搜一个数字补上。

## 3. 数字精度

YouTube.js 部分解析类会调用 parseInt 并返回 Number，超过 9007199254740991 时可能已经丢失精度。不能再调用 String(number) 假装恢复原值。

映射读取 fetch 返回的原始 JSON 副本：

- 原本就是字符串的计数直接保留十进制数字。
- 对超出安全整数范围的 JSON 数值，使用 Node 24 JSON.parse reviver 的 context.source 取回原始 token，之后以 BigInt 校验范围。
- 若输入已经成为不安全 Number，缺少原始 token，拒绝当作精确计数。
- PG bigint 上限 9223372036854775807 可以保留，超出则标记 out_of_range，不截断、不溢出。
- 不支持的科学计数/小数/缩写形式不自动换算成精确指标。

YouTube.js 继续完成原生请求与解析；事实指标使用单独保留的原始响应证据，因此不必修改 YouTube.js 或 Crawlee 源码。

## 4. 缺失值、零值及与旧规则的差异

| 情况 | 当前指标切片行为 |
|---|---|
| 明确返回计数 0 | value="0"、status=exact |
| 未返回、空字符串、只显示 Like | value=null、status=unresolved |
| 只有 1.2K 等缩写 | unresolved，不写 exact=1200 |
| 评论区明确提示关闭 | value="0"、status=disabled、disabled=true |
| 评论入口缺失 | unresolved、disabled=null，不改成关闭或零 |
| 评论关闭提示与计数同时出现 | 证据冲突，unresolved |
| 登录限制/不支持的播放状态 | 报错，当前切片不伪造完整指标 |

**需要明确的 24.4/旧实现差异：**

1. 旧 youtubeJsLikeCount 在 Like/Like 表面下有 zero_from_empty 规则。本次不把未公开点赞数当成实际 0，保留未知。
2. 24.4 S-20 的直播/预告政策零值、A-14 的官方 API 估算及其他 fallback 需要额外证据。本切片没有这些证据链，暂不实现、不默认套用；也没有删除现有数据库对这些状态的支持。
3. 24.4 S-19 的明确关闭归零和 S-24 的未知不覆盖继续保留；本次仅实现可验证的明确表面。

这些限制意味着某些视频不能仅靠本切片得到全部精确指标。后续应补充有证据的来源/策略或 API fallback，不能为了让批次成功而补 0。

## 5. 输出与提交校验

`readYoutubeVideoMetrics(runtime, target, options)` 返回：

```typescript
{
  observation: { channelId, sourceContentId, contentType, observedAt, view, like, comment },
  complete: boolean,
  issues: [{ field, reason }]
}
```

`mapYoutubeMetrics()` 是同样规则的纯映射入口，供样本验证使用。缺失指标按当前 Store 契约表达为 null/unresolved，保留旧值的语义不变。

`buildMetricsSubmission(work, results, submissionId)` 执行：

1. 校验任务 identity、frozen_input、scope、execution_epoch 和目标 Hash。
2. 要求 1～100 个目标齐全，拒绝重复、替换视频、混入其他频道或内容类型变化。
3. 本切片要求三个指标均已明确（或评论明确 disabled），再组装完整批次；不把不完整采集标为已完成。实际字段仍重新校验，不信任调用方单独设置 complete=true。
4. 禁止 page 字段进入存量指标提交，即使它是 null；不触碰评论正文。
5. 对 observation 排序、复制，计算现有 canonical JSON SHA-256，检查 1 MiB 限制。
6. 使用 Worker 提供的稳定 submissionId，不自行在每次重试生成新 ID。

组装函数是纯数据处理，不创建 Plan、不续租、不访问数据库、不签名或发布。真正授权仍由现有 Data Ingestor/PG 检查。COLLECTION/YOUTUBE 内部错误还不是新增加的对外协议原因码；Worker 接入时需统一映射。

## 6. 验证结果

新增 `test/youtube-metrics.test.mjs` 共 10 项，扩展既有 CONNECT/TLS 测试，以真实 YouTube.js getInfo 调用两个本地模拟端点：

- 原始字符串及原始 JSON 数值 9007199254740993 均能精确保留。
- 内部/外部频道身份分离，错视频、错频道、错 next 目标和无效时间被拒绝。
- 缺失、负数、缩写、超出 bigint 的值不补零；明确零和上限值正常映射。
- 关闭评论、表面缺失和冲突正确区分。
- 不读取推荐视频/单条评论的计数。
- 完整 Submission 通过现有 JSON Schema、prepareMetricsApply 的无连接预校验以及 Ed25519 Kafka 编解码。
- 缺失目标、重复目标、冻结 Hash 不符、不完整指标和正文 page 被拒绝。
- 指标采集只发 player、next 两个请求，不携带 continuation、不获取评论页。

最终 `npm test`：50 项全部通过，无跳过。没有运行真实代理、真实 YouTube、Kafka 发送或 PG 写入；没有修改数据库 migration、Topic 或集群部署。

## 7. 下一步

实现 Worker 本地待确认记录（Journal），在发布前保存冻结 Submission 与 Hash，重启后继续发布同一结果，再接现有 Kafka Producer 和 APPLIED 查询。真实代理、任务授权和少量真实视频准备好后，才能验证完整真实链路。
