# 24.10 Crawlee 公共采集模块与代理生命周期实施方案

日期：2026-09-20。状态：架构方向已由用户确认；第一版公共模块及职责修订已完成，相关验证 13 项、全量 npm test 共 40 项通过。真实代理、真实 YouTube 验收及常驻 Worker 接入尚未完成。

## 1. 本次决策及与 24.4 的差异

采用 **Crawlee 公共采集模块 + 自定义代理生命周期扩展 + YouTube.js 请求适配**。

**用户补充明确的职责：Crawlee 原生源码不改；Session、CookieJar、会话有效期使用原生能力；扩展只处理 IP；有限重试由采集层控制。** 请求适配代码只负责将原生会话、选定代理与 YouTube.js 的 fetch 接起来，不是重写 Crawlee。底层不默认重试，避免与采集层和 Temporal 多重叠加。

- 不部署 Rota 或 Resin，不增加独立代理管理服务、中央转发网关或 Redis。
- 参考 Resin 主动探测、真实流量反馈、熔断与恢复的设计，自己实现符合本系统边界的轻量模块；没有复制 Resin 的 Go 源码。
- 公共模块与采集业务代码分目录，运行时加载到 Worker 进程中。共用仓库、同进程运行不等于把代理逻辑散落到采集业务中。
- 原 24.4 中 Rota 的网关、许可与续租路径不再作为新系统必须实现的接口；其连接限额、停止分配、故障恢复等目的仍需落实到新模块和节点分组管理中。
- Temporal 继续负责任务调度；Kafka Results → Data Ingestor → PG 的结果入口不变。Crawlee 不新增一套与 Temporal 竞争的持久任务队列。
- 历史源码只读参考。新实现唯一来源为当前 newcrawlSystem，旧 GitHub 仓库不修改。

## 2. 链路与职责

```text
Business Console（后续实现）
  导入代理、分组、启停、展示状态
             │
平台配置接口（后续实现）
  将代理组分配到固定节点/进程
             │ 配置快照
             ▼
Worker 进程
┌─────────────────────────────────────────────────┐
│ YouTube 业务采集：采什么、解析什么、判定数据有效性│
│                  │                              │
│ YouTube.js：构造接口请求、解析 YouTube 响应       │
│                  │ 自定义 fetch                 │
│ collector-runtime 公共模块                      │
│   Crawlee SessionPool / Session / ProxyConfiguration│
│   自定义 ProxyPool：健康、冷却、占用与配置失效    │
│   Undici：按会话隔离并复用代理连接               │
└──────────────────┬──────────────────────────────┘
                   │
                上游代理 → YouTube

完成且校验的采集结果
  → 后续 Worker Journal / 已冻结 Submission
  → 现有 Kafka Results → Data Ingestor → PG
```

不通过 Crawlee CheerioCrawler 发起 YouTube 请求。这里只复用 `@crawlee/core` 的会话和代理配置能力，再显式适配到 YouTube.js 的 `fetch`。安装 SessionPool 本身不会自动得到完整的请求重试或 Cookie 注入。

## 3. 第一版已实现的接口

代码边界：

| 文件 | 职责 |
|---|---|
| `services/collector-runtime/src/proxy-pool.ts` | IP 扩展：配置、可用池、占用、失效与冷却状态；不导入 Crawlee/YouTube.js，不执行采集或重试 |
| `services/collector-runtime/src/runtime.ts` | 请求适配：调用 Crawlee 原生会话能力，接入 HTTP 客户端和探活连接；单次执行，无业务重试 |
| `services/collection/src/read-step.ts` | 采集层：显式重试预算、总时限、HTTP 状态判定，向 IP 扩展反馈冷却 |
| `services/collection/src/youtube.ts` | YouTube.js 请求注入、视频身份核对 |

| 接口 | 作用 |
|---|---|
| `ProxyPool.configure(revision, definitions)` | 原子替换完整配置；拒绝过期版本、错误 holder、重复 ID/同地址、非法协议 |
| `CollectorRuntime.create(pool, options)` | 创建进程内公共模块，不启动服务、不主动访问网络 |
| `probeOnce({url, validate})` | 通过代理进行有界探活，并校验返回内容 |
| `startProbes(spec, intervalMs)` | 后台周期探活；返回停止函数 |
| `runRead(target, callback, options)` | 仅执行一次，接入原生会话；接收采集层指定的代理排除列表 |
| `runReadStep(runtime, target, callback, options)` | 采集层决定是否重试；默认一次，显式 maxAttempts 才允许重复执行 |
| `withYoutubeRead(runtime, callback)` | 每个尝试建立独立 YouTube.js 实例，并注入受控 fetch |
| `readYoutubeVideo(runtime, videoId)` | 最小 getBasicInfo 集成切片，核对返回视频 ID |
| `ProxyPool.snapshot()` | 提供不含代理密码、地址及 Cookie 的运行状态 |
| `runtime.close()` | 取消请求、停止探活计时器并关闭连接 |

库固定版本：`@crawlee/core@3.18.1`、`youtubei.js@17.2.0`、`undici@8.10.2`，Node 24。

## 4. 可用池与会话

初次导入、代理连接资料变化或 Worker 重启后，一律从待探测开始。第一版不把内存健康状态和 Cookie 持久化；重启不恢复旧采集会话，不猜测它仍可使用。

每次分配检查：所属 holder、启用状态、供应商已知到期时间、健康探测新鲜度、目标站点冷却和并发使用限额。

```text
待探测 ──探测成功──→ 网络健康
                         │
                    连接故障
                         ▼
                 停止分配、等待复测
                         │
                     复测成功
                         └──→ 网络健康

目标站点冷却是独立状态：网络健康 + YouTube 冷却中 = 不分配给 YouTube。
```

- 每次 `runRead` 的尝试拥有独立 Crawlee Session、CookieJar、ProxyAgent 和 YouTube.js 实例。
- 同一次尝试的请求保持代理绑定、复用连接；结束后释放占用并销毁连接。跨任务会话复用暂不实现。
- 用显式命名 Session 加入 SessionPool，避免默认 `getSession()` 在满池时随机复用忙碌会话。
- 是否重试由采集层决定，默认一次；显式配置时最多三次尝试。采集层维护已经用过的代理 ID，选择时交给 IP 扩展排除。
- 同一代理默认最多一个活动采集会话，可显式配置；这仅控制代理资源，不保证供应商提供不同公网出口 IP。
- 新配置停用/移除或健康失效后，已有绑定在下一次请求前也会被拒绝。已发出的网络请求不会因为配置变化被追溯撤回。
- 较早发出的探测成功不能覆盖后来出现的故障或配置变更。
- 所有代理不可用时返回 `PROXY.NO_CAPACITY`，由上层退避，绝不退回全部 IP 乱选或绕过代理直连。

## 5. 什么情况下换代理

下表是采集层目前的策略。IP 扩展仅维护网络健康、冷却和可用候选，不解释 YouTube 正文，也不启动重试；通用请求适配层将 403/429 响应交给采集层。

| 结果 | 当前行为 |
|---|---|
| 连接超时、连接被重置等可识别网络故障 | 暂停分配该代理，退避后复测；可在预算内换其他代理，重新执行只读步骤 |
| 已绑定代理停用/到期/状态失效 | 当前请求前拒绝使用；可在预算内重建会话 |
| HTTP 429 | 记录目标站点冷却，尊重 Retry-After（至少 60 秒、最多 24 小时），本次不立即换 IP 重刷 |
| HTTP 403 | 记录短期目标冷却并报告，不认定整个 IP 已坏，不自动重刷 |
| HTTP 200 但识别到验证码 | 网站适配器识别后调用 `context.cooldownProxy(durationMs)`，再报告业务错误；不由 IP 扩展识别正文 |
| 返回空列表、合法空数据 | 原样返回，不自动换 IP |
| 解析异常、视频身份不符、业务规则失败 | 原样报告，不将其当作代理故障 |
| 其他 HTTP 错误、重定向、超出响应大小限制 | 明确报告，不盲目更换代理 |
| 上层取消/总超时 | 停止请求、释放占用，不把代理标记为失效 |

网络成功不自动清除业务冷却。探活地址由调用方显式配置，必须有内容校验，不能把任意 HTTP 200/验证码页视为健康证据。通用探活通过不等于已经验证 YouTube 可采集。

`runRead` 只调用回调一次。采集层通过 `runReadStep` 显式启用重试时，其回调可能被重新执行，必须只包含可重放的采集步骤。PG 写入、Kafka 发布、Journal 封存放在重试范围之外；不能原样重放任意有副作用的 POST。分页 continuation 是否允许重用由业务层确认，不跨会话盲目续页。

## 6. 多节点和多进程

第一版按稳定的 holderId 绑定代理组，每个 holder 对应一个 Worker 进程。节点上只有一个进程时可使用节点标识；多进程时使用独立的进程组标识，并分配互不重叠的代理子组。

当前库校验导入配置的 holder，但没有实现跨机器仲裁。平台后续必须保证配置唯一归属；旧 holder 确认停止/撤销后才能转移代理组，不因心跳暂时丢失就直接重复分配。

不能把同一配置文件用于多个不同进程，并宣称已有分布式独占。不同代理地址也可能共用同一出口，严格按公网出口排他不在首期承诺内。

## 7. 有界资源与配置边界

默认最大 8 个活动只读操作；每个尝试最多 2 个并发请求、100 个请求。容量耗尽直接返回明确错误，上层负责等待，库内没有无限等待队列。

默认单请求 15 秒、单次执行 60 秒；采集层另设跨所有尝试的总时限，默认也是 60 秒。限制覆盖响应体读取。请求体最多 1 MiB，解压后的响应最多 4 MiB，探活响应最多 64 KiB；不适合视频下载、大媒体流。

后台探活全局并发 2，同一 runtime 的重叠探测合并，单个代理不会被重复探测。首次网络故障就暂停分配，复测退避从 30 秒递增，最多 5 分钟；健康结果默认 5 分钟过期。

首期只支持标准 HTTP/HTTPS 上游代理；不支持 VLESS/VMess/SS/SOCKS 等协议。代理认证只发送给代理；Cookie 按会话和目标域名隔离。默认限制 YouTube 目标域名，禁止自动跟随重定向，不关闭 TLS 校验。

首期仅匿名采集：不支持 YouTube 登录 Cookie/OAuth，不接收外部 Authorization/Cookie 注入。需要认证会话时，必须统一设计 YouTube.js 签名与 Cookie 所有权。

## 8. 验证方式与当前边界

`npm test` 包含 `test/collector-runtime.test.mjs`。测试只启动 loopback HTTP、HTTPS 和 CONNECT 代理；YouTube 域名的 CONNECT 被测试代理定向到本机 HTTPS fixture，证书仅在测试进程中信任并恢复。没有外部代理，没有访问真实 YouTube。

测试覆盖：待探测不分配、配置原子性、真实 CONNECT 故障切换、空数据不重试、并发 Cookie 隔离、429 冷却不被探活清除、过时探测防护、停用绑定、响应大小/重定向限制、取消释放、全部代理失效、响应体超时，以及真实 YouTube.js getBasicInfo 经注入 fetch 请求和解析本地响应。

职责修订新增两项：请求适配层失败只执行一次，采集层需显式允许重试；底层收到 429 原样返回且不自动冷却，采集层作出判断后才更新 IP 状态。

这只能证明本地组件闭环，不能证明真实出口健康、YouTube 成功率、跨服务器独占、生产吞吐或完整 Worker→PG 链路。

依赖审计：安装时 npm audit 报告 Crawlee 引入的 `stream-json` 及其上游关联共 2 条 moderate 条目，归因于同一嵌套 JSON 过滤 DoS 公告（GHSA-528h-pc64-c93x），当时无可用自动修复。当前模块不使用 RequestList 的流式 JSON 过滤路径；不为消除报告强行跨主版本覆盖传递依赖，后续升级需复核。

## 9. 后续按顺序完成

1. 用户提供真实标准代理后，在隔离验证环境运行显式探活和少量指定视频的只读采集，不先全量采集。
2. 业务指标映射的首个切片已完成，详见 24.11：保留原始十进制计数字符串，核对冻结目标、字段完整性和观测时间；现有 Apply 契约和 Kafka 编解码通过本地验证。真实来源布局、完整 fallback 尚待后续验收。
3. 接 Worker Journal、签名 Submission 和现有 Kafka Producer。提交身份冻结后，发布重试复用同一结果，不重新采集替换。
4. 继续已有 Temporal validation 的搭建，将这一采集步骤作为 Activity 接入；重试总预算由任务与请求两层共同限制。
5. 增加 Business Console 代理资料、配置版本、分组接口和状态汇总，之后再做节点扩容与失败恢复验收。

除第 2 项首个映射切片外，以上后续项尚未完成，不能因公共模块和映射验证通过就认为真实采集 Worker 已上线。
