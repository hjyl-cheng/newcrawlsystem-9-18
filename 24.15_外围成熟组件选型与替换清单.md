# 外围成熟组件选型与替换清单

日期：2026-09-21。状态：推荐选型，尚未执行迁移。

用户要求：先选成熟组件，外围通用功能不再自研。此要求优先于之前边搭建边补脚本的实施方式。24.14 保留审查证据，本文件给出推荐组合和明确的职责移交方向。

用户后续确认：对象存储部署在现有服务器上，继续采用 SeaweedFS，不采用 COS 等托管云对象存储。Kubernetes 继续保留；旧版本停止维护属于版本升级事项，不代表 Kubernetes 项目停止维护，也不构成替换该组件的理由。

## 1. 推荐组合

| 外围职责 | 选定的推荐组件 | 实施方式与职责边界 |
|---|---|---|
| 容器调度、服务发现、自愈 | Kubernetes / containerd | 保留；用 kubeadm 官方升级流程维护，不自研调度器或节点接管控制器 |
| Pod 网络隔离、K8s 管理权限 | Calico NetworkPolicy / Kubernetes RBAC | 保留现有 CNI；数据服务使用原生 TLS，不把私网当成加密，也不把网络策略当成数据库资源授权 |
| 应用发布与配置同步 | Argo CD + Helm / Kustomize | 保留；优先官方或成熟社区 Chart，仓库维护 values 和覆盖配置 |
| 主机安装、配置与受控维护 | Ansible | 用成熟模块下发文件、管理 systemd、串行维护；不再维护重复的 SSH/Python 部署框架 |
| PostgreSQL 运维 | **Pigsty 的 PG 运维方案**，底层 Patroni + etcd + HAProxy + PgBouncer | S 节点继续运行主机 PG；不部署第二套选主器。先做现有配置与受支持模块的接管差异核对 |
| PostgreSQL 备份 | pgBackRest | 保留物理备份、WAL 归档、PITR；不要用通用文件备份代替 |
| Kafka 生命周期、安全、Topic/User 管理 | **Strimzi + Apache Kafka** | 目标由成熟 Operator 管理 Kafka、证书、ACL 和滚动维护；需要迁移到 K8s 管理的 Kafka 实例，不是把现有 systemd 进程直接登记进去 |
| 数据库变更读取与消息传输 | Kafka Connect + Debezium PostgreSQL Connector / Outbox Event Router | 保留；连接器、心跳、offset 由成熟组件负责。CDC 的全部故障恢复要求仍有第 4 节的未决项 |
| 持久任务编排 | Temporal 官方服务与 Helm Chart | 保留 PG 后端；Chart 配置数据库、TLS、资源和副本，Workflow 业务代码后续单独开发 |
| 对象存储 | SeaweedFS 原生 Master / Volume / Filer / S3 | 用户明确选择现有服务器自建，不采用 COS 等托管云对象存储；保留原生复制能力，按官方配置管理，不自研对象存储或复制调度器 |
| 分析存储 | ClickHouse 原生存储、TTL、BACKUP/RESTORE | 保留；需 HA 时使用原生 ReplicatedMergeTree + Keeper。当前单机状态不会因选型自动变成 HA |
| K8s 证书生命周期 | cert-manager | 管理明确交给它的证书和 Secret；应用需要按其支持方式重新加载。Kafka 内部证书由 Strimzi 管，不由两套控制器竞争 |
| 主机服务证书生命周期 | step-ca + step CLI 官方续期机制 | 管理主机证书的签发/续期，配合标准 systemd 单元与服务原生 reload；CA 可用性、备份和现有根信任迁移须验证 |
| 凭据保存 | Kubernetes Secret + Ansible Vault | K8s 和主机分别使用标准机制；不开发自己的 Secret 服务，不在 Git 保存明文秘密 |
| 监控、告警、仪表盘 | Prometheus Operator / kube-prometheus-stack | 接替手写的监控部署生成器；继续使用 Prometheus、Alertmanager、Grafana，一套配置源，不另建并行监控平台 |
| 主机、PG、Kafka、HTTP 指标 | node_exporter 原生 systemd collector、PG exporter、Kafka/JMX exporter、Blackbox Exporter / JSON Exporter | 以配置和官方指标替换通用 Python 探针；HTTP 可访问和 Connect 任务 RUNNING 分别检查 |
| 日志 | Vector + 现有 ClickHouse + Grafana | 保留成熟采集/查询组件；清洗、重试、缓冲、TTL 用原生配置。自写容量清理退出前须有实际磁盘隔离/配额与保留方案，不能只删脚本 |
| 配置文件及已生成快照的备份 | restic + 原生快照命令 + systemd timer | restic 负责加密、保留、校验和恢复；etcdctl 等先生成一致快照。保留少量明确的命令编排，不自研备份仓库 |
| 对象持续备份 | SeaweedFS 原生 filer.backup + restic 历史保留 | 目标采用原生导出/同步和成熟版本备份工具；接入前验证覆盖、删除、元数据/对象一致恢复及独立目标。复制本身不是历史备份 |
| 弹性伸缩 | Kubernetes HPA + KEDA | 沿用架构选型；根据真实消费/任务指标配置，外围阶段不编写业务调度 |
| Web HTTPS 入口 | Traefik + cert-manager | 在有实际域名和入口需求时配置；不为了内部服务增加自研网关 |

表中“选定”表示产品方向，不表示现在就安装全部组件。保留既有可用能力，逐项接管，避免同一职责由新旧两套控制器同时负责。也不新增数据库分片、SQLite 或服务器。

## 2. 六台机器的目标部署关系

推荐保留 PG 的主机部署方式，并让 Kafka 使用成熟 Operator。由此产生一个明确的拓扑变化：**S1/S2/S3 需要作为专用数据 Worker 加入现有 K8s 集群，以承载 Strimzi 管理的 Kafka Pod。** 这是待审阅的目标，不是本轮已经执行或获得迁移验收。

```text
A1 / A2 / A3
  Kubernetes 控制面 + 应用 Pod
  Argo CD / Temporal / Connect / 监控
  Strimzi / cert-manager 等管理组件
               │
               │ Kubernetes 管理与调度
               ▼
S1 / S2 / S3（目标：专用 K8s 数据 Worker）
  Kafka Pod：由 Strimzi 管理，三机分散
  PostgreSQL：仍是主机服务，由 Pigsty/Patroni 管理
  SeaweedFS：保留现有主机服务，配置受控管理
  ClickHouse：先保留 S3 现有实例，HA 单独实施
```

S 节点加入 K8s 不等于它们也变成控制面，不在上面再建三份 Kubernetes etcd。用节点标签/污点、资源预算和卷绑定隔离数据工作负载。未来采集 W 节点仍独立扩展。

该目标需要核对 S 节点内存、磁盘、端口、CNI 路由、现有主机服务及迁移期间资源余量。不能在现有数据目录上直接启动另一套 Kafka；Kafka cluster ID、KRaft 元数据、topic、消费者 offset、Connect 内部 topic 和 Debezium 位点须有迁移与回滚方案。

如果决定 S 节点完全不加入 K8s，这条 Strimzi 路线就不成立，应重新讨论主机 Kafka 的完整成熟运维方案；不能暗中把它替换回自写常驻控制器。

## 3. 本次明确不采用的捷径

- **不用 Pigsty Kafka Beta 模块充当成熟生产方案。** 官网明确标注 Beta，并列出版本升级、既有集群安全迁移等边界。Pigsty 的 PG 方案可采用，不等于其每个新模块都已同样成熟。
- 不为减少一个脚本就立即把 PG 迁入 CloudNativePG。其正式版确有槽管理，但本次源码审查仍未证明可等价解决备库离线导致 CDC 等待的问题；还会迁移整套数据库部署与备份方式。
- 不直接上线 Patroni master。上游 PR 已合并不等于当前正式版具备，使用开发快照不能满足“成熟正式组件”的要求。
- 不同时让 PG 原生槽同步、pg_failover_slots 扩展和自写 guard 竞争同一职责。
- 不把“Ansible 部署了 ha-guard.py”称为“guard 已被成熟组件替代”。Ansible 只接替部署工作，程序的正确性责任仍是我们自己的。
- 不为当前明确链路额外引入完整 Service Mesh 或另一套自研认证平台。

## 4. 无法直接宣布完成替代的边界

### CDC 自动恢复

目标仍采用 PG/Patroni + Debezium 原生能力。当前最新正式 Patroni 4.1.5 与三机安装制品不含已在 master 合并的动态 `synchronized_standby_slots` 管理；现有 guard 还承担接管名单、旧槽恢复等约束。

**本轮没有找到并验证一个可以直接删除整个 guard、同时完整保留此前所有故障要求的正式版本组合。** 这项不能伪装成“选型已解决”。当前处理是停止扩展 guard、保留必要保护，继续针对正式制品和完整部署方案验证替代；不用开发分支凑数。

若要立即把这段自研完全退出，需要讨论是否接受有限的 CDC 等待/人工恢复，或改变部署方案。没有明确新决定前，保持原可用性要求，不直接停用 hook、不清理活动槽。

### Argo 发布控制器整机失联

继续使用 Argo CD 官方方案。当前版本的单活控制器整机接管边界尚未验收，不自写“超时就强删 Pod”的切换程序。应核对正式版本的接管能力和节点隔离方式，保留原待办，不把普通 Pod 重启当成整机无人值守接管。

### 容量和灾备

对象备份的独立目标、ClickHouse 独立数据盘/HA、外部备份存储仍是资源与恢复方案问题。成熟组件能够提供功能，但不能凭安装消除同地域、同账号、共用系统盘的故障边界。

## 5. 正式版本与兼容性

用户最新要求：外围组件以官方最新稳定版为选型目标。2026-09-21 已逐项核对官方发布页、Chart 索引和兼容矩阵，详见 [24.16 官方版本核对与草案评审](24.16_外围组件官方稳定版本核对与搭建草案评审.md)。用户已确定以 **Kubernetes 1.37.0** 为最终目标，先验证兼容性再升级，并授权后续由助手继续处理。多项最新外围组件尚未将其列入官方支持/测试矩阵，不能把版本清单当作验收。不静默降级选型，不使用开发版或修改上游源码补齐缺口。现场盘点与分步执行清单见 [24.17](24.17_外围升级接管执行清单与现场基线.md)。

本轮实际核对：

- 集群为 Kubernetes **1.31.14**；Kubernetes 项目持续维护，但官方生命周期表已将 **1.31 分支**列为停止维护（EOL 2025-11-11）。继续使用 Kubernetes，后续 Operator 迁移前先制定 kubeadm 逐小版本升级计划；目标选择实施时仍受支持且与 Calico、容器运行时及各 Operator 兼容的版本，不把过期版本作为生产定案，也不跨多级直接升级。本次仅明确升级方向，未执行升级。
- Strimzi **1.2.0** 正式版支持 Kafka **4.2.0/4.2.1/4.3.0/4.3.1**；现有 Kafka **3.9.1** 不在该组合内。官网存在支持 3.9.1 的历史版本，但不能仅为了兼容就默认选择旧版作长期目标。升级/迁移路线须单独验证后固定版本与 digest。
- Pigsty **v4.5.0** 是本轮核对的正式版；仍须核对其 PG 模块能否接管现有配置，不能直接运行初始化 playbook 覆盖在线服务。
- Patroni **v4.1.5**、CloudNativePG **v1.30.0** 的能力边界见 24.14。
- Ansible Core **2.21.4** 已仅安装到本机隔离工具环境；没有通过它执行主机变更。
- 其他组件升级时逐项固定正式维护版、Chart 版本、镜像摘要及兼容矩阵。本文件不是“全部安装 latest”的指令。

## 6. 已经开发的内容：准确盘点

| 已有路径/内容 | 实际性质 | 当前状态与处理 |
|---|---|---|
| `services/runtime-smoke`、infra-smoke | 应用/网络验证骨架 | 已部署；用于验证，不是完整业务 |
| `contracts/data-plane` | 提交、回执和指标批次协议草案 | 已有 Schema、样例和测试，业务规则仍需维护 |
| `database/migrations/0001`～`0004` | 事实表、最小控制/提交/回执、Kafka 处理结果 | 应用于独立测试库和常驻 Ingestor 验证库；正式 crawler 业务表未整体上线 |
| `services/facts-store` | 指标与评论合并、观察顺序和事务 | 自研业务代码，属于必须表达的领域规则 |
| `services/ingestion` | 最小计划、执行授权、代次、幂等、回执 | 自研业务代码；有这些内部函数不等于 Planner 已开发完成 |
| `services/kafka-results` | 官方 Kafka 客户端适配、签名、入库后确认进度、坏消息隔离 | 已供常驻验证链路使用；不是自研 Kafka 消息队列 |
| `services/data-ingestor` | 结果消费和业务入库服务 | 已部署两副本，只处理受控验证链路；不能用通用 Kafka Connector 自动替代全部领域校验 |
| `services/collector-runtime` | Crawlee 会话 + Undici 代理请求 + 自写 IP 状态扩展 | 本地模块与模拟代理测试，未部署真实采集 Worker；Crawlee 源码未修改 |
| `services/collection` | YouTube.js 接入、存量视频指标映射、有限重试 | 本地实现与测试，尚未接成真实持续采集链路 |
| `ops/cdc/ha-guard.py` | 常驻外围自研控制程序 | 已在三 S 运行；属于自研，完整替代待决 |
| `ops/monitoring/collect-local.py`、`infra-exporter.py` | 自写指标/状态采集 | 长期运行；通用部分由标准 exporter 接替 |
| `ops/backup/backup-control.py`、ClickHouse 搬运、日志容量清理 | 定时外围自写运维逻辑 | 由成熟备份/存储能力逐项接替，保留保护直到等价验收 |
| 其余 prepare/bootstrap/verify 和 checks | 部署迁移、验证工具与证据 | 不全是常驻服务；历史证据保留，重复部署代码停止扩展 |

尚未完成：完整 Planner/Query Engine、三 Clock 持续调度、真实 Worker、业务管理 UI、中央代理分组管理、生产 Journal、正式 Publication/Business Consumer 和分析投递等。没有引入 SQLite。

此前 README 中“尚未部署常驻消费者”等描述已经过期，按本清单及最新现场结果修正。不能说“外围没有做开发”，也不能把已有模块等同完整业务系统。

## 7. 现有自研代码退出顺序

1. 先确认本文件的推荐组件与部署关系；不继续扩大自研控制逻辑。
2. 为版本升级、Pigsty 接管、Strimzi 迁移分别制定可回滚方案，明确配置所有权。
3. 优先替换低风险通用项：标准 exporter、监控 Chart、文件备份、证书维护。
4. 旧逻辑必须在替代方案正常/故障/恢复验收后停用，避免“双控制器竞争”或监控盲区。
5. CDC 与 Argo 的特殊故障边界单独讨论和验收；不得顺手降低既定要求。

我们主要维护配置、版本、权限清单、业务接入和验收用例。组件内部的复制、选主、重试、证书续期等由成熟项目承担。少量命令编排可以存在，但不再发展成本项目自己的基础设施产品。

## 8. 本轮工作区状态

此次仅选型和更新文档，未执行 Ansible playbook、Kafka/PG 迁移或证书切换。前一轮已在本地准备的 guard `verify-full` 参数修正与 Ansible 清单尚未上线；这不等于 guard 已被替换。

前一轮为了安装隔离 Ansible 工具环境，已在 A1 安装 Python venv 支持及其 Ubuntu 依赖更新；相关业务服务未重启。此次已确认安装进程结束，没有遗留后台部署任务。

## 9. 官方依据

- [Pigsty PostgreSQL 方案](https://github.com/pgsty/pigsty)、[Pigsty Kafka 的 Beta 标记及边界](https://pigsty.io/docs/kafka/)。
- [Strimzi 正式版与 Kafka/K8s 兼容矩阵](https://strimzi.io/downloads/)、[Strimzi 功能概览](https://strimzi.io/docs/operators/latest/overview.html)。
- [Kubernetes 版本生命周期](https://kubernetes.io/releases/patch-releases/#non-active-branch-history)。
- [Prometheus Operator](https://github.com/prometheus-operator/prometheus-operator)、[kube-prometheus-stack](https://github.com/prometheus-community/helm-charts/tree/main/charts/kube-prometheus-stack)、[JSON Exporter](https://github.com/prometheus-community/json_exporter)。
- [cert-manager 证书生命周期](https://cert-manager.io/docs/usage/certificate/)、[step 官方续期](https://smallstep.com/docs/step-ca/renewal/)。
- [Temporal 官方 Chart](https://github.com/temporalio/helm-charts)、[ClickHouse 原生备份](https://clickhouse.com/docs/operations/backup)、[SeaweedFS 4.47 原生 filer.backup](https://github.com/seaweedfs/seaweedfs/blob/4.47/weed/command/filer_backup.go)、[restic](https://restic.readthedocs.io/en/stable/010_introduction.html)。
- CDC 的固定版本源码、PR 和现场证据统一见 24.14。
