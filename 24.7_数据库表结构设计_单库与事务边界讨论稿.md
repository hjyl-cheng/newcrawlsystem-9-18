# 24.7 数据库表结构设计：单库与事务边界讨论稿

日期：2026-09-20。状态：总体设计稿；首个 channels/contents migration 已在独立验证库通过，正式 crawler 库尚未应用。实施范围见第 17～18 节，内容 Store 及最小受控提交切片已通过验证（另见第 19 节）。

采集事实的逐字段核对已补充到 `24.8_采集事实表逐字段映射与约束核对.md`，覆盖五张旧表的 173 列。本稿字段组是概要，具体旧字段保留/归属/命名以 24.8 明细为准；新增字段以本稿定义补充。

依据：24.4 架构定案、24.6 第一批通信契约，以及本次讨论确认的数据库和评论保存规则。
本文用于确定数据库归属、目标表、核心字段、关联、约束、事务和实施顺序。它不是可以直接执行的 DDL，也不表示所有业务正文和状态枚举已经完成实现。

## 1. 已确认的设计前提

1. **外部 Business 数据库保持不变。** 不改其表结构、既有数据和接收语义。新系统通过 Publication 与业务端 Consumer/适配层对接；传输调整不得要求业务库改表。
2. **只有一个爬虫业务权威数据库 `crawler`。** 频道、视频、评论、计划、执行权、提交回执、Feature、三 Clock 和发布意图在同一数据库内分 Schema 管理。
3. **评论采用 A 方案。** 第一页评论正文保存在视频表的 JSONB 列，不单独拆评论库或评论表，不默认变成对象存储引用。
4. **评论数量和评论正文分开。** 新视频按原采集规则保存数量及第一页正文；被选中更新的存量视频按有效证据更新数量，已有非空评论页优先保留，缺失/空页按原有规则补齐。
5. 旧代码中存量视频的底层请求仍可能取得评论页；“不替换已保存正文”不等于“不发评论请求”。数据库设计不顺便修改网络采集规则。
6. **旧库只作字段、证据和约束参考。** 不照搬旧 DDL、不复制旧 BullMQ/NATS/Finalize 运行模型。旧数据不是新设计的前置条件；后续若需迁入，单独编写一次性转换、去重和校验脚本。
7. 当前不做数据库分片，不按 Worker、Full/Incremental 或服务器编号拆事实库。
8. **24.4 是待实施检验的架构基线，不是不可修改的结论。** 表名、字段组合和机制由 migration、业务样例与运行证据验证；发现不匹配时明确提出修订，不为了符合文档而保留不合理实现，也不静默改变用户已确认的业务行为。

## 2. 数据库、Schema 和服务器分别是什么

```text
S1 PostgreSQL Primary
  │
  ├─ crawler 数据库：爬虫业务的唯一权威库
  │   ├─ discover       Query、发现来源与查询检查点
  │   ├─ crawler        频道、视频、候选、Observation、对象登记
  │   ├─ control        计划、领域义务、执行权限、可靠启动/唤醒
  │   ├─ ingestion      有界批次、Submission、载荷、回执
  │   ├─ feature_clock  Feature、三 Clock、处理回执与策略
  │   ├─ publication    发布版本、业务 Outbox、交付确认
  │   ├─ analytics      分析事件与投递进度
  │   ├─ read_model     可重建控制台与执行链路投影
  │   └─ platform       迁移版本、业务配置、控制审计
  │
  ├─ temporal            Temporal 自己的持久化表
  └─ temporal_visibility Temporal 自己的工作流检索表
  │
  └── WAL 物理复制 ──► S2 / S3 PostgreSQL Standby

Rota 专用持久化：由 Rota 自己拥有和迁移，是否沿用现有实例在接入时确定
外部 Business DB：保持原样，属于外部业务系统
SeaweedFS：大对象、原始证据、超大结果和受控归档
ClickHouse：历史分析副本
监控/日志系统：CPU、内存、Pod、普通日志与底层 Trace
```

Schema 相当于同一数据库内的表分组。跨 Schema 可以用一个 PostgreSQL 本地事务，不需要跨库双写。
不同数据库需要独立连接，不能把它们当成一个普通本地事务使用。Temporal 的库与 Crawler 之间通过可靠启动/通知意图协调。

Temporal 使用 PostgreSQL 时规划两个专用数据库，由其官方版本化工具管理，不在本项目手写 Temporal 内部表。
这些组件库不意味着业务分片，也不意味着新增服务器。初期共享 PG 进程不产生 CPU/磁盘隔离，必须分别配置账号、连接预算和备份责任；是否物理分离由负载决定。

S2/S3 是相同数据的副本，不分别负责视频库或运维库。当前实装仍是异步复制、人工故障切换和验证入口；本设计不把已有主备部署描述为完成生产 HA。

## 3. 字段、版本与约束的统一约定

| 类型/约定 | 本稿选择 | 说明 |
|---|---|---|
| 自有业务 ID | `text`，非空、最长 256、区分大小写 | 对齐 24.6 不透明 ID；创建方一次生成，重试沿用。外部平台 ID 不改写 |
| 计数 | `bigint`，非负；未知允许 NULL | NULL 与 0 不混。跨语言输出超出安全整数的值使用字符串 |
| generation / epoch / revision | 受整数/范围 CHECK 约束的 `numeric` 域，0～99999999999999999999 | 对齐首批协议允许的 20 位十进制字符串；禁止客户端 Number 转换；递增溢出明确失败 |
| 日常内部小枚举版本 | `integer`，按定义限制 | 例如 Schema 版本，不能混成执行 epoch |
| 时间 | `timestamptz`，连接时区 UTC | 保存真实发生时间与入库时间；日覆盖的 `date` 另存，不能由重试当天重新推导 |
| Hash | `text`，按具体契约 CHECK | 新 Submission 的裸 64 位与旧发布/Agent 的 `sha256:` 前缀分别定义；记录算法/profile/Schema 版本，不统一剥前缀或重算已确认数据；不把 ETag 当 SHA-256 |
| 状态/原因码 | `text` + CHECK / 受控目录 | 原因码遵守 24.4；未知不能自动解释为成功 |
| 有界证据/配置 | `jsonb` | 必须有具体 Schema、大小限制和版本；不能把整个任务系统塞进一个 JSON |
| 必须按原字节重放的载荷 | `bytea` 或受保护对象引用 | JSONB 会规范化表示，不能代替原 wire 字节来核验旧 Hash |
| 乐观版本 | `row_version`，同一修改事务递增 | 与执行 epoch、业务输入 revision 和 Feature 版本含义分开 |
| 当前实体时间 | `created_at`、`updated_at` | updated_at 只表示当前行变更，不替代某字段的 observed_at |

下面表格中的字段组不是一个名为“字段组”的 JSON。明确列出的主键、查询过滤、关联、权限、版本和状态字段采用实体列；只把变长证据和非热点扩展放 JSONB。
必填以业务身份和当前状态为准，条件约束由 CHECK/外键/Store 联合实现；进入 migration 时必须列齐每一列的 nullability、默认值和状态转换。

关联原则：

- 计划使用 `UNIQUE(plan_id, channel_id)`；批次、领域、请求冗余频道时采用匹配的复合外键，阻止“合法 plan_id + 错误 channel_id”。
- 批次同时暴露 `UNIQUE(logical_batch_key, plan_id, channel_id)`；Submission 引用这组身份。
- 回执采用复合关联约束其 Submission、批次、计划和内容 Hash，不只按可伪造的字符串关联。
- 权威事实、回执、Outbox 默认 RESTRICT 删除，不沿用旧库所有 ON DELETE CASCADE。
- 跨库不建外键。Business Category 使用本地受控映射和版本，不能把远端 Business DB 作为在线授权依赖。

## 4. 核心事实：频道、视频、评论与原始证据

### 4.1 `crawler.channels`：每个频道一份当前事实

主键：`channel_id`。频道长期存在，不使用一个永久 `complete=true` 表示所有轮次完成。

| 字段组 | 主要字段 | 用途 |
|---|---|---|
| 稳定身份 | channel_id、channel_url、handle | 平台身份与展示链接；handle 不是稳定主键 |
| 资料 | title、avatar_url、summary、about_description、keywords、external_links、available_tabs | 当前有效资料，数组/链接等使用有界结构 |
| 国家 | country_code、country_source、country_observed_at | 保留判定来源，未知不能猜国家 |
| 数量 | subscriber_count、total_view_count、total_video_count | 非负 bigint，可未知 |
| 数量证据 | 各数量对应 `_status / _source / _observed_at`，必要时 `_text_raw` | 保留估算、精确、缺失等差别 |
| 注册/身份信息 | joined_at、joined_at_precision、joined_date_text、is_verified、is_verified_status | 保留精度和未知状态，不强填布尔值 |
| 联系证据 | youtube_business_email_available、youtube_business_email_observed_at | 保留原业务字段 |
| 生命周期 | lifecycle_status、lifecycle_reason_code、lifecycle_changed_at、lifecycle_evidence_ref | 活跃/休眠/移除事实；不替代本轮 Plan 状态 |
| 版本/观察 | about_observed_at、about_hash、identity_observed_at、identity_hash、row_version | 字段证据优先级与受控更新 |

字段状态不能因为 API 未返回就清空已有可信值。国家、描述、计数、身份等有不同的证据优先级，不能仅凭“新消息时间更晚”全覆盖。

逐字段核对补充：明确保留 country/country_canonical_name、rss_url、vanity_channel_url、is_family_safe，以及 keywords/available_tabs/description/external_links 的状态列。
removed/dormant 原因、来源、证据、复查日、最后探测和 cycle 分列保留，不以 lifecycle_changed_at 代替所有时间；如保留通用生命周期摘要，须同事务派生，不能产生两套独立权威值。
About/身份观察沿用 `about_last_observed_at/about_current_hash/about_identity_last_observed_at/about_identity_current_hash` 名称；上表简写不创建重复同义列。
joined_at 为 date，不受“时间默认 timestamptz”的通用约定覆盖。ready_for_agent 与其最低输入约束迁到具体请求的 input_policy/readiness，不继续作为永久频道写权限。

### 4.2 `crawler.contents`：视频事实，采用 A 方案

主键：`content_key`；外键：channel_id → channels。
新增 `platform`、`resource_kind` 与 `source_content_id` 唯一身份核验，建议 `UNIQUE(platform, resource_kind, source_content_id)`；content_key 稳定映射这组身份。
resource_kind 表示平台身份命名空间，例如 youtube_video / youtube_post；video/short/live 都属于视频身份，不把可修正的 content_type 当作命名空间。
这是新模型的约束建议，实施前用身份样例验证，包括错误频道绑定、已知平台例外和旧身份映射；不宣称旧库已具备该唯一键。
重复提交同一视频但绑定错误频道必须冲突/隔离，不能因 `(channel_id, video_id)` 不同而另插一份。

| 字段组 | 主要字段 | 类型与用途 |
|---|---|---|
| 身份与归属 | content_key、platform、resource_kind、source_content_id、channel_id | text，绑定真实平台实体；不得用 Short/Live 分类变化生成新身份 |
| 展示资料 | title、description、url、thumbnail_url、hashtags、keywords | text / text[]，变更遵守字段证据规则 |
| 内容类型 | content_type、content_type_status、content_type_source | 普通视频/Short/Live 等不能只按时长猜测 |
| 可访问性 | access_status、access_status_source、access_observed_at | 私有、会员、不可用等证据 |
| 发布/直播时间 | published_at、published_at_status、published_at_source、published_at_precision、published_text_raw、live_scheduled_at、live_started_at、live_ended_at | UTC 和明确精度 |
| 时长 | duration_seconds、duration_status、duration_source | 非负 integer，未知不是 0 |
| 指标 | view_count、like_count、comment_count | 非负 bigint，允许 NULL |
| 指标证据 | 每个指标的 `_status / _source / _observed_at`，必要的原始计数字符串 | 单独裁定值与质量，不整行盲目覆盖 |
| 评论开关 | comments_disabled、comments_disabled_source、comments_disabled_observed_at | nullable boolean；明确关闭才按旧规则处理数量零值 |
| **评论正文** | **comments_first_page** | **JSONB，保存原规则认可的第一页** |
| 评论页证据 | comments_page_status、comments_page_source、comments_page_observed_at、comments_page_hash | 指向当前被保留的那份页，不能误标为最新请求时间 |
| 缺失修复 | enrich_state、enrich_reason_code、next_recheck_at | 原有补全/Disposition 语义的受控映射 |
| 来源和版本 | first_seen_at、last_seen_at、player_observed_at、next_observed_at、last_observation_id、extractor_version、row_version | 保留实际观察与最后应用来源 |
| 其他证据 | evidence_json、raw_object_id | 有界证据或对象引用；不默认保存完整巨型上游 JSON |

正文页仍保留 version、sort、comments、returned_count、collected_at、total_count 及 resolution 等经契约确认的字段。
每条评论的身份、正文、作者、点赞和其他 Agent 所需证据按旧提取样例映射，不能为省空间只保留评论 text。
`returned_count` 必须与数组长度一致；它不是视频总评论数。

字段核对后明确补回：length_text、description_status/source、position、is_recent、last_enriched_at、playlist_last_seen_at、player_last_observed_at、next_last_observed_at、player_current_hash、next_current_hash、video_change_probability、publication_item_hash、is_members_only。
player/next 观察列沿用旧明确名称，概要中的 player_observed_at/next_observed_at 不另建同义列。
position 仅为 Current 的兼容位置快照，各轮权威顺序在 domain_items.ordinal；is_recent 保留原计算语义，是选择辅助，不替代冻结时间。
旧 run_id 映射为当前结果的 last_source_plan_id 及完整 DomainItem/Observation 来源关系，不能把旧 run 字符串直接当新 Plan ID。
raw_json 中参与合并/质量/发布判定的有界证据保留在 evidence_json；大型上游材料才进入 raw_objects，不能整列外置后丢掉在线决策输入。

**评论更新规则：**

1. 新增视频保存合法评论数量和第一页正文；明确关闭、合法空页、未知/失败继续分开。
2. 存量视频被选中更新时，按有效观察与采集策略更新 comment_count 及对应证据；不是所有存量视频每轮都更新。
3. 已保存的非空第一页正文优先保留。一次新请求拿到新评论页，不代表允许覆盖已有正文。
4. 原页为空/缺失时，按旧字段合并、补采和明确空页规则决定是否补齐；不能把空页一律当采集失败。
5. 若只更新 comment_count，不改已保存正文的 Hash/观察时间。页里的 total_count 属于那次页观察，不要求永远等于当前 comment_count。
6. 关闭评论的数量规则与历史正文保留是两个独立判断，不能为凑一致而删除历史有效正文。
7. 评论页既然未变化，不把它反复塞进每次普通指标 Delta、Observation 或分析事件。必须携带它的业务发布/Agent 输入仍遵循对应版本契约。

### 4.3 压缩与查询

- comments_first_page 使用 PostgreSQL TOAST 自动压缩/行外存储，不使用手工 gzip 后的 bytea 取代可查询 JSONB。
- 初期沿用默认 pglz；已读到验证节点 PostgreSQL 17 支持 LZ4，实际是否为该列启用 LZ4，比较真实样例的空间、CPU、WAL 后再定，不修改全实例默认值冒充优化。
- 查询列表明确选择列，不 SELECT * 搬运评论；需要详情/Agent 输入时才读取正文。
- 不默认给整列评论 JSONB 建 GIN 索引。索引须对应实际检索需求。
- 不因评论变大就立即拆表；动静拆分需按 24.4 §11.6 另测事务、锁、查询和 WAL 收益。
- 不减少既有采集字段、不截断必要评论来伪装压缩。超过提交预算采用有界分批/受控大对象路径，不静默丢正文。

### 4.4 其余事实表

| 表 | 主键/唯一性 | 核心字段与责任 |
|---|---|---|
| `crawler.channel_candidates` | candidate_id；规范 platform/channel_id 唯一 | channel_id、snapshot/evidence、qualification_status、reason_code、row_version、admitted_plan_id；候选归并与晋升 |
| `crawler.channel_candidate_sources` | source_id；稳定 source_identity 唯一 | candidate_id、query_run_id、page_ordinal、source_ref、rank、evidence_hash；同频道多来源不重复准入 |
| `crawler.observations` | observation_id；(channel_id, kind, kind_sequence) 唯一 | plan_id、domain_generation、source_batch_key、observed_at、outcome/reason、facts_hash、有界事实变化、extractor/policy 版本；恢复与 Feature 输入，不是普通日志 |
| `crawler.observation_sequences` | (channel_id, kind) | last_sequence；与对应 Observation 同事务递增，失败回滚，不用会天然跳号的 sequence 冒充无 Gap 业务序号 |
| `crawler.first_seen_ledger` | 稳定业务项 key 唯一 | content_key、plan_id、source_batch_key、事实引用、消费 Observation、consumed_at；内容先保存时与 ledger 同事务，后续 Observation 幂等消费 |
| `crawler.raw_objects` | object_id；(storage_endpoint_id, bucket, object_key) 唯一 | object_kind、wire/content Hash、size、codec、Schema、verified_at、retention_state、created_at；端点为受控引用，不存任意外部 URL |
| `crawler.object_references` | (object_id, owner_kind, owner_id, purpose) | 引用来源、保护原因、retain_until、released_at；未应用/隔离/发布/备份等保护并存，不能只看一个过期时间就 GC |
| `crawler.channel_about_metric_snapshots` | observation_id；与 Observation 绑定的频道/时间一致 | 三项数量及状态、facts_hash、observed_at、snapshot_origin、evidence_source/ref、recovered_at；保留 Feature 所需历史事实，区分新观察与历史恢复 |

首次发现 ledger 与 Observation 的去重 key 必须绑定真实业务项，不以“同频道同一天”吞掉合法新增结果。

## 5. 控制与执行：计划不是通用任务队列

### 5.1 `control.channel_coordination`

主键/外键：channel_id → crawler.channels。
主要字段：active_plan_id（可空）、coordination_version、updated_at。active_plan_id 的频道一致性用复合外键维护。
这行是同频道短事务的统一协调锁，不在外部采集/模型调用期间长期持锁。
Agent-only/补充计划是否允许并存由原活动 Gate/覆盖规则决定，不能仅建一个 `UNIQUE(channel_id) WHERE running` 禁掉所有合法并行。

### 5.2 `control.plans`

| 字段 | 作用 |
|---|---|
| plan_id / channel_id | 计划主键与频道绑定 |
| intent_key / intent_hash | 同一业务意图不重复创建；同 key 不同输入拒绝 |
| kind | FULL 或 INCREMENTAL；Data API 是阶段，不是新频道主计划 |
| required_domains | 冻结 ABOUT/VIDEO/AGENT 等本轮要求；实际义务有子表 |
| reference_at / coverage_day / supplemental_sequence | 冻结参考时间和日覆盖上下文，跨午夜不换计划 |
| policy_version / planner_version / frozen_context | 冻结参数及总预算来源 |
| active_stage / lifecycle_state / outcome / reason_code | 当前阶段、执行状态和本轮结果分开 |
| business_deadline_at / budget_policy_version | 总期限与预算，重试不重置 |
| row_version / started_at / settled_at / cancelled_at | 版本及真实状态时间 |

约束：PK(plan_id)、UNIQUE(intent_key)、UNIQUE(plan_id, channel_id)。不得把 `(channel_id, coverage_day)` 设为禁止合法补充计划的唯一键。
required_domains 在启动后不可随新 Clock 到期原地改写；若调整义务，走符合原规则的明确后继计划/代次命令。

### 5.3 `control.execution_authorizations`

主键：scope。字段：channel_id、plan_id、lane、execution_epoch、holder_id、active_stage、state、expires_at、row_version。
正式接管在锁住原授权后递增 epoch；SQL 重试、同意图启动重发不递增。
Main Lane 的 Full/Incremental 与 API 接管遵守互斥写权；Agent 请求可有独立授权 scope。
租约超时不证明外部请求已物理停止，事实应用仍需核验 epoch。Worker 自报授权不能直接入库成真。
这里只保存业务写入权，不重建 Temporal 的通用任务心跳、领取和重试系统。

### 5.4 领域义务、封口和覆盖

| 表 | 主键/唯一性 | 核心字段 |
|---|---|---|
| `control.domain_obligations` | (plan_id, domain, generation) | channel_id、input_revision/hash、target_hash、policy_version、work_sealed、expected/settled/unresolved 计数、outcome、required_feature_revision、row_version |
| `control.domain_items` | (plan_id, domain, generation, item_key) | target_identity、ordinal、frozen_target、state、disposition/reason、source_receipt_id、request_id、checkpoint；唯一业务项控制计数更新 |
| `control.domain_receipts` | receipt_id；(plan_id, domain, generation) 唯一 | input/target Hash、policy、outcome、proof_summary、source_receipts、recorded_at；由可信 Store 生成 |
| `control.feature_requirements` | (plan_id, domain, generation, observation_id) | required_feature_version、feature_receipt_id、state；明确本轮到底等哪份 Feature 回执 |
| `control.coverage` | (channel_id, coverage_day, domain, coverage_generation) | source_plan_id、eligibility、outcome/reason、policy_version、updated_at；保留原日覆盖定义，不把它简化为 success bit |

计数必须来自唯一业务项首次状态转换；已结算项不重复加一。`work_sealed=false` 时，即使未决数为零，也不能证明领域已完成。
扫描连续区间、锚点、parse gap、冻结目标等证据保存为有界检查点/关联对象，不以“没发现某视频”直接判 deleted/private。

### 5.5 可靠控制意图与预算

| 表 | 主键/唯一性 | 核心字段与边界 |
|---|---|---|
| `control.workflow_intents` | intent_id；稳定 dedupe_key 唯一 | plan_id、START/WAKE、workflow_id、目标版本、payload/hash、state、attempts、next_attempt_at、last_error；与产生意图的业务事实同事务 |
| `control.control_commands` | command_id；调用方 command_key 唯一 | actor_id、target_kind/id、expected_version、action、request_hash、state/result、created/applied_at；暂停/取消/重试先复核权威状态 |
| `control.budget_usage` | (owner_kind, owner_id, resource_kind) | limit_snapshot、used、reserved、policy_version、deadline、row_version；分 parser/网络/API/Agent/提交/SQL 等类别，禁止每层重置总预算 |
| `control.budget_entries` | entry_id；稳定 operation_key 唯一 | owner/resource、预留/消耗/释放、amount、attempt_identity、recorded_at；幂等记账，真实重复外部尝试仍记录消耗 |
| `control.capacity_reservations` | reservation_id；稳定 request_key 唯一 | resource_domain、owner、amount、state、expires_at、release_identity；有界资源准入，不将 CPU 空闲当授权 |
| `control.channel_preferences` | channel_id | manual_priority、policy_version、updated_at、updated_by；承接旧人工优先级输入，不与 Feature 计算分数混写 |

预算中属于 Rota 的真实网络账本继续由 Rota 管理；本地记录业务约束/关联回执，不能通过同步一份遥测替代 Rota 权威授权。
workflow_intents 是内部启动/唤醒义务，不进入 Publication Kafka。Temporal history 不替代 PG 的业务事实和幂等键。

## 6. 结果接收：批次、Submission、载荷和回执

### 6.1 `ingestion.logical_batches`

主键：logical_batch_key。主要字段：plan_id、channel_id、domain、generation、phase、ordinal、input_revision/hash、target_hash、policy_version、created_at。
引用正确的计划和领域代次，提供 UNIQUE(logical_batch_key, plan_id, channel_id)。
同一逻辑工作使用稳定 key，key 不包含 execution_epoch；重新采集可以创建新 Submission，但不能借此重复应用同一批业务效果。
批次边界冻结后不得原地换目标。批内分帧不增加新的 logical batch。

### 6.2 `ingestion.submissions`

主键：submission_id。核心列：

| 字段组 | 列 |
|---|---|
| 业务绑定 | logical_batch_key、plan_id、channel_id、producer_role |
| 原提交执行上下文 | original_scope、original_execution_epoch |
| 冻结输入 | generation、input_revision、input_hash、target_hash、policy_version |
| 协议 | contract_version、payload_schema_id、hash_profile、mode |
| 完整性 | content_sha256、wire_sha256、manifest_hash、wire_bytes、uncompressed_bytes、compression |
| 载荷入口 | payload_kind、object_id（对象模式）、frame_count（stream 模式） |
| 处理追踪 | processing_state、last_issue_id、created_at、received_at、applied_at |

保存不可变清单/其可重放表示用于核对，manifest_hash 的算法单独版本化。正文 Hash 对齐 24.6，不允许拿 JSONB 的输出字符串重新计算原 wire Hash。
同 submission_id 的身份、冻结输入和内容必须一致。处理状态可变，不可变清单不能被 `ON CONFLICT DO UPDATE` 覆盖。
schema 草案支持的角色目前仅 Full/Incremental；后续 Discover 查询级上下文和 API/Agent request 上下文使用版本化扩展，不能填假 plan/channel 通过外键。

### 6.3 `ingestion.submission_payloads`

主键/外键：submission_id → submissions。字段：wire_payload bytea、stored_bytes、stored_at、purged_at、retention_reason。
用于 PG 耐久接收路径。payload_kind=INLINE 时，返回 RECEIVED 必须已有完整、验证通过、可恢复的载荷；OBJECT 模式依赖 raw_objects 与受保护引用。
SQL CHECK 不能跨表证明载荷完整存在，因此 T2 Store 必须在同一事务锁定/验证并写回执，结合权限收口，禁止应用任意直接 INSERT RECEIVED。

普通 Fast Apply 可以 T2+T3 合并，在单事务保存身份、必要输入/摘要、事实和 APPLIED，不强制保留一份永远重复的完整正文。
需要后续 Apply 或恢复的完整载荷不得只剩 Hash。清理前必须满足重放、隔离、审计等保留义务。
Frame 先有界组装/验算，未持久的分帧进度不是 RECEIVED；首期不为每个网络 Frame 永久建一行数据库记录。

### 6.4 `ingestion.receipts`：唯一的持久 Submission 回执

字段：receipt_id（主键）、submission_id、logical_batch_key、plan_id、channel_id、content_sha256、status、recorded_at、receipt_schema_version。

约束与语义：

- status 仅 RECEIVED / APPLIED，回执行不可变。
- UNIQUE(submission_id, status)：同阶段只产生一份回执；返回重复请求时复用原 receipt_id/time。
- 部分唯一索引 `UNIQUE(logical_batch_key) WHERE status='APPLIED'`：一个逻辑批次仅一份最终应用回执。
- 复合外键约束回执所属 Submission/批次/计划/频道/内容 Hash 一致。
- RECEIVED 与 APPLIED 是两种不同确认，可以分别有不可变记录；当前已 APPLIED 时优先返回 APPLIED。Fast Apply 不必造一个中间 RECEIVED 回执。
- 另一个 Submission 竞争相同批次，不能把已采用提交 A 的回执改成 B 的成功；未采用提交需按明确处置记录，不静默丢弃其已接收义务。
- processing_state 仅帮助安排处理，不能与此表形成两套互相矛盾的应用真相；状态更新与回执提交同事务。

`NOT_OBSERVED` 只是一次权威查询的观察，不建成“永不提交”的否定回执。
已持久成功的原身份重放先核对完整性和读取权限，再返回原回执；原执行失效不抹掉已提交事实。新的应用效果始终核验当前执行授权。

### 6.5 `ingestion.submission_issues`

字段：issue_id、submission_id（可信时关联）、logical_batch_key、reason_code、effect、evidence_ref、occurrence_id、recorded_at、resolution_state。
同身份不同 Hash 的冲突必须保留原登记，另存有界冲突证据；不能为满足外键先创建一条伪 Submission。
COMMIT_UNKNOWN 不能在原未知事务里伪造可靠的“失败记录”；按原身份恢复查询后再更新诊断与处置。
受控隔离保留 RECEIVED 义务，设明确处理责任，不以日志清理策略删除待处理结果。

## 7. Query、API 和 Agent 的后续目标表

这部分列出目标结构，在相应切片补齐正文 Schema 和状态转换后分批建表，不现在一次性创建空壳。

### 7.1 `discover`：Query 闭环

| 表 | 身份/约束 | 核心字段 |
|---|---|---|
| business_category_mapping | (taxonomy_version, business_category_id) 主键；版本内 code 唯一 | 固定 19 类的 ID/code/中文名映射、enabled；来自现有业务分类，不改 Business DB |
| query_terms | query_term_id；normalized_text 的受控唯一性 | display_text、normalization_version、created_at；变更规范化算法需冲突评估，不默认允许每个版本复制同一词 |
| query_bindings | binding_id；UNIQUE(query_term_id, country_code, business_category_id) | taxonomy_version/code、lifecycle_state、refresh_factor、last_success_at、next_run_at、policy_version、row_version |
| query_candidates | candidate_id；来源输入的幂等键 | raw/normalized_text、country/category、source_type/ref、parent_binding_id、status、reason_code、settled_at |
| query_sources | source_id；稳定 source_identity 唯一 | binding_id、source_type/ref、parent_binding/channel/video/agent_request 引用、source_hash |
| query_runs | run_id；稳定启动意图唯一 | binding_id、冻结窗口/locale/sort/category/policy/adapter、预算、开始/结束、真实产出计数、outcome、下一档/生命周期决定和原因 |
| query_page_checkpoints | (run_id, page_ordinal) | continuation Hash/受保护引用、结果 Hash、applied receipt、parse gap、stop evidence |

QueryBinding 与分类映射的版本/code一致性由复合关联/受控更新保证。
首次 Query Run 使用 THIS_YEAR；成功后 WEEK/THIS_WEEK 或 MONTH/THIS_MONTH 的离散规则按 24.4 执行，不迁入旧 quality_score 连续权威分数。
同 Run 翻页/重试不改冻结参数，不因没有采集频道结果就跳过合法完成证据。
Query 页的候选、检查点和反馈意图同事务；跨频道晋升使用后续有界准入事务，避免一次锁住大量频道。

### 7.2 `control`：API / Agent 请求与批次

| 表 | 身份/约束 | 核心字段 |
|---|---|---|
| api_requests | request_id；稳定请求意图唯一 | plan/channel/domain/generation、phase、target_identity、required_fields/comments、input revision/hash、credential/quota 域、status、预算、result_ref、receipt_id |
| agent_requests | request_id；稳定请求意图唯一 | plan/channel/domain/generation、agent_kind、input revision/hash、input_policy、model/prompt/agent/policy 版本、state、budget、result_ref、receipt_id |
| request_batches | batch_id | role、compatibility_key、manifest_hash、execution_attempt、state、predecessor_batch_id、created_at |
| request_batch_members | (batch_id, request_id)；批内 ordinal 唯一 | request_kind、冻结成员输入引用；API/Agent 关联通过可验证 FK 或分别建成员表实现，不靠多态字符串假装有外键 |
| request_receipts | receipt_id；(request_kind, request_id, input_revision) 对有效应用唯一 | input_hash、result_hash、accepted_outcome、recorded_at；按请求确认，不以整批完成代替每项验收 |

批次执行后成员和参数不可变；失败项进入后继批次，业务 request_id 不变。API 是原计划依赖，Agent 可与主采集并行，但等待最低输入时必须 WAIT_INPUT。
Agent 已验证当前输出保存在 `crawler.agent_results`：channel_id + agent_kind 当前唯一，带 applied_request_id、input_revision/hash、output_hash、模型/策略/分类版本、observed_at 和有界输出；历史必要结果由原请求/版本证据管理。
具体字段按 24.8 补齐 input_url、input_content_ids、input_content_hash、agent_model、agent_config_id、prompt_template_id、prompt_hash、prompt_variant、taxonomy_version、agent_version_hash、last_observation_id、last_observed_at、current_output_hash，以及 output_json。概要 output_hash/observed_at 为含义简写，不额外创建同义值。
请求失败状态进入 agent_requests，已接受 Current 保留；这与旧 agent_profiles 失败时覆盖 status 的实现有差异，需要对发布资格和控制台展示做显式回归。
API/Agent 结果写入、请求回执、原领域证明和结算重检保持同一个短事务。

## 8. Feature 与三 Clock

| 表（Schema 均为 feature_clock） | 身份/约束 | 核心字段/作用 |
|---|---|---|
| event_inbox | event_id；来源 Observation + 消费域去重 | kind_sequence、payload_hash、pending_payload/ref、received/applied_at、state、reason；Gap 时保留输入 |
| observation_checkpoints | (channel_id, kind) | last_applied_sequence、last_observation_id、state_version；逐 kind 的处理水位 |
| channel_feature_state | channel_id | 订阅/播放变化、更新频率、Recent 变化、Agent 主题和证据变化、置信度、reference/policy/feature/state 版本 |
| channel_clock_state | channel_id | about/video/agent 各自 due_at、tier、last_complete_at、outcome；clock_version、feature_state_version、lifecycle |
| processing_receipts | receipt_id；(observation_id, engine_version, processing_generation) 唯一 | 输入/输出版本、outcome、state_hash、applied_at；区分在线处理与受控重算代次 |
| policies | policy_version | 不可变规则配置、checksum、activated_at；禁止无版本原地改策略 |
| reference_distributions | reference_version + reference_key | 参考分布、样本范围、生成时间与 Hash |
| recalculation_runs | recalculation_id | 目标规则/参考版本、冻结范围、进度、state、result_version；有界重算，不保存 Worker 通用队列 |
| clock_decisions | decision_id；来源处理身份 + clock_kind 去重 | before/after due、reason_codes、Feature/策略版本、source_observation、decided_at |

Feature 数学字段和完整配置按旧算法与样例逐一映射，不在本稿以几个指标替代原统一算法。
同一 Feature 事务维护 Inbox、状态、三 Clock、Checkpoint 和处理回执；必要时按统一锁序调用 trySettlePlan。
Feature 输入由 Observation/内部可靠义务驱动，不走对外 Publication Kafka；可靠投递义务复用下面的内部事件机制，不能提交后裸发消息。

## 9. Publication、内部投影与分析彼此独立

### 9.1 对外 Publication

| 表 | 身份/约束 | 核心字段 |
|---|---|---|
| `publication.streams` | stream_id；业务聚合身份唯一 | channel_id、domain、current_revision、last_sequence、row_version；序号在业务事务内分配 |
| `publication.revisions` | revision_id；(stream_id, sequence) 唯一 | plan_id、contract/policy_version、previous_sequence/hash、result_hash、payload_hash、不可变 payload/受保护引用、occurred_at |
| `publication.outbox` | id（event_id）主键；(revision_id, event_type) 唯一 | aggregate_type/id、event_type、schema_version、channel_id、revision、payload、occurred_at；Event Router 映射到约定字段 |
| `publication.delivery_receipts` | destination + event_id 主键 | revision_id、business_receipt_id、accepted_payload_hash、delivered_at、持久接受证据；业务侧幂等确认 |
| `publication.delivery_state` | destination + stream_id 主键 | 最新连续交付水位、缺口/等待原因、updated_at；单个后序 ACK 不能越过前序缺口宣称连续交付 |

Outbox 随事实/结算 INSERT，不保存旧轮询 Publisher 的 claim/lease/发送状态机；重试与 CDC 进度归 Debezium/Kafka。
PostgreSQL Publication 只包含 `publication.outbox`，不是全库 CDC。
payload 必须代表当时的不可变版本，不能等消费时按 channel_id 再查最新 Current。

**业务库不变的接入边界：**先核对现有 Inbox/Projection/回执字段及版本契约，再把新 Publication envelope 适配为现有业务端能够正确处理的输入。不能把新增传输字段直接要求业务库增加列，也不能把 Kafka offset 当业务提交回执。
业务端已有字段能否表达该事件身份、顺序和 Hash，要在对接样例验证；本稿不把旧表名与新内部字段同名视为已兼容。

### 9.2 内部事件与控制台投影

`control.internal_events`：event_id 主键、event_kind、source_kind/id/revision、channel/plan 引用、不可变有界 payload、payload_hash、created_at；来源身份 + 事件种类唯一。
`control.internal_consumptions`：(consumer_id, event_id) 主键、state、applied_at、next_attempt_at、error_code；Feature/投影等每个消费者独立去重、推进和清理确认。
需要异步投影的业务事务同事务登记内部义务；不默认向所有消费者广播所有事件，不复制全量评论正文做追踪。

| 表（read_model） | 主键 | 主要内容 |
|---|---|---|
| channel_control_state | channel_id | 当前完整性/新鲜度、本轮计划/各域状态、等待原因、next_due、发布/交付版本 |
| query_control_state | binding_id | 国家/分类、Query 生命周期、下次运行、最近 Run 和来源摘要 |
| plan_control_state | plan_id | 本轮 required domains、API/Agent 等待、领域证明、结算与发布资格 |
| execution_trace | trace_entry_id；source identity/revision + stage 唯一 | parent_ref、关联对象、stage/outcome/reason、发生时间；业务链路投影，不是原始网络日志 |
| dashboard_current | 固定 scope_key | 有明确口径的低成本当前汇总 |
| projection_checkpoints | projector_id + partition_key | source_watermark、projection_version、updated_at、rebuild_generation |

每个投影记录 source_fact_revision/source_watermark、projection_version、updated_at；异步汇总显示延迟。
控制动作回权威 Store 复核；投影丢失可按保留事实/事件重建。权威数据清理策略必须同步定义可重建的范围，不能一边删除唯一来源一边声称历史无限可重建。

### 9.3 分析投递

`analytics.outbox`：event_id 主键、kind、source_identity/revision、occurred_at、schema_version、payload_hash、有界分析 payload；与必要关键事实同事务写入。
`analytics.delivery_state`：consumer_id + event_id 主键，state、attempts、next_attempt_at、ack 信息。
ClickHouse 保存历史趋势和分析副本，不能成为 APPLIED、写权限或 Clock 的权威来源。
analytics 和 publication 不共用表、消费水位、清理条件或故障预算。普通可丢遥测也不能伪装成关键业务 Outbox。

## 10. 平台配置与运维数据

| 表/存储 | 内容 | 边界 |
|---|---|---|
| `platform.schema_migrations` | version、checksum、applied_at、release_revision | 唯一迁移入口，已应用版本不可悄悄修改 |
| `platform.configuration_versions` | config_kind/version、不可变配置、checksum、activated_at | 业务策略已有专用表的配置不重复放两份；凭据用 Secret 引用 |
| `platform.control_audit` | actor、command_id、before/after revision、action、result/reason、recorded_at | 关键控制操作与其事实变更同事务记录；敏感内容脱敏 |
| 监控/日志系统 | CPU、RSS、磁盘、Pod 状态、普通 debug、底层 Trace | 独立采样、保留；不为每个 HTTP 请求都同步写核心 PG |

Plan、Receipt、预算、冻结输入虽然会出现在运维页面，但属于业务恢复事实，不能迁到可随时删除的日志库。

## 11. 关联与事务：必须一起成功的写入

```text
QueryBinding → QueryRun → Page → ChannelCandidate
                                      │ 晋升/准入
                                      ▼
Channel ──► Plan ──► DomainObligation ──► DomainItem
   │          │              │
   │          │              └─► LogicalBatch ──► Submission ──► Receipt
   │          │                                        │
   └─► Content（含评论页） ◄────── T3 Apply ───────────────┘
              │
              └─► Observation / Ledger → Feature / Clock / FeatureReceipt
                                              │
DomainReceipt / RequestReceipt ────────────────┤
                                              ▼
                                  同事务 trySettlePlan
                                              │ 发布资格通过
                                              ▼
                                 Revision + Publication Outbox
                                              │ Debezium → Kafka
                                              ▼
                                      原有 Business DB
```

| 边界 | 同一事务中完成 | 事务外完成 |
|---|---|---|
| T1 准入 | 频道协调/活动 Gate、Plan、冻结义务、执行授权、START 意图 | 调用 Temporal、申请网络资源 |
| T2 接收 | Submission 登记、完整载荷/受保护引用、RECEIVED 回执 | 网络收帧、解压、大 Hash/Schema 静态验算、对象上传 |
| T3 应用 | 授权/输入版本复核、事实、检查点/ledger、Observation、APPLIED、领域计数/证明、必要事件意图 | YouTube/API/模型/S3/Kafka 调用 |
| T4 结算 | 核对封口、领域/API/Agent/Feature 义务；按策略更新计划/覆盖/发布资格；必要 revision/outbox/唤醒 | 等待外部依赖、全频道重扫 |
| Feature | Inbox、状态、统一三 Clock、Checkpoint、处理回执、必要结算重检 | 重型计算/外部 I/O，计算前后按版本复核 |
| Console 控制 | 幂等控制命令、当前版本/权限复核、状态变更、审计、可靠取消/唤醒意图 | 通知实际执行进程 |

T4 是函数和原子边界，不是新服务或 Finalize 队列。最后一个必要事实/Feature 回执到达的事务负责再次尝试结算。
统一锁顺序：按 channel_id 排序锁协调行 → Plan → 授权/领域/批次 → 按稳定 key 排序锁内容/请求 → Feature/Clock/发布流。
在 migration/Store 实现中核对所有写路径，包括控制命令与后台恢复；多频道批次优先拆成按请求的短事务，不能持锁等待网络。

并发同批次提交需要批次锁 + 唯一约束共同防护，不能仅靠先 SELECT 看不到回执就 INSERT。
COMMIT_UNKNOWN：沿用原身份去权威 Primary 核对，不因一次没查到就换 ID 重做；Standby 的延迟结果不能作为否定依据。
同步复制/RPO、备份与故障切主决定已确认数据保护水平，单个 COMMIT 或一个唯一索引不等于完整 HA。

## 12. 初始索引、空间与保留策略

初始只建主键、业务唯一键、必要外键查询索引和明确工作入口：

- contents：平台视频唯一性；(channel_id, published_at, content_key) 支持频道有界读取；Recent/修复索引按实际筛选条件和执行计划确定。
- plans：(channel_id, created_at, plan_id)；活动/待处理状态用符合业务谓词的部分索引。
- domain_items：(plan_id, domain, generation, state, ordinal)；仍需扫描排序的范围必须有界。
- submissions/receipts：submission_id、logical_batch_key 查回执；待应用提交 (processing_state, received_at, submission_id)。
- query_bindings：可运行生命周期下 (next_run_at, binding_id)，按 country/category 实际调度入口补索引。
- workflow/internal/analytics 待处理：next_attempt_at + 稳定 key，避免无界全表轮询。
- Observation/Trace/历史：channel/plan + time + 稳定 ID 游标分页，不用深 OFFSET。

不默认给所有 JSONB 建 GIN，不默认给每个 status 单独建索引，不为“未来规模”提前分片。
PG 原生时间分区是后续数据治理选项，必须先解决跨分区唯一性、回执保留和真实查询收益。

| 数据 | 首期保留原则 |
|---|---|
| 当前频道/视频/已保留评论 | 保留当前业务有效数据，不按普通日志 TTL 删除 |
| 未应用 RECEIVED、未完成计划、隔离材料 | 恢复责任未终结前不能删除 |
| 已完成载荷 | 满足重放/核验/审计窗口后可清理重复正文，保留必要身份、Hash、回执和证明 |
| APPLIED/幂等键 | 不因清理正文就删除；压缩/归档后仍须防止合法重放变成新效果 |
| Publication/分析事件 | 各自达到消费者确认、重放、备份和恢复条件后清理，不能共用一个 TTL |
| 历史 Observation/决策 | 近期 PG，长期按恢复/分析需要归档或进入 ClickHouse；不默认每次保存完整频道副本 |
| Raw/普通诊断 | 按证据与成本制定保留；Raw 若承担恢复责任则不按可丢诊断规则清理 |

保留天数没有生产数据和最长故障/重放窗口依据，不在此虚构固定天数。首期不启用自动破坏性清理；生产前明确各类 TTL、磁盘阈值、背压和归档验证，不能无限堆积。
Comment 页体积需评估原始/TOAST 后大小、P50/P95/P99、重复副本、WAL/备份成本；本稿没有测量生产库，不能承诺压缩比或所需容量。

## 13. 账号、迁移和发布

- migration_owner：创建/变更结构，记录迁移版本；运行期 Worker 不执行 DDL。
- data_ingestor：通过受控 Store 写事实/Submission/回执及必要领域/事件；无业务库写权限。
- planner/control：计划、授权、控制命令和准入相关权限，不能随意写 APPLIED。
- feature：必要 Inbox/Feature/Clock/回执/结算相关权限，按明确事务需要授予。
- projector/console：投影读写与展示分别授权；Console 控制写走 Control API 权威复核。
- debezium：必要复制权限及批准的 Outbox；Worker 不持有 Crawler PG 凭据。
- temporal/visibility/rota：各自独立账号、迁移版本和连接预算。

Store 共用事务 client 只发生在可信服务内部，模块边界不拆散必要原子事务。按账号/服务实例总数预算连接池，PgBouncer transaction pooling 不能承载未经确认的 session 级锁假设。
所有 migration 有版本、checksum、升级前检查、验证 SQL 和恢复说明。回退优先回退兼容应用版本，不能把 DROP TABLE 当成通用数据回滚。

## 14. 分批实施与验收

### 第一批：身份、提交、回执和最小事实闭环

先实施 platform.schema_migrations、channels/contents 的首个业务切片字段、channel_coordination、plans、execution_authorizations、domain_obligations/items、logical_batches、submissions、必要 payload、receipts/issues。
若启用 OBJECT/RECEIVED 则必须同时实现对象登记/保护或 PG 完整载荷路径；未实现的模式显式不启用，不用空引用通过验收。
首个事实切片实现到哪些字段由对应正文 Schema 决定，不宣称一个演示 JSON 就覆盖全部视频/评论业务。
应用、领域证明、Observation/ledger/可靠意图若在该切片产生，则其相关表随同进入该批迁移；在这些义务实现前，不生成“完整领域/计划成功”的假回执。

验收至少包括：

1. 首次提交保存事实与匹配 APPLIED；重复同身份同内容只返回原确认。
2. 同 submission_id 不同内容拒绝，原事实/回执不变。
3. 不同 Submission 并发争同一 logical batch，仅一个有效应用结果。
4. 错频道、错计划、错目标/输入版本被拒绝；旧 epoch 不产生新效果。
5. 已成功原提交在接管后仍可读取匹配回执。
6. 任一 SQL 失败时事实、检查点、回执一起回滚；COMMIT_UNKNOWN 按原身份对账。
7. RECEIVED 后重启/Worker 消失仍可恢复；只存 Hash/半包不能确认接收。
8. 更新评论数量不覆盖已有非空正文，不改原正文 Hash/时间；空/缺失补齐、disabled/unknown 按样例验证。
9. 同一平台视频不能因错误频道写出第二份身份；兼容范围与数据约束一致。
10. 实际列范围与首批协议的 20 位版本、计数、UTC、NULL/0 规则匹配。

### 后续批次

1. Query 与候选来源/准入：固定 19 类映射、Binding 去重、冻结 Run、分页与 Query Clock。
2. Temporal 启动/恢复、API/Agent 请求与批次：稳定请求身份、部分失败、预算和迟到结果。
3. Feature/Clock、Domain Receipt、覆盖与 trySettlePlan：封口、Gap、最后完成者原子结算。
4. Publication/分析可靠事件、Read Model/Trace：业务库兼容、独立水位、可重建和回放。

每批 migration 只引入该批已经定义和可验证的表，按最小端到端路径调整依赖，不把表数量当进度。
第一批分切片推进：事实表与迁移工具已在隔离库验证；Store、授权、Submission 与回执原子事务尚未完成，不能将本轮建表等同于本节整批验收。

## 15. 旧库参考与明确差异

只读参考路径：`/home/ubuntu/workspace/oldjiagousys`。已检查快照与相关运行期 SQL，不将其称为当前生产库全量盘点。

| 旧对象/行为 | 本设计如何处理 |
|---|---|
| crawler.channels / contents | 保留字段证据和稳定身份语义，新增字段按明确业务需要；不照搬所有旧混合状态 |
| contents.comments_first_page + retainedCommentPageSql | 采用同表 JSONB、已有非空正文优先保留；数量更新独立 |
| query_terms.quality_score、旧 Query 调度 | 按 24.4 改为 Term/Binding/Run、离散 factor 和生命周期，不迁回旧连续权威分 |
| channel_runs 的 queue/job/epoch 混合字段 | Plan 业务、执行授权、Temporal 执行关联分清；旧队列字段不是新业务主键 |
| incremental video batch/item、First-Seen ledger | 冻结目标、条目回执、检查点和待消费事实保留并统一身份 |
| feature_clock.* | 保留统一算法、三 Clock、版本和可靠处理语义，不另建 Feature 业务库 |
| remote_ingestion whole_channel_inputs/chunks | 保留完整性、重放和耐久责任；不复制 NATS/Base64 与每帧永久落表形态 |
| 旧 publication.outbox 的 lease/attempt 轮询状态 | 改为 INSERT 型业务 Outbox + Debezium/Kafka；业务交付确认另表保留 |
| Business 库 Inbox/Projection | 保持原库原样，通过接入契约/Consumer 适配满足新传输链 |
| 旧数据导入 | 后续一次性脚本，字段映射、冲突/计数/Hash 对账；不作为新库设计先决条件 |

具体源码证据：

- `oldjiagousys/database/bootstrap/crawler.sql`、`business.sql`：结构快照与约束。
- `oldjiagousys/services/qybullmq/src/schema.sql`、`services/feature-engine/sql/schema.sql`：运行期补充结构。
- `oldjiagousys/services/qybullmq/src/videoContentMutationPolicy.js`、`videoContentStore.js`：评论保留与证据更新。
- `oldjiagousys/services/qybullmq/src/incrementalYoutubeJsVideo.js`、`youtubeJs.js`：Full/Metrics 与底层评论请求区别。
- `oldjiagousys/services/qybullmq/src/commentFirstPageBackfill.js`：缺失/空页补齐条件。

架构对照：24.4 §9（事务/回执）、§11（字段/索引/拆表边界）、§14（接收/对象/背压）、§19.4/§21.12（单库）、§23（Publication）、§24.10～24.13（证明/结算/覆盖）、§26（Query）、§27（Console）。
24.6 的字段名仍是契约草案，任何落库映射差异必须同步更新契约、样例和测试，不静默缩小范围或改换身份。

## 16. 架构与实施差异登记：发现问题就修订

用户已明确：24.4 尚未经过完整实际实施，遇到数据库、代码或运行结果冲突时必须提出，允许修改架构。
这里区分“已确认的冲突”“兼容性尚未验证”“待实测优化”，不把所有待验证项都说成架构错误。

| 编号/状态 | 原设计或原理解 | 实际证据/问题 | 建议处置与影响 |
|---|---|---|---|
| DB-01 首批类型范围已落实 | 24.6 counter 接受最多 20 位十进制字符串；很多旧版本字段是 bigint，24.4 发布示例 revision 也是 bigint | PostgreSQL 有符号 bigint 最大为 9223372036854775807，容纳不了整个草案范围。此次是协议范围与落库选择的差异，不是全部版本字段必须同一种类型 | 0003 已对 epoch/generation/input_revision 使用 numeric 整数域，CHECK scale=0 且不超过 20 位上界；避免 numeric(20,0) 隐式舍入小数。20 位、溢出、小数拒绝均已测试，JS 保持字符串；采集数量仍是 bigint，不混用 |
| DB-02 待验证的外部兼容性 | 24.4 新链采用 versioned event + Debezium/Kafka；用户确认 Business DB 不变 | 旧 Business Inbox/Revision 有 revision_id、stream_id、domain、data_sequence、payload/envelope Hash 和原接受语义；新 event_id/aggregate/revision 示例不能直接假设兼容 | 先制作旧接收样例与新事件映射，再决定 Outbox 字段和 Consumer 适配。必要时修订 24.4 发布契约；不先定死新格式再要求业务库改表 |
| DB-03 已核实的行为理解差异 | 曾将“存量不更新评论正文”解释为“存量不再请求评论接口” | 旧 metrics 入口仍会请求评论；写入保留已有非空第一页，评论数量独立更新。24.4 S-17/I-23/I-24 已提醒这一边界 | 本稿分开数量、正文和观察时间，纠正此前口头概括；若以后希望减少网络请求，另列行为修改并回归数量/缺失/失败规则 |
| DB-04 待实测的存储优化 | “评论大就拆表/外置/换压缩”只是优化假设 | PG 已有 TOAST；未改变的行外值通常可复用。当前没有生产页大小、WAL 或查询成本样本 | 用户已选 A 方案，本稿保留同表 JSONB。若实际证据显示瓶颈，提出比较报告后调整；不以原方案定案阻止改进，也不预先承诺 LZ4 或拆表收益 |
| DB-05 首批协议覆盖不足 | 24.4 是五角色统一受控结果入口；24.6 当前只接受 Full/Incremental 且必填 plan/channel | Discover 查询页结果未必对应单频道计划；API/Agent 还需要 request 身份、输入版本和批成员关联 | 第一批范围显式受限，后续做按上下文区分的版本化契约。新表不能通过假 channel_id、万能 NULL 或任意 JSON 来伪装五角色已统一 |
| DB-06 已确认的部署与目标差距 | 24.4 目标具有已确认数据恢复、HA 与稳定入口 | 已有验收报告明确当前是异步 PG 主备、人工切换、验证期入口；SeaweedFS 保护/复制也未完成生产验收 | RECEIVED/APPLIED 的 SQL 原子性与机器故障下的持久保护分别验收。生产前明确 RPO、同步策略、备份、受保护对象与切主恢复，必要时调整部署方案；不能仅凭表结构宣称目标已达成 |
| DB-07 已核实的字段遗漏 | 本稿最初只列核心字段组，初步视频列扫描报 55 列 | 快照实际 56 列，包含带引号 position；另有 is_recent、Player/Next Hash、休眠复查、字段质量等实际语义 | 24.8 逐列覆盖五表共 173 列；补齐本稿，不把概要未写出的字段视为删除批准 |
| DB-08 首批身份样例已验证 | 最初拟 UNIQUE(platform, source_content_id)；旧 content_key 包含 channel/type/source | 旧 contents 同时允许 video/short/live/post，旧 upsert 按 channel/source 冲突复用已有 key；内容类型可能纠正 | 候选唯一键增加稳定 resource_kind 命名空间，视频三种类型共用身份；content_key 不因纠正类型改变。0001 migration 已在独立库通过错频道重复拒绝、video→short→live 主键不变、帖子独立命名空间样例；旧数据导入/发布引用兼容仍待验证 |
| DB-09 已核实的 Hash 契约差异 | 本稿通用 Hash 约定最初写成全部裸 64 位 | 旧 Agent 多个 Hash 和 publication_item_hash 带 sha256:，prompt_hash 不带；规范算法也可能不同 | 分字段/契约记录格式和算法，不能去前缀后当新 JCS；发布适配保持旧接收契约 |
| DB-10 已核实的 Raw 归属差异 | 大原始响应外置原则容易被误用成整列 raw_json 外置 | 实际 Store 从该列读取 observed 标记，并写发布证据冲突、采样和来源信息 | 业务判定所需有界证据留 PG，原始字节再外置；迁移前审计 JSON 路径，不删除未知键 |
| DB-11 请求失败与 Current 的模型差异 | 旧 agent_profiles 一行同时含输出和最新成功/失败状态 | 失败写入会更新 status；新架构区分长期 Current 与本轮请求结果 | 保留有效输出、单独记录请求失败；不能自动宣称仍满足本轮发布资格，按冻结 Plan/Request 规则判定，列为业务兼容测试 |

| DB-12 首批落库的证据约束调整 | 旧 is_members_only 必填默认 false；部分 JSON/disabled CHECK 可返回 NULL | false 混淆未知；PostgreSQL CHECK 的 NULL 不拒绝缺失字段 | 新 facts 中会员标记可空、已知 access_status 配对；评论页严格校验必填/类型/日期。指标数值与质量状态配对，缺失观察须由 Store 保留旧值及旧证据。独立库测试通过；不是允许改外部业务库，旧消费者兼容需适配验收 |

| DB-13 Store 首批观察顺序边界 | 并发重复/迟到结果需要稳定判定，单靠到达先后不足 | 同时刻不同有效指标无法仅凭 timestamp 确定谁正确；页面正文保留时间与最新检查时间不同 | 首批按各指标有效 observed_at 判定新旧，等时异值回滚报冲突；页面另设 checked_at，已保留非空页不替换。内部 Hash 采用显式 PG JSONB profile，不充当 wire Hash。Plan/epoch/Submission 仍须另做授权与幂等判定 |

| DB-14 受控提交首批范围与目标模型分开 | 24.4/本稿描述完整多角色、多批次、领域证明与计划结算 | 本轮只能证明有界视频指标批次的原子应用，不具备完整领域结算证据 | 0003 限定 INCREMENTAL/delta、VIDEO_METRICS、一义务一批，使用单独 batch_checkpoints 存合并结果；不创建假的 Domain 成功。真实 Planner 接入前须扩展/迁移此局部结构，不把限制当全局架构定案 |

| DB-15 用户已确认 Kafka 结果入口 | 原 24.4 限定 Kafka 仅 Distribution，Worker 直连 gRPC | 大量采集结果需要成熟消息缓冲与消费组分发；用户确认复用 Kafka | Results 与 Publication 分 Topic；0004 记录 Kafka 消息 APPLIED/QUARANTINED，成功处理记录与事实/回执同事务后才提交 offset。BROKER_ACKED 不是 PG RECEIVED。新采集入口依赖 Kafka，须补齐保留窗口、权限和积压管理，详见 24.9 |

后续每发现一个问题，记录：涉及章节/表/契约、可复查证据、影响的业务行为、备选方案、建议选择、验证方法及结论。
影响身份、采集规则、外部兼容或数据保留含义的改动先明确讨论；不影响语义的实现细节可在授权范围内推进并记录依据。
方案采纳后同时修改相应架构章节、24.6 契约、24.7 表设计及 migration/测试，保留变更理由。没有采纳结论的候选方案不写成已实施事实。


## 17. 首个事实表 migration 的实际落地范围

- 文件：`database/migrations/0001_collection_facts.sql`；执行器：`ops/db/migrate.mjs`。
- 已在 S1 独立库 `crawler_schema_test_20260920_facts01` 建立迁移台账、频道 61 列、内容 59 列。
  样例数据全部事务回滚，保留空表与版本记录供检查。目标 `crawler` 和外部 Business 未执行本批迁移。
- 频道直接事实/生命周期字段已经落地，旧执行字段不搬入事实表。
  contents.last_observation_id / last_source_plan_id 随后续 Observation / Plan 一起添加并建立外键；不能把“173 列有映射”说成“173 列已经全部建表”。
- 新内容身份选择已落实并有数据库样例；默认随机 UUID 文本，不把可变 content_type 拼入主键。
- 评论页保留 collected_at，当前数量另有 observed_at；保持 JSONB 默认 EXTENDED/TOAST。
  独立正文 Hash/profile、合并保留规则和迟到结果判定由下一步 Store 补齐，本轮没有为 Hash 编造算法。
- 采集指标暂用非负 bigint，Node pg 返回十进制字符串；DB-01 的 20 位执行/版本计数尚未进入本轮表，不能误称范围矛盾已解决。
- 新库未知会员标记为 NULL，已知公开/会员状态成组校验；这是新事实表达的明确差异（DB-12），Business 输出适配不能省略。
- 数据库集成 10 项、既有契约/应用 19 项通过；重复执行无新增迁移，故意错误 migration 的 DDL/台账一起回滚。
- 说明与执行/恢复方式见 `database/README.md`；验收证据见 `ops/checks/2026-09-20-database-facts.md`。

下一切片实现受控 Store 的字段合并、评论保留/空页补齐及观察顺序测试，再接 Plan/授权/Submission/回执同事务。
迁移 owner 只是验证账号；运行时角色权限、业务正文 Schema、真实采集、HA/备份和 Business 兼容均不在本轮已完成范围。


## 18. Store 首个指标/评论切片实施更新

`services/facts-store/src/content-store.ts` 已实现可信内部事务库，覆盖 youtube_video 的播放量、点赞数、评论数量和第一页评论。
已有频道为前提；按稳定来源身份插入/锁行，错误频道拒绝，指标观察不能顺便纠正内容类型。
当前仍没有 Worker 入口、Plan/epoch 授权及 Submission/Receipt；不能绕过这些外围复核直接当生产写入 API。

- 指标按各自有效 observed_at 更新；未知/缺失保留旧值及证据，旧观察不回退，同时间相同数据重复无变更，同时间冲突回滚。
- 已有非空评论页保留；缺失/空页允许有效页面补齐；页面自身采集时间和 checked_at 决定迟到页处理。
- 新增 0002 migration 的四列后，contents 当前为 63 列。正文 Hash/profile/observed_at 由数据库随正文生成，checked_at 由 Store 推进；正文未变不重算 Hash。
- Hash profile 为 pg17-jsonb-text-sha256-v1，仅作本地存储校验；不是 JCS 或 Publication Hash。
- 一次事务中任意操作失败则整体回滚；后续检查点/回执需使用同一 tx.query，不另开连接。COMMIT_UNKNOWN 仍待外围对账实现。
- S1 隔离库中 24 项数据库测试通过，含有数据升级及真实双连接并发；19 项原有测试通过。原 migration 0001 校验和未改。

说明见 `docs/migration/content-store.md`，证据见 `ops/checks/2026-09-20-content-store.md`。
正式目标 crawler 尚未应用 0001/0002，外部 Business 和旧仓库没有改动。
下一项是实现最小 Plan/执行授权/Submission 与事实、检查点、回执原子事务；频道和完整内容字段 Store 仍按各自切片补齐。


## 19. 最小受控 Submission 原子事务实施更新

新增 `0003_metrics_submission.sql` 和 `services/ingestion/src/metrics-submission.ts`。
最小计划/活动频道/执行授权、冻结输入和目标、Submission、APPLIED 回执与批次检查点已经落到独立验证库。
事实写入与回执/检查点同事务；同提交重试复用回执，不同提交竞争同批只能一个成功；接管后旧 epoch 拒绝新效果，历史成功回执仍可读取。

同步路径保存完整有界规范字节，未开启 RECEIVED/OBJECT/后台 Apply；Hash 编码、正文 Schema 和样例均已补充。
在库内模拟写完事实/回执后检查点失败，所有结果一起回滚；客户端模拟 COMMIT 确认丢失后能够按原身份对账。
38 项数据库集成测试和 21 项契约/应用测试通过；0001/0002 校验和未改，正式 crawler 与外部 Business 未应用变更。

本轮采用局部 VIDEO_METRICS 义务与 batch_checkpoints，domain_items 主键按 batch/source，未实现第 5.4 节完整业务项状态机或 DomainReceipt。
所建 plans 也不是第 5.2 节全部字段/控制逻辑，缺少真实预算、期限、覆盖/Feature/结算；不以相同表名宣称整个架构已经实现。
内部错误码仍需映射到对外 problem 契约，Principal 仍需真实认证，尚无网络服务入口。
详细规则见 `docs/migration/metrics-submission.md`，证据见 `ops/checks/2026-09-20-metrics-submission.md`。
下一步先补 Data Ingestor 的认证/Manifest 传输适配与接口，再在验证环境部署，不直接启动真实生产采集。

## 20. Kafka 结果入口与处理记录实施更新

用户已确认采用 Worker → Kafka Results → Data Ingestor → 单逻辑 PG，取代上一节的直连传输待办。
新增 `0004_kafka_result_outcomes.sql`，仅在 S1 隔离库 `crawler_schema_test_20260920_facts01` 应用；正式 crawler 与 Business 保持原样。

| 字段/约束 | 含义 |
|---|---|
| stream_id/topic/partition_id/record_offset 联合主键 | 一条 Kafka 记录的稳定位置；重建 Topic 必须换 stream_id |
| record_sha256 | 原始 key/value 含 NULL 区别的消息指纹；同位置不同字节拒绝 |
| disposition | APPLIED 或 QUARANTINED；不是 Submission 的 RECEIVED |
| receipt_id / error_code | APPLIED 关联原成功回执；隔离记录保存错误原因且没有成功回执 |
| key_bytes / raw_value | 保留 key；仅隔离记录保留 value，分别限制 4 KiB/2 MiB |
| recorded_at / UPDATE 拒绝触发器 | 不可覆盖处理结果；隔离时间部分索引用于查询 |

成功记录与授权核对、事实、Submission、APPLIED、批次检查点同事务提交；随后才确认 Kafka 进度。
永久错误先回滚业务事务，再持久化隔离原文；隔离保存失败、数据库失败或 COMMIT_UNKNOWN 均不推进进度。
同一位置重放复用记录，同一 Submission 跨位置重发复用业务回执；不声称 Kafka 与 PG 具有分布式原子提交。

9 项真实 Kafka 测试通过，覆盖双消费者并行、故障重试、重启重放、坏消息隔离、排队后失权和保留缺口；数据库回归 37 项通过、1 项已升级库跳过，24 项无数据库测试通过。
0001～0003 校验和未变，测试数据/临时 Topic/消费者组已清理；见 `ops/checks/2026-09-20-kafka-results.md`。
常驻消费者、生产认证/ACL/最小 PG 权限、终态查询及保留/积压告警仍待下一步部署，当前没有上线真实 Worker。
