apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - network-policy.yaml
  - pdb.yaml
configMapGenerator:
  - name: postgresql-entry-config
    files: [haproxy.cfg]
