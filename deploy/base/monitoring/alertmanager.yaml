global:
  resolve_timeout: 5m
route:
  receiver: platform-only
  group_by:
  - alertname
  - node
  - instance
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 4h
receivers:
- name: platform-only
