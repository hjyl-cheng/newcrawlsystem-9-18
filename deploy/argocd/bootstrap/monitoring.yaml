apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: crawl-monitoring
  namespace: argocd
spec:
  project: crawl-monitoring
  source:
    repoURL: https://github.com/hjyl-cheng/newcrawlsystem-9-18.git
    targetRevision: main
    path: deploy/overlays/validation/monitoring
  destination:
    server: https://kubernetes.default.svc
    namespace: crawl-monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 30s
