# 24.6 业务通信协议与数据契约：第一阶段

更新日期：2026-09-20。

> 后续决策：用户已确认以 Kafka Results 作为采集结果主入口，见 [24.9](24.9_Kafka采集结果入口与并发入库实施方案.md)。本文提交身份、业务 Hash 和 APPLIED/NOT_OBSERVED 语义继续复用；旧 gRPC Manifest/Frame/OBJECT 是历史传输草案，不能当作当前已实现接口。下文“尚未实现”的状态是本阶段初始记录，最新实施状态见 24.9。
>
> 当前消息版本为 `crawler-result-kafka/1-draft.1`：规范 JSON 签名封装包含 key_id 与 submission，Kafka key 为 channel_id。生产者的 BROKER_ACKED 仅说明 Kafka 确认接收，不是 PG RECEIVED/APPLIED；QUARANTINED 是消息处理记录，也不是成功回执。当前没有启用 RECEIVED。消费端入库时仍检查现行执行授权。

本文把 24.4 中的业务通信规则整理成第一批可检查的协议定义。当前只推进“确定数据契约”这一项，供下一步建表、Store 和 Data Ingestor 实现使用。

**通俗地说：先统一提交单、签收单和异常通知的格式，再编写真正收货、入库的程序。**

## 1. 本次交付到哪里

本次先定义 **Full / Incremental Worker → Data Ingestor 的提交与回执边界**，包含：

1. 谁在提交、属于哪个计划和批次、使用哪一份冻结输入。
2. 结果如何标识、分帧、引用大对象和校验完整性。
3. 怎样查询、识别 RECEIVED / APPLIED 回执。
4. 没查到回执、结果不确定、重复提交、执行过期时如何解释。
5. JSON Schema、正常/异常测试样例与跨语言编码向量。

当前协议标识为 **`data-plane/1-draft.1`**，是供实现与验证使用的第一版草案，不是已经上线的生产协议。
24.4 已定案的业务语义继续有效；本文选择的字段名、严格格式和 Hash profile 属于本次工程草案。
正式冻结 v1 前，还必须通过载荷编解码、身份核验、T2/T3 数据库事务与故障恢复验收。

当前没有实现数据库迁移、Store、gRPC 服务、Worker、Journal 持久化或业务状态机，也没有修改现有线上部署。

## 2. “通信协议”分两层

| 层次 | 解决的问题 | 本系统选择 |
|---|---|---|
| 传输方式 | 消息怎么送到另一个程序 | 当前结果入口使用 Kafka Results、长连接、有界消息与消费组；旧 gRPC 草案由 24.9 替代 |
| 业务数据契约 | 传什么、是什么意思、收到后能确认什么 | 本文的身份、清单、回执、问题码与恢复规则 |

这里的 JSON Schema 是跨语言的元数据定义和测试表达，不代表热结果要使用 JSON + Base64 传输。
后续 gRPC IDL 将元数据映射到消息字段，Frame 正文使用二进制 bytes。

```text
Full / Incremental Worker
   │
   ├─ Local Journal：保存这次不可变结果
   │
   ├─ Submission Manifest：提交清单
   ├─ Binary Frames：有界结果字节
   │         gRPC / HTTP2
   ▼
Data Ingestor
   ├─ 契约 / Hash / 权限 / 版本 / 质量 / 容量检查
   ├─ T2：耐久接收，必要时先返回 RECEIVED
   └─ T3：原子应用，返回 APPLIED
               │
               ▼
       PgBouncer → 单逻辑 Crawler PostgreSQL

Worker ── Receipt Query ──► Data Ingestor ──► 权威回执

大对象例外：Worker → SeaweedFS → 对象引用提交 → T2/T3
```

Kafka 用于对外 Publication 分发，不插入这条内部结果提交链。正常 Delta 不强制经过 SeaweedFS。

## 3. 三种编号必须分开

| 身份 | 可以理解成 | 重试时的规则 |
|---|---|---|
| `logical_batch_key` | 这批要完成的工作编号 | 冻结输入/目标决定；不包含 execution epoch；普通重试不换 |
| `submission_id` + Hash | 这一次采集产出的包裹编号和内容指纹 | 原字节重传不换编号；重新采集产生新观察时可以有新编号 |
| `scope` / `execution_epoch` | 当前谁有权把结果应用进去 | 正式接管才变；网络重传、SQL 重试、重复启动不自行加一 |

`plan_id` 表示业务计划，`channel_id` 表示频道。一个频道可以有多轮计划，一个计划可以拆成多个有界批次。
不使用“频道 ID + 日期”代替全部计划身份，也不把一次请求的连接编号、Pod 名称、服务器 IP 当成业务身份。

ID 的生成和唯一约束归创建该业务对象的权威模块；本次定义其含义与传输格式，不用 Hash 或格式校验冒充数据库唯一性。
草案使用区分大小写的 ASCII 不透明 ID，长度 1～256，不自动 trim、改大小写或替换字符。
大整数版本、generation 和 epoch 使用无前导零的十进制字符串，最多 20 位，避免 JavaScript Number 丢精度。
这只是传输格式上限；数据库具体列类型及其数值范围在建表阶段另行核验。

## 4. 第一批消息清单

| `kind` | 方向 | 表示什么 |
|---|---|---|
| `SUBMISSION_MANIFEST` | Worker → Ingestor | 声明本次提交身份、输入、内容指纹、字节数与载荷位置 |
| `FRAME_DESCRIPTOR` | Worker → Ingestor | 描述一片二进制数据的所属提交、序号、偏移和 Hash |
| `RECEIPT_QUERY` | Worker → Ingestor | 查询原提交的持久回执，不创建新执行身份 |
| `SUBMISSION_RECEIPT` | Ingestor → Worker | 确认 RECEIVED 或 APPLIED |
| `RECEIPT_OBSERVATION` | Ingestor → Worker | 当前查询未观察到回执，状态为 NOT_OBSERVED |
| `SUBMISSION_PROBLEM` | Ingestor → Worker | 说明本次交互遇到的问题，不是持久成功回执 |

`RECEIPT_QUERY` 查到匹配回执时直接返回 `SUBMISSION_RECEIPT`；权威查询成功但未看到记录时返回 `RECEIPT_OBSERVATION`。
数据库不可用、查询权限不足或提交身份/内容冲突不能伪装成 NOT_OBSERVED。

## 5. 提交单包含哪些信息

| 字段 | 通俗解释 | 约束 |
|---|---|---|
| `contract_version` | 按哪一版协议解释 | 当前只接受 `data-plane/1-draft.1` |
| `identity.plan_id` | 哪个业务计划 | 由计划权威模块给出 |
| `identity.channel_id` | 哪个频道 | 必须与冻结输入、结果主体一致 |
| `identity.logical_batch_key` | 哪一批工作 | 重试保持，不带 epoch |
| `identity.submission_id` | 哪一次不可变产出 | 原提交重传保持 |
| `producer_role` | 哪类 Worker 提交 | 本批只开放 `fullcrawl`、`incremental` |
| `execution.scope / execution_epoch` | 依据什么执行权提交 | 服务端向权威 Store 复核，不能只相信报文 |
| `frozen_input.generation` | 哪一代领域义务 | 不能混用其他轮次的完成证据 |
| `frozen_input.input_revision / input_hash` | 当时使用的输入版本和指纹 | 与权威冻结输入对照 |
| `frozen_input.target_hash` | 本批冻结目标的指纹 | 不允许恢复时偷偷换成新目标 |
| `frozen_input.policy_version` | 按什么策略执行 | 重试不悄悄切换策略 |
| `mode` | 完整目标、变化还是修复 | `bootstrap_snapshot` / `delta` / `repair` |
| `created_at` | 本次提交建立时间 | UTC，毫秒精度；重传保留原值 |
| `payload` | 内容格式、Hash、字节数、位置 | 详见下面的规则 |

三种 mode 的含义来自 24.4 §14.2。Snapshot 仍然有界，不代表把频道多年历史塞成一包；Delta 只携带本轮变化与必要证据；Repair 针对指定实体或阶段。

`payload.schema_id` 表示结果正文的独立版本。服务端只允许注册并实现的正文 Schema，不能因为 envelope 合法就接受任意 `schema_id`。
样例中的 **`example.result.v1` 仅为协议演示，不是生产频道数据格式**。
频道、视频、评论、Disposition、覆盖证明等完整正文契约仍按相应业务切片补齐；不以一个任意 JSON 字段宣称已完成业务保真。

## 6. 内容、分帧、对象和 Hash

`payload` 包含：

- `schema_id`、`hash_profile`：正文格式和内容规范化规则。
- `content_sha256`：规范内容的 SHA-256，小写 64 位十六进制。
- `wire_sha256`：整个传输载荷字节的 SHA-256。
- `uncompressed_bytes`、`wire_bytes`：解压前后各有独立字节计数。
- `compression`：草案允许 none/gzip；允许的编码仍须在服务端实际实现后启用。
- `location`：stream + frame_count，或者 object + bucket/object_key；两种方式互斥。

本草案的 `jcs-sha256-v1` 对以下内容对象做 RFC 8785 JSON Canonicalization，再将 UTF-8 字节计算 SHA-256：

```text
{
  identity,
  producer_role,
  frozen_input,
  mode,
  result
}
```

把业务身份和冻结输入纳入内容，避免只校验 result 而忽略它属于谁。执行授权仍单独向 Store 复核，Hash 不是授权证明。
执行上下文、创建时间及清单其他字段也不能在同一 Submission 重放时随意修改；Store 后续须核对已登记清单。
新执行接管不修改已确认的原始提交，应用授权的接管命令需另行定义。

选择 none 时传输字节就是上述规范字节；选择 gzip 时，wire Hash 校验压缩字节，解压后再检查规范内容、content Hash 和实际字节数。
大对象保存的是同一套 wire 字节。ETag 不能代替 SHA-256。
此 profile 只适用于新的草案数据；旧的已确认数据保留原算法和原字节。

Frame 的序号从 0 开始，offset 按 wire 字节计算。接收实现必须检查所属 submission、连续偏移、数量、总长度、每帧 Hash 和整体 Hash：

- 缺帧、重叠、内容冲突或总量不符，不能形成 RECEIVED。
- 同一序号同一内容的重传可以识别为重复；重复帧不是新的业务提交。
- 收到一帧或全部进入内存，都不等于中央已耐久接管。
- schema 中的整数范围只是编码范围，不是允许单包达到该大小的运行预算。

生产前必须给 Frame、Submission、解压比例、在途字节、活跃 RPC、Apply 事务、待应用耐久字节配置独立上限；具体值通过实际样本与资源测试确定。
长期复用少量 gRPC Channel，每次 Submission 或有界 Group 的 RPC 必须结束。普通 ClusterIP 不保证已建立的长连接自动迁到新 Pod。

对象引用只允许受控存储端点和受限前缀，不能接收任意外部 URL。必须验证对象存在、大小、Hash、保留保护和 T2 登记；单凭 S3 上传成功不能回 RECEIVED。

## 7. 两种签收单和一个“没查到”

| 结果 | 真正承诺 | 没有承诺 |
|---|---|---|
| `RECEIVED` | 中央已持有完整、可独立恢复的提交，Worker 消失也能继续处理 | 不代表业务事实已应用，不代表 Plan 完成 |
| `APPLIED` | 对应批次事实、检查点/账本、必要事件和回执已在同一事务提交 | 不代表所有领域完成，也不代表外部业务库已交付 |
| `NOT_OBSERVED` | 这次权威查询在该时刻未观察到匹配回执 | 不证明原事务永不提交，不证明可以换编号重做 |

收到回执必须核对计划、频道、逻辑批次、Submission 和内容 Hash。另一份结果的成功回执不能用来删除当前 Journal。
已 APPLIED 的重放返回原持久确认内容，不创建第二次应用；单个 logical batch 只产生一份最终应用回执。
多个 Submission 竞争同一逻辑批次时，由 Store 决定采用哪份；不能把已采用 A 的回执改名成 B 的成功。

Fast Apply 可以直接返回 APPLIED，不要求先发一个 RECEIVED。排队处理必须先完成真正的 T2 耐久接收。
RECEIVED 之后校验失败或进入隔离，不得把它静默删掉或伪装 APPLIED；接收义务与后续处理结果分开记录。
APPLIED 后不因下游 Kafka/Business 延迟而退回“未采集”。

Journal 清理规则：普通快路径等匹配 APPLIED；中央已完整耐久接管的 Durable Receive/Object 路径可在匹配 RECEIVED 后清理。
**这补充了此前口头解释中“只能等 APPLIED 清理”的简化说法，以 24.4 §14.6 为准。**

## 8. 重复、断网、过期和背压怎样处理

| 场景 | 必须采取的行为 |
|---|---|
| 已提交但回执丢失 | 按原身份查回执/重传原字节，不因为断网就重新采集 |
| 同 Submission、不同内容 | `IDEMPOTENCY.PAYLOAD_CONFLICT`；保留原记录，拒绝覆盖 |
| 重复应用同一逻辑批次 | 核对权威回执和已采用提交，不能重复产生业务效果 |
| 已完成提交重放，但原执行已过期 | 完成必要身份/完整性及查询访问校验后，优先返回原匹配回执；不要求仍持有新的写权 |
| 旧执行尝试产生新效果 | `AUTHZ.EXECUTION_STALE`；停止旧执行，不能自行增加 epoch 绕过 |
| 内容版本冲突 | `DATA.VERSION_CONFLICT`；复核当前版本与证据，不等同于执行权必然失效 |
| COMMIT 响应丢失 | `DB.COMMIT_UNKNOWN`；去权威主库查回执，一次查无不能断言失败 |
| 接收/应用容量不足 | 在接纳义务前背压，保留 Worker Journal；不得先回成功再丢弃 |
| 已返回 RECEIVED 后资源紧张 | 中央继续承担恢复义务，不能靠删除已接收数据释放空间 |
| 协议/载荷版本不支持 | 明确停止该版本消费并保留证据，不丢弃未知字段后强行接受 |

查询回执本身仍须服务身份认证和访问授权；“过期执行可读取原回执”不等于允许匿名读取其他业务的回执。
未知提交、仍在途事务和真正回滚的区分，需要下一步 Store 的事务/唯一约束/恢复协议保证，本次格式检查不能证明这一点。

本批使用 24.4 §10.2 的以下原因码，不重新命名原有 69 码：

| 原因码 | 对应处置语义 |
|---|---|
| `AUTHZ.EXECUTION_STALE` | STOP_ATTEMPT |
| `CTRL.CAPACITY_UNAVAILABLE` | WAIT_CAPACITY |
| `CONTRACT.INVALID` | QUARANTINE_UNIT |
| `CONTRACT.VERSION_UNSUPPORTED` | WAIT_UPGRADE |
| `IDEMPOTENCY.PAYLOAD_CONFLICT` | QUARANTINE_UNIT |
| `DB.COMMIT_UNKNOWN` | VERIFY_APPLY_RECEIPT |
| `DATA.VERSION_CONFLICT` | RESOLVE_VERSION |
| `OBJECT.HASH_MISMATCH` | QUARANTINE_UNIT |

问题响应使用 occurrence_id 和 code。输入严重损坏时可以没有可信 identity，不编造身份。
它不替代完整 Issue 证据，完整 Issue 格式继续遵守 24.4 §10.4；也不自动授予一个不受业务总预算限制的 `retry=true`。

## 9. 跨语言与版本规则

1. UTC 时间固定为 `YYYY-MM-DDTHH:mm:ss.sssZ`，无时区时间、非法日期不接受。
2. 大整数身份/版本采用字符串；禁止读成 Number 后再写回。
3. 未提供字段和显式 null 不等价；首批必填元数据不接受 null。
4. 保留 Unicode 码点，不擅自做 NFC/NFD 归一化；看起来一样的文本可以有不同 Hash。
5. 正文数值须符合所选 Schema 和 RFC 8785 支持范围，不传 NaN/Infinity，不用浮点数表示精确的大整数。
6. 严格拒绝未知元数据字段，不剥离后声称验证通过；新增字段或改变语义必须发布新契约版本并定义兼容矩阵。
7. 接收版本、正文 schema_id、Hash profile 都要逐一检查；升级不能重写已确认的旧字节。
8. 不自行实现“排序 JSON 就等于完整 JCS”。正式编解码使用兼容实现，并扩充完整数字/Unicode/重复键等验收。

本次黄金向量覆盖大整数字符串、UTC、缺失/null、组合/分解 Unicode、有限小数，能由 Node/Python 共用。
当前测试核对固定规范字节、字节长度和 SHA-256，不表示已经实现通用 JCS 编解码器或验证过所有语言客户端。

## 10. 本地文件和验收

| 文件 | 用途 |
|---|---|
| `24.6_业务通信协议与数据契约_第一阶段.md` | 本说明，给人阅读 |
| `contracts/data-plane/v1-draft.schema.json` | JSON Schema 2020-12，给程序检查元数据结构 |
| `contracts/data-plane/fixtures/*.json` | 提交、查询、两类回执、未观察到、问题、Frame 和编码样例 |
| `contracts/data-plane/fixtures/submission-content.jcs` | 与提交样例匹配的规范 UTF-8 内容，文件末尾没有换行 |
| `test/contracts.test.mjs` | 正例、反例、版本/身份保护、原因码来源和字节/Hash 测试 |

stream/object 两个提交文件表示同一内容在不同传输方式下的独立演示，不是允许运行中随意改写已登记清单。
示例 input/target Hash 对应演示冻结内容，绝不代表真实频道目标已验收。

本地运行：

```bash
npm ci
npm test
```

本批必须通过：全部正常样例；缺身份/冻结目标；非字符串 epoch；未知字段/版本/角色；错误 Hash 格式；非法时间/大小；把 DELIVERED/Plan 完成冒充 Submission 回执；问题冒充成功；字节损坏后 Hash 变化；固定向量一致性。

**Schema 通过只代表格式合格。** 实际 Hash 核验、载荷 Schema 注册、帧组装、限额、认证、执行授权、领域质量、耐久接收、唯一约束和原子应用，必须在后续实现中另行测试。

## 11. 其他边界如何接续

| 边界 | 24.4 已确定的要求 | 后续补充的具体契约 |
|---|---|---|
| Query / Discover | QueryRun 绑定 country/category、窗口与策略，候选可归并 | 查询级身份、分页/候选结果；不能硬套必填 channel_id 的本批 Schema |
| Planner / Starter / Temporal | PG 启动意图、稳定 Workflow ID、重发查原身份 | 启动命令、冻结执行上下文和恢复参数 |
| Data API / Agent | request_id、plan 绑定、输入版本、不可变批次清单 | 逐请求结果与批内部分失败；不能省掉 request_id 套用本批 envelope |
| Domain / Feature / Settlement | 可信领域证明、三 Clock、同事务结算 | 领域回执、Observation 和 Feature 回执 |
| Publication / Business | 与事实同事务 Outbox，业务端 Inbox/Apply | 发布 revision/event_id、持久交付回执 |
| Console / Trace | 可重建、带水位，不作授权真相 | 查询投影、控制命令及版本复核 |

这些边界已在架构层定案，但本次没有宣称它们的全部字段和协议均已实现。
按业务切片逐项补齐，不一次性把所有未来服务的字段猜完。

完成本批定义后，下一项才是：围绕同一份 Submission 契约实现 migration、接收登记/应用回执 Store 及幂等事务测试。
数据库验收通过后再实现 gRPC Data Ingestor、Journal、测试 Worker 和 Temporal 接入。

## 12. 对照来源

权威架构：`24.4_爬虫平台完整重构方案_业务控制台与全链路追踪定案版.md`。

- §9.2～9.7：三身份、T1～T4、本地事务、提交未知与完成口径。
- §10.2、§10.4、§10.7：原有原因码、Issue 格式和兼容/跨语言规则。
- §14.1～14.10：Submission/Frame/Connection、三载荷模式、gRPC、耐久边界、对象和 Journal。
- §24.1、§24.7～24.12：五角色、API/Agent 依赖、恢复和完成证明。
- §25.1：最小业务对象、版本与唯一性要求。

24.4 是架构基线，仍需实际实现验证。发现本草案、数据库设计或运行行为与其冲突时，明确记录证据、影响和修订建议，不机械按任一文档覆盖另一份。用户已确认的业务需求优先；采用调整后同步修改架构、契约与测试，保留修改依据。当前差异登记见 24.7 第 16 节。


## 后续实施记录：有界视频指标 Fast Apply

2026-09-20 新增 `contracts/data-plane/content-metrics-batch.v1-draft.schema.json` 与固定正文样例。
对应内部实现 `services/ingestion/src/metrics-submission.ts`，限制 incremental/delta、1～100 视频、规范正文不超过 1 MiB；暂未接 Manifest/分帧/压缩/对象传输。
正文身份、冻结输入和完整目标需绑定；JCS 编码器仅接受此切片的无损 JSON 子集，并已对照既有黄金字节和 Hash。
成功输出 APPLIED、查询输出 APPLIED/NOT_OBSERVED 已通过本契约 Schema；没有开启 RECEIVED，不伪造 Domain/Plan 成功。
新增内部错误如 IDEMPOTENCY.BATCH_ALREADY_APPLIED 还未冻结为对外 problem 码；网络适配需要先做错误映射，不能跳过 Manifest/正文身份、wire 字节及认证检查。
本记录不将共同 envelope 升格为完整生产 v1，具体实现边界见 `docs/migration/metrics-submission.md`。
