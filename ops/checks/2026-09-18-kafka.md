# Kafka KRaft 验证部署

日期：2026-09-18

## 目标拓扑

```text
S1 10.4.4.2  node.id=1  Broker :9092 + Controller :9093
S2 10.4.4.8  node.id=2  Broker :9092 + Controller :9093
S3 10.4.4.5  node.id=3  Broker :9092 + Controller :9093
```

验证环境使用 Kafka 3.9.1 KRaft combined broker/controller。配置已经设置 `default.replication.factor=3`、`min.insync.replicas=2`，后续 `crawler.publication.v1` 使用三副本。

## 已完成

- 三台节点已安装 Java 17 和 Kafka 3.9.1。
- 三台节点已使用同一个 KRaft cluster id 格式化日志目录。
- Kafka systemd 服务和 256/512 MiB JVM 验证期内存配置已写入。
- Kafka 二进制下载使用华为云 Apache 镜像，安装脚本不包含凭据。

## 当前状态

防火墙规则已应用到 S1/S2/S3，Kafka 三节点已启动并通过验证：

- KRaft quorum 已建立，当前 Leader 为 node 2。
- 三个 Controller voter 为 `10.4.4.2:9093`、`10.4.4.8:9093`、`10.4.4.5:9093`。
- `MaxFollowerLag=0`，三个节点均为正常 voter。
- `crawler.publication.v1` 已创建为 3 分区、复制因子 3、`min.insync.replicas=2`。
- Topic 三个分区的 ISR 均为 `1,2,3`。
- 生产/消费探针 `20260918-kafka-probe` 成功。
- A1 到 S1 的 6432、9092、9093 均可连通。

后续重启或恢复时执行：

```bash
for host in 10.4.4.2 10.4.4.8 10.4.4.5; do
  ssh ubuntu@$host 'sudo systemctl start kafka'
done
/opt/kafka/bin/kafka-metadata-quorum.sh --bootstrap-server 10.4.4.2:9092 describe --status
/opt/kafka/bin/kafka-topics.sh --bootstrap-server 10.4.4.2:9092 --create --if-not-exists \
  --topic crawler.publication.v1 --partitions 3 --replication-factor 3 \
  --config min.insync.replicas=2
```

## 防火墙

入站来源统一为 `10.4.4.0/22`，只应用到 S1/S2/S3：

| 端口 | 用途 |
|---:|---|
| 9092/TCP | Broker 与应用访问 |
| 9093/TCP | KRaft Controller quorum |
