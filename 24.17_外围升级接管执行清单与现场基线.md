# 外围升级接管执行清单与现场基线

日期：2026-09-21。阶段：现场盘点、API/结构检查和隔离运行兼容性验收已完成；线上升级尚未执行。

后续进展：已完成固定制品、Chart 渲染、独立 Kubernetes 1.37.0 API/CRD/schema 检查，以及两节点 LXD 环境中的跨节点网络、Argo、证书、Kafka/Connect、KEDA、监控与 Temporal 运行验证，详见 [兼容性配置与结果](ops/compatibility/kubernetes-1.37/README.md)。线上仍为 1.31.14，没有执行升级。

## 1. 已确定的目标

- 用户明确选择 Kubernetes **1.37.0**，先补齐兼容性验证再升级；后续普通实施细节由助手处理，不再反复确认版本方向。
- 各外围组件目标为本轮官方最新稳定版，来源与完整候选清单见 24.16。实施前重新检查是否有新的稳定修复版；发生兼容性冲突要保留证据，不能偷偷降级或采用开发分支。
- 六台现有服务器，A1–A3 控制与应用、S1–S3 数据服务；对象存储继续服务器自建 SeaweedFS。未来采集 W 节点独立扩展，不将数百 Worker 挤入 S 节点。
- 新仓库为唯一配置来源；主机配置交 Ansible/Pigsty，K8s 配置交 Argo CD，受管子资源交相应 Operator。一个资源只有一个配置管理者。
- 外围阶段不继续开发业务功能、自研常驻基础设施控制器或上游组件分叉。没有新增 SQLite、云对象存储或服务器。

## 2. 本轮现场实测

核查时间约为北京时间 17:07–17:12；本轮只读，没有重启、安装、升级、切主或写入验证消息。

| 节点 | 内网 | 资源与根文件系统可用空间（约） | 现场职责 |
|---|---|---|---|
| A1 | 10.4.4.12 | 4 核 / 8 GB；可用内存 5.1 GiB；根盘空闲 152 GiB | K8s 控制面、本机 API HAProxy、应用 Pod；配置/ClickHouse 备份任务执行点 |
| A2 | 10.4.4.3 | 2 核 / 8 GB；可用内存 5.3 GiB；根盘空闲 60 GiB | K8s 控制面、本机 API HAProxy、应用 Pod；Prometheus 本地卷 |
| A3 | 10.4.4.17 | 2 核 / 8 GB；可用内存 5.0 GiB；根盘空闲 61 GiB | K8s 控制面、本机 API HAProxy、应用 Pod；Prometheus 本地卷；当前 Argo 应用控制器 |
| S1 | 10.4.4.2 | 2 核 / 8 GB；可用内存约 5.9 GiB；80 GiB 单系统盘 | PG 主库、独立 PG etcd、Kafka、SeaweedFS 四类服务、PgBouncer、CDC Guard |
| S2 | 10.4.4.8 | 2 核 / 8 GB；可用内存约 5.9 GiB；80 GiB 单系统盘 | PG 备库、PG etcd、Kafka、SeaweedFS、pgBackRest 仓库及备份计时器 |
| S3 | 10.4.4.5 | 2 核 / 8 GB；可用内存约 5.1 GiB；80 GiB 单系统盘 | PG 备库、PG etcd、Kafka、SeaweedFS；单机 ClickHouse |

六台均有 node_exporter 和 Vector 主机服务。内存可用值为瞬时观测，不是生产容量承诺。S 节点尚未加入 K8s，不存在已上线的 Strimzi Kafka。

实测健康状态：

- K8s 只有 A1–A3 三个节点，全部 Ready；63 个 Pod 记录中没有未就绪的非完成任务 Pod。
- 八个 Argo Application 全部 Synced/Healthy，均指向新仓库。
- PG 主库 S1、两台备库；CDC 只读检查 PASSED、Connect 与任务运行、故障转移槽和 Guard 状态就绪。范围是既有验证 publication，不等于正式业务交付验收。
- 存储类只有 `crawl-monitoring-local`，provisioner 是 `kubernetes.io/no-provisioner`。已有五个 Retain 本地 PV：Prometheus 两个 5 GiB、Alertmanager 三个 1 GiB。没有 Kafka 动态供卷能力。
- S1 `crawl-signal-check.service` 与 S3 `crawl-ch-restore-895d0831361a.service` 为 `Transient=yes`、MainPID=0 的失败临时验证单元；核心服务另行核查为运行态。后续保留证据后可清理失败状态，本轮未执行清理。

数据量观测：PG 数据目录每台约 0.65 GiB；Kafka 实际目录 `/srv/crawlsystem/kafka/logs` 每台约 39 MiB；S3 ClickHouse 约 0.73 GiB。`/var/lib/kafka` 不是实际日志目录，不能用其大小估算迁移容量。当前七个非模板数据库包含 crawler、Grafana、Temporal/visibility 和验证库；不得因名字带 validation 就默认删除。

## 3. 当前版本、目标与配置归属

| 组件 | 现场版本 / 状态 | 目标 | 放置与唯一管理者 |
|---|---|---|---|
| Ubuntu | 六台 24.04.4 LTS | 官方最新 LTS 26.04.1；OS 大版本升级独立批次验证 | Ansible；不得与 K8s/PG 迁移同时重装 OS |
| Kubernetes | A1–A3 1.31.14 | **1.37.0** | kubeadm 管理控制面；主机包由 Ansible 管 |
| containerd / runc | 2.2.1 / Ubuntu runc 1.3.4 | 2.4.0 / 1.5.1 | A 节点及未来加入的 S 节点；包/制品来源与配置格式先验证 |
| Calico | 3.29.3，现有 manifest 安装 | 3.32.2 | Argo 管理固定版本清单；保留 VXLAN、网段及既有策略 |
| Argo CD | 实际二进制 2.13.5，HA 清单；应用控制器单副本 | 3.5.3；候选 Chart 10.9.2 | A 节点；逐版本升级与清单归属迁移，不直接覆盖 |
| Argo Redis | 7.0.15 | 使用经验证的新版 Argo HA 配套依赖并固定摘要 | 不单独将最高 Redis 大版本强塞进旧 Argo 清单 |
| HAProxy | 主机 2.8.16；PG 入口 Pod 3.2.13 | 3.4.4 LTS | 主机入口由 Ansible；K8s 入口由 Argo；分别验收 |
| Pigsty | 尚未安装接管；当前为手工配置 Patroni 栈 | 4.5.0 的 PG 管理方案 | S1–S3 主机服务；不是再建一个数据库集群 |
| PostgreSQL | 17.11，S1 主库、S2/S3 备库 | 18.6 | Pigsty/Patroni；大版本升级必须另做数据与 CDC 槽迁移验收 |
| Patroni | 4.1.5 | 4.1.5，版本已一致 | 接管配置；不能借 Pigsty 安装重置 DCS 或清理数据 |
| PG 专用 etcd | 3.5.24 | 3.7.1，经过 3.6 中间版本 | 主机 Ansible/Pigsty；与 K8s 内置 etcd 分开 |
| PgBouncer / pgBackRest | 1.25.2 / 2.59.1 | 相同版本，已是本轮最新版 | 配置接管与恢复验证；仓库当前在 S2，不能写成 S3 |
| Kafka | S1–S3 systemd 3.9.1，三个 broker/controller | Strimzi 1.2.0 + Kafka/Connect 4.3.1 | S 节点加入 K8s 后承载 Kafka；CR 交 Strimzi，Git 管期望配置 |
| Debezium | 3.6.3.Final，两份 Connect Pod | 相同插件版本，保留实际 connector/offset/slot | 向 Strimzi Connect 迁移；不能仅换镜像而丢位点 |
| Temporal | 1.32.0，两副本 | 相同服务版本；候选 Chart 1.7.0 | Argo；保留现有 default/visibility PG 库和 schema |
| SeaweedFS | S1–S3 4.47 | 相同版本 | 主机 Ansible 配置；不改变元数据 PG、复制与 S3 身份 |
| ClickHouse | S3 26.8.6.5 | 26.8.9.10 LTS | 主机 Ansible；当前单实例，版本升级不等于 HA |
| Vector | 六台 0.58.0 | 相同版本 | 主机 Ansible；保留缓冲、TLS 和日志恢复能力 |
| Prometheus / Alertmanager / Grafana | 3.14.0 / 0.34.1 / 13.2.2 | 相同程序版本；候选 kube-prometheus-stack 91.4.1 / Operator 0.94.0 | Argo 管 Chart/CR；Operator 管生成对象；接管前映射现有 PVC/规则/PG 后端 |
| node_exporter | 六台 1.12.1 | 相同版本，采用原生 collector | 主机 Ansible；K8s Chart 内默认重复 exporter 必须禁用或先迁移 |
| 证书管理 | 现有自建 CA 与静态证书；未部署自动续期组件 | cert-manager 1.21.2；主机 step-ca 0.30.2 / step 0.30.6 | 每张证书唯一管理者；Strimzi 证书由 Strimzi 管，先验证信任迁移和 reload |
| 文件备份 | 自研控制备份与搬运程序 | restic 0.19.1 + 原生快照/备份 | 主机 Ansible/systemd timer；不替代 pgBackRest 的数据库一致备份 |
| 弹性伸缩 / 外部 HTTPS | KEDA / Traefik 尚未部署 | KEDA 2.20.2 / Traefik 3.7.13（按需） | Argo；KEDA 的 1.37 兼容性独立验证，不为安装而安装 |
| CDC Guard | 三 S 自研常驻程序 | 正式成熟能力等价替代待验证 | 保留现有保护、停止扩展；不把 Ansible 分发称为替代完成 |

K8s 内置 etcd/CoreDNS/kube-proxy 按 kubeadm 对应版本配套，不使用 PG etcd 的目标版本覆盖。Chart 与应用版本不同，不通过强行覆盖所有子镜像来伪造“全最新”。

## 4. 先通过的兼容性关卡

1. **固定制品和迁移路径。** 下载官方固定版本 Chart/manifest、CRD、发布说明和签名/摘要；仅在临时目录展开。清点旧 CRD、废弃 API、Helm 默认依赖和既有 GitOps 所有权。
2. **离线检查。** 官方 Helm/Kustomize 渲染，使用 1.37 OpenAPI schema 和对应 CRD 做结构校验；检查 PVC 名称、node affinity、TLS、Secret 引用、PDB、Service 地址。缺 schema 不能记为通过。
3. **隔离运行验证。** 在现有机器可承受的临时隔离环境运行固定的 1.37.0 制品；先确认磁盘、内存上限及官方镜像可用性，不连接现网 PG/Kafka、不导入现网凭据，不以安装到现网的方式“试兼容”。若资源不足，调整测试批次，不能在无隔离条件下冒充已经验证。
4. **组件实际行为。** Calico 的跨节点流量/NetworkPolicy、Argo 渲染与同步、Strimzi 的 CRD/协调和 Kafka 小消息、监控发现、证书签发续期、Temporal SQL 配置分别验证。单节点实验只能证明其覆盖的功能，不能代替跨节点 VXLAN 和主机故障恢复。
5. **记录范围。** 上述通过属于本项目实测，不等于厂商已支持 1.37；当前 Calico/Argo/Strimzi/cert-manager 矩阵到 1.36，KEDA 到 1.35。失败时先采用成熟正式修复版或调整部署方式，不修改上游源码。

关卡 1–5 已在隔离环境完成并留存证据。它证明目标组合在本项目测试范围内能够运行，不替代三节点 HA、生产容量或线上迁移验收。下一步必须先完成 B0 的最新备份与实际恢复，不能直接开始版本替换。

## 5. 执行批次、验收和恢复

### B0：基线与可恢复入口（已通过）

本轮已完成只读基线。2026-09-21 19:00–19:11 CST 又生成了升级前新备份并执行隔离恢复：Calico 网卡修复后的控制面加密档案 `control-20260921T111103Z.tar.gpg` 在 A1/S2 哈希一致；K8s etcd 与 PG 专用 etcd 都能在 loopback 临时实例启动并读取预期记录；新 PG 全备 `20260921-190130F` 能恢复到命名时间点，恢复点前数据存在、恢复点后数据被排除，回执一致。临时 PG 实例已停止、验证 schema 已删除，三节点复制延迟为 0。详细证据见 `ops/checks/2026-09-21-pre-upgrade-recovery-gate.json`。

敏感配置仍放 ignored secrets/受控加密仓库，证据文件只存安全元数据。每个后续重大批次前还要生成对应增量快照，B0 通过不是后续任意时刻都可跳过备份。

在独立分支准备变更并检查 Argo 自动同步触发范围，避免推送准备清单就意外上线。每批次以固定 Git revision 记录前后状态。保持已有 API 本机 HAProxy 和 PG 双入口，不换成单个 S1 地址。

### B1：Argo 维护安排与底座分阶段升级

执行进度（2026-09-21 19:54 CST）：Kubernetes 1.32.13 与 1.33.13 已分别在 A1→A2→A3 顺序升级并通过关卡；Calico 已按 3.29.3→3.30.7→3.31.7 桥接，VXLAN 网卡固定为 eth0，跨节点 Redis Service/两个端点各 100/100 次连接成功。当前下一跳是 Kubernetes 1.34.11，完整过程证据持续写入 `ops/checks/2026-09-21-kubernetes-live-upgrade.json`。

**已发现的具体阻塞：** A3 上 `argocd-application-controller-0` 单副本，PDB `disruptionsAllowed=0`。通用 drain 会阻塞；不能直接用强删绕过。安排短暂发布暂停、保存 application 状态、受控调整该 PDB 的维护条件，控制器恢复后再恢复原策略。暂停 GitOps 发布不等于停采集或数据库。其整机无人值守切换是独立待办。

本轮官方分支入口返回的 Kubernetes 路线：

```text
1.31.14 → 1.32.13 → 1.33.13 → 1.34.11 → 1.35.8 → 1.36.4 → 1.37.0
```

中间版本用于过渡，不是最终停留方案。每一跳先控制面、再节点组件，A1/A2/A3 单台串行，保持其余两个 etcd 成员。执行前按对应版本官方升级文档核对 kubeadm API 与参数，不把跨版本命令机械复用。

Calico 桥接候选：3.29.3 → 3.30.7 → 3.31.7 → 3.32.2。官方测试范围分别为 1.29–1.32、1.31–1.35、1.32–1.35、1.34–1.36。因此在 K8s 1.31/1.32 阶段桥接 3.30，在 1.32 后桥接 3.31，在 1.34 后进入 3.32；最后 1.37 必须有隔离验证记录。保留 VXLAN UDP 4789 配置，不切 CNI 类型。

2026-09-21 隔离 LXD bridge 曾让 A1 的 Calico 默认 `first-found` 误选 `172.31.137.1`，导致跨节点 Pod 流量和 Argo Redis 间歇超时；已将 `IP_AUTODETECTION_METHOD` 固定为 `interface=eth0`，滚动恢复后 Service 与两个 Redis HAProxy 端点各 300/300 次连接成功。后续每个 Calico 版本清单必须保留该设置，不能在升级时退回自动选网卡。证据见 `ops/checks/2026-09-21-calico-interface-recovery.json`。

containerd 路线为 2.2.1 → 2.3.5 → 2.4.0；上游要求连续 minor 升级。每台先检查 CRI、systemd cgroup、sandbox 镜像和配置废弃项，再更新运行时，避免一次升级运行时、CNI、控制面三项。runc 固定 1.5.1 的配套制品与包归属也在此验证。

Argo 2.13.5 → 3.5.3 涉及多个 minor/major；必须沿官方升级说明核对中间版本、RBAC 和 ApplicationSet 行为，所需桥接制品在 B1 实施前锁定。本轮未将“直接换成 3.5.3 镜像”列为可执行操作。避免把清单迁 Chart 与程序跨大版本升级放同一批。

每一跳验收：节点 Ready、API 三个后端与本机入口可用、etcd quorum、DNS、跨节点 Pod 网络和既有 NetworkPolicy、八个应用健康、CDC 恢复原状态、监控仍有采样。没有通过就停止下一跳。

恢复边界：不把 kubeadm 降级当作回退方案。保留当前可服务的健康节点；若升级不可修复，按该阶段快照及配置进行灾难恢复。涉及 etcd/schema 迁移的回退必须考虑一致性，不能直接安装旧包覆盖新数据目录。

### B2：低风险通用运维接管

先将 node_exporter 原生 systemd collector 和标准数据库/Kafka/HTTP 探针接替通用自研采集。新指标/告警比对通过后，再退出旧 timer/exporter；不要仅删除脚本造成监控盲区。

再分批接管监控 Chart、主机 Vector 配置、证书续期和文件备份。监控新旧可短期并行读指标，但不能共同写同一 TSDB/PVC；Grafana 保留现有 PG 后端。证书先分发兼容信任、后换叶证书，验证实际服务 reload 与到期告警。restic 首先备份原生快照产物并做一次恢复，原备份保留至替代验收。

验收：对应监控目标恢复、旧告警功能保留、续期后新连接成功、历史数据或配置能够恢复；失败时恢复原配置/身份/采集器，避免改变事实库数据。

### B3：Pigsty 接管既有 PG 运维

先渲染 Pigsty 4.5.0 配置与现网比较，核对 cluster scope、node ID、PGDATA、DCS/TLS、同步复制、入口、pgBackRest 仓库、Guard/pre_promote。只接管明确职责，不默认启动 Pigsty 全套额外监控，也不执行初始化 `install.yml`/`pgsql.yml` 覆盖现网。

备库先做受控接管，保持 PG 17.11 与现有数据不变；主库后做。此处暂时保留旧 PG 大版本是迁移阶段，不取消 18.6 最终目标。PG 专用 etcd 逐 minor 更新，使用正式版本且保持 quorum；具体 3.6 桥接补丁在实施前锁定。

验收：服务启停方式、配置所有权、连接身份、备份、复制与 CDC 状态一致。没有等价接管方式的部分明确保留，不宣称“装了 Pigsty 就已全部接管”。失败恢复受管配置并继续用现有成熟服务，不能重新 bootstrap 同一数据目录。

### B4：S 节点加入 K8s 与 Strimzi Kafka 迁移

依次加入 S1–S3，作为带数据专用污点的 Worker，**不增加 K8s 控制面/etcd**。先核对 CNI 路由、kubelet/CNI 防火墙、主机服务端口和资源保留；PG/SeaweedFS/ClickHouse 继续主机运行。内网规则不足时仅列实际缺少的规则，不要求所有端口对公网开放。

Strimzi Operator 在 A 节点；Kafka 在 S 三节点反亲和，当前验证规模可用三个 combined broker/controller，生产容量不在本次承诺。采用 `v1` Kafka/KafkaNodePool、TLS、最小权限用户/topic。

迁移采用**独立目录/PV的新集群**，不把现有 systemd Kafka 数据目录交给 Operator，不重用 cluster ID 初始化。初始 Kafka 本地 PV 每节点 10 GiB 为验证预算，Retain、绑定指定 S 节点；它不是自动扩容或磁盘配额。目录在同一系统盘，必须独立配置消息保留及磁盘告警。

迁移窗口内先暂停受控提交并排空消费，保存 topic 配置、分区、消息 ID/计数、consumer offsets、Connect 配置与 offsets、PG slot/publication 状态。明确每一项在新 Kafka 中的映射，不假定旧 offset 在新集群可直接复用。只有验证数据可以重放也不能未经确认删除原库或位点；使用成熟复制/备份手段或保留重放来源。

Connect 与 Debezium 迁移时同一个 PG slot 只有一个活动读取者；停旧连接器后，按已验证的 offset/slot 方案启动新连接器。先小数据投递、去重与无缺漏核对，再切 Ingestor/Temporal 相关客户端。旧 Kafka 保留停写可恢复状态，不能在新集群验收前删除。

验收：三个 broker ISR 满足配置、TLS/ACL 正反例、重启单实例后恢复、既有验证链路成功、消息核对与位点连续。失败先暂停新写入，再依据新旧已确认消息恢复，不能盲切旧地址丢掉新期间已接收的数据。

### B5：数据库/存储版本与恢复收尾

PG 17.11 → 18.6 是独立大版本迁移。先核对官方 pg_upgrade/Patroni/备份支持、逻辑槽升级限制和 CDC 位点，隔离副本执行检查与恢复；不得仅替换数据库二进制或让 18 直接打开 17 目录。停止写入时的迁移、备库重建、验证和回切边界另有具体窗口记录后执行。

ClickHouse 26.8.6.5 → 26.8.9.10 同 LTS 分支补丁升级，先原生备份和可恢复性检查，单实例停机窗口内执行，验证日志继续进入、历史查询与 TTL。SeaweedFS/Temporal 已是目标版本，主要做配置归属与恢复能力收尾。

SeaweedFS 持续备份仍需验证元数据/对象一致恢复、覆盖删除和历史版本；本地副本不是历史备份，也不能宣称同六台机器内备份具备地域级灾备。当前 ClickHouse 单机、CDC Guard 替代、Argo 整机切换分别标记状态，不混成一个“已完整 HA”。

### B6：外围验收结束条件

逐项输出部署版本/配置 revision、健康证据、小数据联通、必要重启/恢复及已知限制。完成后冻结外围范围，再进入业务开发。不存在为通过验收而自动引入更多自研外围程序的步骤。

## 6. 六节点资源实施预算

本节是当前小数据验证阶段的起始预算，不是生产配置下限或硬件隔离保证。

- A1–A3 保留现有应用分布，Operator 放 A 节点。A2/A3 的本地 Prometheus 卷不能随意移动到别的节点；维护期间依靠另一副本采样，不能强行挂载同一个本地卷。
- S1/S2 加入 K8s 时，为 OS 与主机 PG/SeaweedFS 等预留合计约 4 GiB；S3 因 ClickHouse 预留合计约 5 GiB，并留 eviction 余量。落到 kube/system reserved 及调度预算后核对 Allocatable，不能只在文档写“隔离”。
- Kafka 初始单实例预算：request 512 MiB，limit 1.5 GiB，JVM heap 512 MiB，CPU request 250m；实际以小数据验收调整。S3 的余量需同时包含 kubelet/CNI；新旧 Kafka 并存仅限迁移，逐步验证而非长期双跑。
- 节点预留不会自动限制主机服务。现场大部分 PG/Kafka/SeaweedFS systemd 单元未设 MemoryMax；不能一刀切设置过低上限触发数据库 OOM。先依据实际内存与组件原生缓存/线程参数配置，再制定资源限制。
- S3 ClickHouse MemoryCurrent 约 2.3 GiB，包含被计入 cgroup 的缓存，不能直接等同查询进程 RSS；其他主机与 Pod 资源也按同样口径解释。
- S 节点都是 80 GiB 单系统盘。为新 Kafka 分配的 10 GiB PV 要加上旧 Kafka、PG WAL、备份、镜像和日志预算，保留至少约 15 GiB 可用空间作为本轮维护门槛。该值是操作余量，不是性能保证；不足则先调整验证保留量/批次，不默认购买服务器。

## 7. 本轮产物与下一项

第一步产物已完成：六节点版本和职责、健康状态、容量约束、目标版本、配置所有权、执行批次、验收与恢复边界。未完成的制品桥接和兼容性关卡明确保留，不能将本文件称作全部可直接复制执行的安装脚本。

**下一项：执行 B0 的最新备份与隔离恢复验证，再按 B1 路线逐 minor 升级现网。** 用户已授权继续处理，无需再次询问是否改成 1.36 或是否开始普通准备工作。

证据：

- [Kubernetes、Pod、镜像、Argo、PV/PDB 现场盘点](ops/checks/2026-09-21-upgrade-kubernetes-inventory.json)
- [六节点 OS/包/版本与资源](ops/checks/2026-09-21-upgrade-host-inventory.json)
- [主机服务、资源限制及临时失败单元](ops/checks/2026-09-21-upgrade-host-details.json)
- [实际 Kafka 路径与存储用量](ops/checks/2026-09-21-upgrade-storage-details.json)
- [应用二进制版本及 CDC 只读健康检查](ops/checks/2026-09-21-upgrade-application-readiness.json)
- [官方最新版本证据](ops/checks/2026-09-21-official-component-versions.json)
- [Kubernetes 1.37 API/结构兼容性证据](ops/checks/2026-09-21-k8s137-api-compatibility.json)
- [Kubernetes 1.37 隔离运行兼容性证据](ops/checks/2026-09-21-k8s137-runtime-compatibility.json)
- [现网升级前备份与隔离恢复关卡](ops/checks/2026-09-21-pre-upgrade-recovery-gate.json)
- [Calico 网卡误选诊断与恢复证据](ops/checks/2026-09-21-calico-interface-recovery.json)
- [Kubernetes/Calico 现网逐级升级证据](ops/checks/2026-09-21-kubernetes-live-upgrade.json)

官方升级依据：[kubeadm 升级](https://kubernetes.io/docs/tasks/administer-cluster/kubeadm/kubeadm-upgrade/)、[containerd 升级路径](https://github.com/containerd/containerd/blob/main/RELEASES.md)、[Calico 升级](https://docs.tigera.io/calico/latest/operations/upgrading/kubernetes-upgrade)、[Calico 3.30](https://docs.tigera.io/calico/3.30/getting-started/kubernetes/requirements)、[Calico 3.31](https://docs.tigera.io/calico/3.31/getting-started/kubernetes/requirements)、[Argo 升级说明](https://argo-cd.readthedocs.io/en/stable/operator-manual/upgrading/overview/)。
