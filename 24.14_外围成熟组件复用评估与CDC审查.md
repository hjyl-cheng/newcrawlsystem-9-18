# 外围成熟组件复用评估与 CDC 审查

日期：2026-09-21。用户要求：外围优先采用成熟组件，认真评估完整方案；不确定的功能和可用性取舍先讨论，不继续用自写控制逻辑填补所有缺口。

本轮是审查，未修改线上配置、重启服务、切主、删除复制槽或更换组件。只读核对了三台 S 节点，运行了已有的 18 项本地 CDC 单元测试。机器证据见 `ops/checks/2026-09-21-cdc-reuse-audit.json`。其余外围条目是现有实现与官方能力的选型复核，尚未在本环境验证替换方案。

## 1. 结论与实施原则

大部分外围需求已有成熟组件。现有 PostgreSQL、Patroni、pgBackRest、Kafka、Debezium、Prometheus、Grafana、Vector、Kubernetes、Calico、Argo CD 均应继续复用。主要需要减少的是围绕它们编写的配置管理、证书维护、通用采集和备份编排。

不能只用“这些都是脚本”解释维护成本。常驻 CDC guard 会改变数据库参数、维护接管名单并阻止不符合条件的晋升，属于需要长期承担正确性责任的自研控制逻辑，优先审查。

选型从完整受支持方案出发：

1. 明确实际需要的故障行为，再查成熟组件的正式版本和支持范围。
2. 优先采用组件配置和官方运维方式；配置、标准客户端接入、少量 hook 不等于重新实现基础设施。
3. 如果现有拓扑让自研变多，评估换部署方式，而不是默认继续补代码。
4. “开源项目已有代码”“开发分支已合并”“正式版具备”“本环境验证通过”分别标记，不混为一谈。
5. 引入成熟组件必须真正接替原职责，不能在自写控制器之外再叠一层 Operator。
6. 不自动接受降低可用性、丢失已确认事件或改变六机部署方式；这种取舍要先与用户讨论。

TypeSafe 技能与在线文档索引已读取。本轮不调用 AI 判断数据库晋升、WAL 安全或证书身份；这些使用确定性规则和可核验的实现证据。

## 2. 外围能力逐项复核

| 能力 | 已实施 | 成熟方案与建议 | 必须核对的边界 |
|---|---|---|---|
| PG 选主、复制、连接入口 | Patroni / etcd / HAProxy / PgBouncer | 保留；优先评估 Pigsty 接管部署与配置管理 | Pigsty 使用上述成熟组件，不代表额外解决所有 CDC 槽切换问题；既有集群不能直接重跑初始化 |
| PG 备份与 WAL | pgBackRest | 保留，不替换 | 现有仓库故障域、异地副本另行处理 |
| 主机配置与扩节点 | 多份 Python/Shell 经 SSH 安装、分发、重启 | 优先 Pigsty 受支持模块及 Ansible；逐步减少重复部署逻辑 | 同一个配置文件只由一个管理方负责；不用 Ansible 和 Argo 同时管理同一资源 |
| K8s 发布与网络权限 | Argo CD / Calico / NetworkPolicy / RBAC | 保留 | 不因减少脚本而换 CNI 或额外引入全套 Service Mesh |
| 证书签发与续期 | OpenSSL + 多份自写签发/分发脚本；自动轮换未完成 | K8s 证书评估 cert-manager；主机证书评估 step-ca/step 官方续期工具和 systemd 配套 | 签发、根信任迁移、续期、文件权限、应用重新加载、CA 自身恢复须形成闭环；不能只装 cert-manager 就宣称六机完成 |
| Kafka 加密和授权 | 原生 TLS；ACL 尚未启用 | 保留原生 TLS / StandardAuthorizer；主机部署先用成熟配置管理 | 不开发 ACL 引擎；原生能力不能省去 topic/group 规则配置 |
| Kafka 全生命周期 | S 节点 systemd 服务 + 滚动迁移脚本 | 若决定 Kafka 迁入 K8s，评估 Strimzi 接管 TLS、用户/ACL、维护 | Strimzi Cluster Operator 不会原地接管这三套主机进程；迁移需处理数据、存储、地址和消费者位点 |
| 通用指标与存活检查 | node_exporter / kafka_exporter / KSM + 自写 collect-local、infra-exporter | 标准部分改用组件原生 metrics、postgres_exporter、Blackbox Exporter；Connect 任务状态评估 JMX 或合适 JSON exporter | HTTP 200 不代表 Connect 任务 RUNNING；专用备份成功时间等仍可用少量 textfile 指标 |
| 配置/快照文件备份 | 自写 tar/GPG/SSH/保留清理 | 评估 restic 等成熟工具接管加密、版本、校验、保留策略；etcd 先生成官方一致快照 | 文件备份工具不负责数据库一致性，不能直接打包在线 PG/etcd 数据目录 |
| ClickHouse 备份 | 原生 BACKUP + 自写搬运与保留脚本 | 保留原生备份能力，评估原生远端存储或成熟备份工具管理 | 不为了消除少量配置再新增一套存储依赖；需验证当前版本和恢复格式 |
| SeaweedFS 持续备份 | 目前只有小规模短停维护备份 | 优先评估 SeaweedFS 原生 filer.backup / 复制能力与独立目标 | 同步副本不自动防止误删传播，不自动提供历史恢复点；必须验收删除/覆盖、元数据和对象的一致恢复 |
| 日志采集与查询 | Vector / ClickHouse / Grafana | 保留，清洗规则放在成熟组件配置中 | 自写容量清理和 TTL 不等价；如替换必须保留当前磁盘保护或改变存储配置后重新验证 |

这些是分项替换方向，不是全部已定案或已部署。尤其不应同时部署 Pigsty 全套监控和现有全套监控，制造第二套维护负担。

## 3. CDC 现场事实

- 三节点均为 PostgreSQL 17.11、Patroni 4.1.5；S1 主、S2/S3 备。
- 当前严格同步模式启用，`synchronous_node_count=1`。
- PG 原生 `sync_replication_slots=on`、`hot_standby_feedback=on`；Debezium 使用 `slot.failover=true`。
- 三机常驻 guard 与仓库源码 SHA256 一致；均有 Patroni `pre_promote` hook。
- 健康时 S1 的 CDC 屏障为 `s2,s3`，三机槽有效，Connect 与任务 RUNNING；只读健康检查 PASSED。
- 已有单元测试 18 项通过。其中 13 项 guard 测试与 5 项旧槽维护测试使用 mock；这不是本轮重新做了故障演练。

guard 实际承担四件事：

1. 动态修改 `synchronized_standby_slots`，在备库离线时减少 CDC 等待对象。
2. 在 etcd 保存被允许接管的成员名单和 WAL 准入位点，并通过 `pre_promote` 执行约束。
3. 旧主作为备库返回后，在严格条件下删除本地非 synced 旧槽，交给 PG 原生同步重建。
4. 对未完成初始同步的备库，定期 CHECKPOINT 并向验证 Outbox 写恢复标记。

PG 选主仍由 Patroni 负责，guard 不自行 promote。但它已成为 PG 自动切主的依赖：槽或政策异常时会阻止晋升。这种取舍需要明确，不能只称为“CDC 小脚本”。

## 4. CDC 成熟方案对比

### 4.1 PostgreSQL 17 原生能力：保留，但不单独解决成员变更

PG17 已负责 failover slot 的持续同步。`synchronized_standby_slots` 填的是物理备库槽，逻辑发送要等待列表中的所有备库确认 WAL；它不是自动选择一个健康备库的调度器。

因此，固定填两台备库可以保护两台的接管条件，但任一备库长期离线就可能暂停 CDC。直接删掉等待条件也不是等价替换：下游可能已经看到了尚未到达未来新主的数据。

PG 官方文档明确：槽同步是异步的，接管前仍要核对槽存在、synced、非 temporary、未失效，以及下游进度与备库的关系。不能把“打开 failover=true”当作完整自动恢复方案。

### 4.2 Patroni：方向匹配，但必须区别正式版与开发分支

本轮 GitHub Release 查询最新正式版为 **v4.1.5（2026-08-12）**。

上游 PR **#3527** 已于 **2026-09-03** 合并：`manage_synchronized_standby_slots` 让 Patroni 根据同步复制成员管理 PG17+ 的 CDC 屏障。功能默认关闭；严格同步模式缺少合格备库时有保守等待逻辑。

这与当前 guard 的一项核心职责直接对应。但 v4.1.5 标签源码和三机安装代码均不含该功能；不能把开发分支文档当成当前正式版配置。核对的 master 为 `eec38cd0b8f51d23c8a965515bd0df6dd05a5df1`，功能提交为 `a092a75917c5654d6a336648c9853ee9d77c8a72`。

上游 master 还包含旧主返回后清理 `failover=true, synced=false` 槽的处理；本次核对的 v4.1.5 文件与三机安装代码不含该方法。发布说明、合并时间、分支之间不能仅凭版本号推断，后续必须核对拟安装制品的实际代码。

**结论：优先跟踪正式版接替 guard，而非扩写自己的控制器；当前尚不能批准整段删除。** 新能力是否覆盖接管前槽就绪检查、旧主恢复及现有所有故障场景，仍需隔离验收。清除自写 `ALTER SYSTEM` 遗留覆盖、移交参数所有权也是迁移内容。

### 4.3 CloudNativePG：正式版具备相关能力，但不是无条件满足全部要求

核对 **v1.30.0** 正式版文档与源码，已支持 `replicationSlots.highAvailability.synchronizeLogicalDecoding=true`，并管理 PG17 的 `synchronized_standby_slots`。

进一步检查 `pkg/management/postgres/configuration.go`：它从 `cluster.Status.InstanceNames` 中收集除主库外的实例槽；这段逻辑没有按 Ready 状态过滤。离线但仍在成员清单中的备库，仍可能位于 CDC 等待列表中。因此不能凭“支持逻辑槽 HA”就承诺“一台备库离线时 CDC 自动不停”。需核对实例移除/重建、准入与故障时序并实测。

此外，CloudNativePG 管理 Kubernetes 内的 PG 实例。现有三台 S 上的 Patroni/systemd 集群不能原地加一个 Operator 就接管。迁移涉及：S 节点是否加入 K8s、专用调度与持久卷、备份体系、PG 数据迁移、业务连接地址，以及 Debezium 槽和 Kafka offset 连续性。

**结论：可作为完整部署方案候选，尚不足以为当前 CDC 问题推荐立即迁移。** 不因为 Operator 成熟，就跳过“它是否真的解决本例的可用性要求”的验证。

### 4.4 Pigsty：优先评估主机运维接管，不把它当成新的 CDC 算法

24.4 已列出 Pigsty/pgBackRest。Pigsty 将 Patroni、etcd、HAProxy、PgBouncer、pgBackRest、监控等组合成受管理方案，适合当前数据服务在 Linux 主机上的方向。此前应更早评估该完整方案，减少重复安装和配置脚本。

本轮确认官方项目与 v4.5.0 发布；未对现有集群执行其 playbook，也未完成无损接管验证。它底层仍依赖相应版本的 PG/Patroni，不因使用 Pigsty 就自动补齐上节的正式版能力缺口。需确认所用 Patroni 制品、配置差异、目录、端口和监控是否冲突。

### 4.5 pg_failover_slots：成熟扩展候选，不直接叠加

核对 v1.2.0 文档，具备槽同步、指定备库 WAL 屏障，以及 `standby_slots_min_confirmed` 最少确认数设置。

“任一备库确认就放行”不自动证明 Patroni 后续选中的恰好是保有该 WAL 的备库。它仍需要与晋升资格、槽就绪及物理同步规则一致。PG17 已使用原生槽同步，不能让两个管理方同时操作同一个槽，或只调最少确认数就宣称替代 guard。

**结论：保留为对比候选；未确认它能在当前组合下完整替代，不安排线上接入。**

## 5. 审查发现与处理建议

### 优先修复：guard 的远程 SQL 未校验服务端身份

`ops/cdc/ha-guard.py` 的 `Guard.query()` 未传入 `sslmode=verify-full` 和 CA 路径。三机常驻进程均无相关 PGSSL 环境设置。

本轮通过原方法执行只读 `SELECT 1`，记录官方驱动返回的有效连接参数：六条跨机路径都是 **sslmode=prefer、sslrootcert 未指定、ssl_in_use=true**。即已加密但没有要求可信证书和名称校验；本地 Unix socket 不属于跨机 TLS 通道。

修复应使用 psycopg/libpq 原生参数或统一的受控配置，并同时覆盖常驻 guard 与 Patroni 调用的 hook。无需编写 TLS SDK。之前“全部 SQL 调用方均 verify-full”的总括表述需要修正，既有业务客户端验收不能覆盖这个遗漏。

### 需要收敛：恢复标记耦合验证 Outbox

`progress_pending()` 依赖 `cdc_validation.outbox` 并使用 postgres 权限执行 CHECKPOINT/INSERT，固定三台节点、一个库和一个槽。它不是可直接迁往生产的通用恢复组件。

Debezium 已有 `heartbeat.interval.ms`、`heartbeat.action.query`。后续优先利用原生心跳和独立受限心跳表处理空闲库位点推进，但不能未经验证就声称心跳替代了所有初次槽同步所需的 CHECKPOINT 或恢复步骤。

### 需要控制：同一参数和槽不允许多个管理者竞争

guard 通过 `ALTER SYSTEM` 写入 auto.conf，覆盖 Patroni 的保守参数。未来启用 Operator、Patroni 原生动态屏障或扩展前，必须把参数与槽的所有权移交；不能仅启动新组件并停掉旧进程。

### 测试边界

已有测试覆盖名单变更顺序、CAS 拒绝、多个晋升拒绝条件及旧槽修复条件；本轮复跑通过。未因此证明任意整机冻结、全部网络分区、双节点故障、WAL 超限、所有并发时序或任意新拓扑安全。

常驻 guard 与 pre_promote 使用同一个本地锁，hook 有 12 秒超时；常驻循环没有统一的整轮截止时间。网络异常导致锁等待时可能影响晋升可用性，需在替代方案验收中覆盖。本轮没有对线上制造该故障，不把静态风险描述成已观察到的事故。

## 6. 推荐路径和待讨论取舍

推荐先保持六机拓扑，缩减维护面：

1. 先用官方驱动配置修正 guard SQL 身份校验遗漏，并验证错误 CA/名称拒绝；本轮只列为待修，未修改线上。
2. 对主机运维进行 Pigsty/Ansible 的配置差异评估；优先接管新增/可隔离模块，不重建现有数据库。
3. 证书、标准指标、文件备份分别采用成熟工具，验证接替后停用对应旧实现；不同时迁移全部外围。
4. CDC guard 冻结新功能，保留必要保护，跟踪正式版能力及其他成熟方案的完整替代证据。
5. 如果要改变数据层部署方式，先对 CNPG/Strimzi 做隔离原型和资源/存储评估，再给出逐项迁移与回滚清单。不能把整层迁移当作一次普通升级。

本轮没有确认到一套在现有版本与拓扑下、无需自写控制逻辑且已经覆盖全部现有 CDC 故障要求的直接替代。也不能由此推导“只能继续自研”：还可以选择受支持的完整拓扑、等待正式版、或明确接受有限的同步暂停。

需要用户决定的实际取舍是：如果当前正式版成熟方案暂时只能安全等待，是否接受 **PG 继续服务、CDC 在备库离线期间暂时积压，恢复后在 WAL 保留预算内补发**。这会降低此前已经要求的 CDC 自动恢复能力，不能默认接受；超出 WAL 保留预算还需要明确恢复方案，不能承诺无限离线后自动补齐。

在没有改变该要求前，仍以原要求为准：不删除现有保护，也不把开发分支作为成熟正式版上线。本次不要求立即选择；先把已确认的通用运维复用项与这个具体取舍分开讨论。

## 7. 官方依据与版本

- PostgreSQL 17：[槽同步](https://www.postgresql.org/docs/17/logicaldecoding-explanation.html#LOGICALDECODING-REPLICATION-SLOTS-SYNCHRONIZATION)、[逻辑复制故障切换](https://www.postgresql.org/docs/17/logical-replication-failover.html)、[synchronized_standby_slots](https://www.postgresql.org/docs/17/runtime-config-replication.html#GUC-SYNCHRONIZED-STANDBY-SLOTS)。
- Patroni：[v4.1.5 正式版](https://github.com/patroni/patroni/releases/tag/v4.1.5)、[该版本槽管理源码](https://github.com/patroni/patroni/blob/v4.1.5/patroni/postgresql/slots.py)、[动态屏障 PR #3527](https://github.com/patroni/patroni/pull/3527)、[旧槽恢复 PR #3493](https://github.com/patroni/patroni/pull/3493)。已合并不等于已进入所用正式制品。
- CloudNativePG：[v1.30.0 复制文档](https://github.com/cloudnative-pg/cloudnative-pg/blob/v1.30.0/docs/src/replication.md)、[屏障配置源码](https://github.com/cloudnative-pg/cloudnative-pg/blob/v1.30.0/pkg/management/postgres/configuration.go#L371)、[外部库迁移边界](https://github.com/cloudnative-pg/cloudnative-pg/blob/v1.30.0/docs/src/bootstrap.md)。
- Pigsty：[官方项目](https://github.com/pgsty/pigsty)、[v4.5.0](https://github.com/pgsty/pigsty/releases/tag/v4.5.0)、[PG 集群配置](https://pigsty.io/docs/pgsql/config/cluster/)。
- pg_failover_slots：[v1.2.0 文档](https://github.com/EnterpriseDB/pg_failover_slots/blob/v1.2.0/README.md)。
- Debezium：[3.6 PostgreSQL 文档源码](https://github.com/debezium/debezium/blob/3.6/documentation/modules/ROOT/pages/connectors/postgresql.adoc)，用于核对 slot.failover 与 heartbeat.action.query。本次读取到的 failover 配置示例把逻辑槽名用于 synchronized_standby_slots，与 PG 官方定义不符；该参数以 PG 官方物理备库槽定义为准，不照搬该例。
- 证书：[cert-manager Certificate 生命周期](https://cert-manager.io/docs/usage/certificate/)、[step 官方证书续期及 systemd 示例](https://smallstep.com/docs/step-ca/renewal/)。
- Kafka：[Strimzi 官方概览](https://strimzi.io/docs/operators/latest/overview.html)。
- 监控：[postgres_exporter](https://github.com/prometheus-community/postgres_exporter)、[Blackbox Exporter](https://github.com/prometheus/blackbox_exporter)。
- 备份：[restic 官方入门](https://restic.readthedocs.io/en/stable/010_introduction.html)。

Patroni ReadTheDocs 本轮返回 403，已改读 GitHub 的固定版本源码、文档及官方 Release API。Ansible 入门页返回 429，未将该页面作为能力已核验的证据。候选组件的“可选”不等于已验证部署兼容。
